-- Protected Script (Spanish)
-- Script ID: 2b752b6256975ee3b5e5233cc8d5e740
-- Created: 2025-10-27T13:59:27.385Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()


local moduleLink = "https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/AnimeRise"
local Module = loadstring(game:HttpGet(moduleLink))()

local Options = Library.Options
local Toggles = Library.Toggles

local Window = Library:CreateWindow({
    Title = "COMBO_WICK",
    Footer = "Version: 1.0",
    NotifySide = "Right",
    ShowCustomCursor = true,
    Size = UDim2.new(0, 460, 0, 480),
})

local Tabs = {
    Main = Window:AddTab("Principal", "user"),
    Pets = Window:AddTab("Animales de compañía", "package"),
    Misc = Window:AddTab("Varios", "sparkles"),
    ["UI Settings"] = Window:AddTab("Configuración de la interfaz", "settings"),
}

local LeftGroupBox = Tabs.Main:AddLeftGroupbox("Agricultura automática")
local RightGroupBox = Tabs.Main:AddRightGroupbox("Selección de NPC")
local MiscGroupBox = Tabs.Misc:AddLeftGroupbox("Varios")
local PetGroupBox = Tabs.Pets:AddLeftGroupbox("Gestión de mascotas")
local ShadowGroupBox = Tabs.Pets:AddRightGroupbox("Esencia de la sombra")

LeftGroupBox:AddToggle("AutoRank", {
    Text = "Ranking automático",
    Default = false,
    Callback = function(Value)
        getgenv().AutoRank = Value
    end,
})

LeftGroupBox:AddToggle("Autoataque", {
    Text = "Ataque automático",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAttack = Value
    end,
})

LeftGroupBox:AddToggle("GrabDrops", {
    Text = "Recoger elementos caídos",
    Default = false,
    Callback = function(Value)
        getgenv().GrabDrops = Value
    end,
})

LeftGroupBox:AddToggle("AutoBuyZone", {
    Text = "Zona de compra automática",
    Default = false,
    Callback = function(Value)
        getgenv().AutoBuyZone = Value
    end,
})

LeftGroupBox:AddToggle("AutoAr", {
    Text = "Renacimiento automático de la Ascensión",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAr = Value
    end,
})

LeftGroupBox:AddToggle("AttackNPC", {
    Text = "Personaje de no jugador de ataque",
    Default = false,
    Callback = function(Value)
        getgenv().AttackNPC = Value
    end,
})

LeftGroupBox:AddToggle("Ataque automático más cercano", {
    Text = "Ataque automático al enemigo más cercano",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAttackNearest = Value
    end,
})

Module.availableNPCs = Module.scanAllNPCs()
table.insert(Module.availableNPCs, 1, "None")

Module.NPCDropdown = RightGroupBox:AddDropdown("NPCSelector", {
    Values = Module.availableNPCs,
    Default = 1,
    Multi = false,
    Text = "Seleccionar el carácter de no jugador",
    Callback = function(Value)
        Module.selectedNPC = Value
    end,
})

RightGroupBox:AddButton({
    Text = "Entra en la Batalla de Raid",
    Func = function()
        Module.joinRaid()
    end,
})

RightGroupBox:AddToggle("AutoJoinRaid", {
    Text = "Entra automáticamente en la batalla de raid",
    Default = false,
    Callback = function(Value)
        getgenv().AutoJoinRaid = Value
    end,
})

PetGroupBox:AddToggle("AutoEquipMejor", {
    Text = "Equipar automáticamente las mascotas más fuertes",
    Default = false,
    Callback = function(Value)
        getgenv().AutoEquipBest = Value
    end,
})

PetGroupBox:AddSlider("EquipInterval", {
    Text = "Tiempo de espera del equipo (segundos)",
    Default = 15,
    Min = 5,
    Max = 600,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        getgenv().EquipInterval = Value
    end,
})

PetGroupBox:AddButton({
    Text = "Mascotas más fuertes (manuales)",
    Func = function()
        Module.equipBestPets()
    end,
})

PetGroupBox:AddToggle("AutoRollEggs", {
    Text = "Huevos de porte automático",
    Default = false,
    Callback = function(Value)
        getgenv().AutoRollEggs = Value
    end,
})

PetGroupBox:AddDropdown("EggMap", {
    Values = {"Lost Temple", "Desert", "Snow", "Volcano"},
    Default = 1,
    Multi = false,
    Text = "Seleccione el mapa para el egg hatching",
    Callback = function(Value)
        getgenv().SelectedEggMap = Value
    end,
})

ShadowGroupBox:AddLabel("Shadow Essence Generator")
ShadowGroupBox:AddInput("ShadowAmount", {
    Default = "9e15",
    Numeric = false,
    Finished = false,
    Text = "Monto",
    Placeholder = "Enter amount (example: 9e15)",
})

ShadowGroupBox:AddButton({
    Text = "Generar Esencia de Sombras Infinitas",
    Func = function()
        local amount = Options.ShadowAmount.Value
        if amount == "" then return end
        Module.giveShadowEssence(amount)
    end,
})

MiscGroupBox:AddToggle("AntiAFK", {
    Text = "Anti-lejos del teclado",
    Default = false,
    Callback = function(Value)
        getgenv().AntiAFK = Value
    end,
})

local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Menú")

MenuGroup:AddToggle("KeybindMenuAbrir", {
    Default = Library.KeybindFrame.Visible,
    Text = "Mostrar menú de accesos rápidos del teclado",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})

MenuGroup:AddToggle("MostrarCustomCursor", {
    Text = "Cursor de ratón personalizado",
    Default = true,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})

MenuGroup:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = "Right",
    Text = "Posición de la notificación",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})

MenuGroup:AddDivider()
MenuGroup:AddLabel("Menu Hotkey"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Tecla rápida del menú" })

MenuGroup:AddButton("Close Script", function()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("AutoFarm")
SaveManager:SetFolder("AutoFarm/game")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])

Module.startLoops()