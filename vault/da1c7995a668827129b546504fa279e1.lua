-- Korean Translation
-- Script ID: da1c7995a668827129b546504fa279e1
-- Created: 2026-05-12T19:35:08.550Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo         = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library      = loadstring(game:HttpGet(repo.."Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo.."addons/ThemeManager.lua"))()
local SaveManager  = loadstring(game:HttpGet(repo.."addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

local MODULE_URL = "https://raw.githubusercontent.com/checkurasshole/Script/main/catchandtamemodule"
local M = loadstring(game:HttpGet(MODULE_URL))()(Library)

local Window = Library:CreateWindow({
    Title            = "COMBOWICK",
    Footer           = "자동 포획 시스템 ",
    ShowCustomCursor = false,
    Size             = UDim2.new(0,400,0,570),
})

local Tabs = {
    AutoCatch       = Window:AddTab("자동 포획 ",  "zap"),
    PenManager      = Window:AddTab("펜 관리 ", "home"),
    Teleports       = Window:AddTab("텔레포트 ",   "map-pin"),
    Codes           = Window:AddTab("코드 ",       "gift"),
    Misc            = Window:AddTab("기타 ",        "settings"),
    ["UI Settings"] = Window:AddTab("UI 설정 ", "settings"),
}

local CatchGroup = Tabs.AutoCatch:AddLeftGroupbox("자동 포획 ")

CatchGroup:AddToggle("AutoCatchEnabled",{
    Text="자동 포획 활성화 ",Default=false,
    Callback=function(v)
        M.AutoCatchState.Enabled=v
        if v then
            Library:Notify({Title="자동 포획 ",Description="시작됨! ",Time=2})
            task.spawn(M.autoCatchLoop)
        end
    end,
})
CatchGroup:AddDivider()
CatchGroup:AddSlider("CatchDelay",{
    Text="포획 간격 (초) ",Default=1,Min=0.1,Max=5,Rounding=1,Compact=false,
    Callback=function(v) M.AutoCatchState.CatchDelay=v end,
})
CatchGroup:AddSlider("TeleportDelay",{
    Text="텔레포트 지연 (초) ",Default=0.3,Min=0.1,Max=2,Rounding=1,Compact=false,
    Callback=function(v) M.AutoCatchState.TeleportDelay=v end,
})
CatchGroup:AddDivider()
CatchGroup:AddButton({
    Text="포획 기록 지우기 ",
    Func=function()
        M.AutoCatchState.CaughtPets={}
        Library:Notify({Title="지워짐 ",Description="포획 기록이 초기화되었습니다. ",Time=2})
    end,
})
CatchGroup:AddDivider()
CatchGroup:AddLabel("수동 포획 보조 ")
CatchGroup:AddLabel("올가미를 평소처럼 던지면 미니게임이 자동으로 완료됩니다. ")
CatchGroup:AddToggle("ManualCatchEnabled",{
    Text="포획 보조 활성화 ",Default=false,
    Callback=function(v)
        M.ManualCatchState.Enabled=v
        if v then
            M.startWatching()
            Library:Notify({Title="포획 보조 ",Description="포획 창이 나타나기를 기다리는 중입니다. ",Time=3})
        else
            M.stopWatching()
        end
    end,
})

local StatusGroup = Tabs.AutoCatch:AddRightGroupbox("상태 ")
local execIcon = M.execWarn and "Warning" or "Ready"
StatusGroup:AddLabel(execIcon.." Executor: "..M.execStatus.." ",true)
StatusGroup:AddDivider()
local islandLabel  = StatusGroup:AddLabel("섬: 스캔 중... ",  true)
local caughtLabel  = StatusGroup:AddLabel("세션 포획 수: 0 ",    true)
local nearbyLabel  = StatusGroup:AddLabel("근처 펫: 0 ",       true)
local bossMapLabel = StatusGroup:AddLabel("맵의 보스 수: 0 ",     true)
local weatherLabel = StatusGroup:AddLabel("날씨: 확인 중... ", true)
local eventLabel   = StatusGroup:AddLabel("활성 이벤트: 없음 ",   true)
local bossLabel    = StatusGroup:AddLabel("보스: — ",              true)
StatusGroup:AddDivider()
StatusGroup:AddLabel("── 희귀도 필터 ── ")
StatusGroup:AddDropdown("RarityFilter",{
    Text="희귀도 포획",Values=M.KnownRarities,Default={},Multi=true,
    Tooltip="비워두면 모든 희귀도를 포획합니다. ",
    Callback=function(v) M.AutoCatchState.SelectedRarities=v end,
})
StatusGroup:AddDivider()
StatusGroup:AddLabel("── 날씨 필터 ── ")
StatusGroup:AddDropdown("WeatherFilter",{
    Text="활성 날씨",Values=M.KnownWeathers,Default={},Multi=true,
    Tooltip="비워두면 모든 날씨에서 포획합니다.\n'Any' = 활성 이벤트 중일 때만 포획합니다.\n특정 선택 = 선택한 날씨에서만 포획합니다. ",
    Callback=function(v)
        local set={}
        for w, en in pairs(v) do if en then set[w]=true end end
        M.AutoCatchState.SelectedWeathers=set
    end,
})
StatusGroup:AddDivider()
StatusGroup:AddLabel("── 변이 필터 ── ")
StatusGroup:AddToggle("MutatedOnly",{
    Text="변이 펫만 ",Default=false,Tooltip="변이가 0인 펫은 건너뜁니다. ",
    Callback=function(v) M.AutoCatchState.MutatedOnly=v end,
})
StatusGroup:AddDropdown("MutationFilter",{
    Text="특정 변이 ",Values=M.KnownMutations,Default={},Multi=true,
    Tooltip="비워두면 변이 필터를 적용하지 않습니다.\n'Any' = 변이된 아무 펫이나 포획합니다.\n특정 선택 = 선택한 변이만 포획합니다.",
    Callback=function(v)
        local set={}
        for mut, en in pairs(v) do if en then set[mut]=true end end
        M.AutoCatchState.SelectedMutations=set
    end,
})

local FeedGroup = Tabs.PenManager:AddLeftGroupbox("자동 먹이 주기 ")
FeedGroup:AddDropdown("FoodSelect",{
    Text="음식",Values=M.FoodSelectValues,Default="농부의 사료",
    Callback=function(v) M.PenManagerState.FoodName=v end,
})
FeedGroup:AddDivider()
local initialPetLabels  = M.buildPenPetLabels()
local PetSelectDropdown = FeedGroup:AddDropdown("PetSelect",{
    Text="먹이를 줄 펫 ",Values=initialPetLabels,Default={},Multi=true,
    Callback=function(v)
        M.PenManagerState.SelectedPetUUIDs={}
        for label, enabled in pairs(v) do
            if enabled then
                local uuid = M.penPetLabelToUUID[label]
                if uuid then table.insert(M.PenManagerState.SelectedPetUUIDs,uuid) end
            end
        end
    end,
})
FeedGroup:AddButton({
    Text="펫 목록 새로고침 ",
    Func=function()
        local labels = M.buildPenPetLabels()
        PetSelectDropdown:SetValues(labels)
        M.PenManagerState.SelectedPetUUIDs={}
        Library:Notify({Title="먹이 주기 ",Description="펫 목록이 새로고침되었습니다 ("..#labels.."마리). ",Time=2})
    end,
})
FeedGroup:AddDivider()
FeedGroup:AddSlider("FeedCycleDelay",{
    Text="간격 (초) ",Default=30,Min=0.5,Max=300,Rounding=1,Compact=false,
    Callback=function(v) M.PenManagerState.FeedDelay=v end,
})
FeedGroup:AddToggle("AutoFeedEnabled",{
    Text="자동 먹이 주기 활성화 ",Default=false,
    Callback=function(v)
        M.PenManagerState.AutoFeedEnabled=v
        if v then
            local pen = M.findMyPen()
            if pen then
                Library:Notify({Title="자동 먹이 주기 ",Description="먹이 주는 중: "..M.PenManagerState.FoodName.." ",Time=2})
                task.spawn(M.autoFeedLoop)
            else
                M.PenManagerState.AutoFeedEnabled=false
                Toggles.AutoFeedEnabled:SetValue(false)
                Library:Notify({Title="자동 먹이 주기 ",Description="펜을 찾을 수 없습니다! ",Time=4})
            end
        end
    end,
})
FeedGroup:AddButton({
    Text="지금 먹이 주기 ",
    Func=function()
        task.spawn(function()
            local count = M.feedSelectedPets()
            Library:Notify({
                Title="수동 먹이 주기",
                Description=string.format("Fed %d pet(s) using %s. ",count,M.PenManagerState.FoodName),
                Time=3,
            })
        end)
    end,
})

local BuyFoodGroup = Tabs.PenManager:AddLeftGroupbox("자동 먹이 구매 ")
BuyFoodGroup:AddDropdown("BuyFoodSelect",{
    Text="구매할 먹이",Values=M.FoodNames,Default={},Multi=true,
    Callback=function(v) M.AutoBuyState.SelectedFoods=v end,
})
BuyFoodGroup:AddSlider("BuyFoodAmount",{
    Text="구매당 수량 ",Default=1,Min=1,Max=50,Rounding=0,Compact=false,
    Callback=function(v) M.AutoBuyState.Amount=v end,
})
BuyFoodGroup:AddSlider("BuyFoodDelay",{
    Text="간격 (초) ",Default=10,Min=1,Max=120,Rounding=1,Compact=false,
    Callback=function(v) M.AutoBuyState.BuyDelay=v end,
})
BuyFoodGroup:AddToggle("AutoBuyEnabled",{
    Text="자동 구매 활성화 ",Default=false,
    Callback=function(v)
        M.AutoBuyState.Enabled=v
        if v then
            Library:Notify({Title="자동 구매 ",Description="자동 구매가 시작되었습니다. ",Time=2})
            task.spawn(M.autoBuyLoop)
        end
    end,
})
BuyFoodGroup:AddButton({
    Text="지금 구매 ",
    Func=function()
        task.spawn(function()
            for food, enabled in pairs(M.AutoBuyState.SelectedFoods) do
                if enabled then
                    M.buyFood(food,M.AutoBuyState.Amount)
                    task.wait(0.2)
                end
            end
            Library:Notify({Title="먹이 구매",Description="구매가 완료되었습니다. ",Time=2})
        end)
    end,
})

local PlaceGroup = Tabs.PenManager:AddRightGroupbox("배치 및 수집 ")
PlaceGroup:AddToggle("AutoPlaceEnabled",{
    Text="자동 배치 활성화 ",Default=false,
    Callback=function(v)
        M.PenManagerState.AutoPlaceEnabled=v
        if v then
            local pen = M.findMyPen()
            if pen then
                Library:Notify({Title="자동 배치 ",Description="백팩을 감시하는 중... ",Time=2})
                task.spawn(M.autoPlaceLoop)
            else
                M.PenManagerState.AutoPlaceEnabled=false
                Toggles.AutoPlaceEnabled:SetValue(false)
                Library:Notify({Title="자동 배치 ",Description="펜을 찾을 수 없습니다! ",Time=4})
            end
        end
    end,
})
PlaceGroup:AddSlider("PlaceDelay",{
    Text="배치 지연 (초) ",Default=0.5,Min=0.1,Max=3,Rounding=1,Compact=false,
    Callback=function(v) M.PenManagerState.PlaceDelay=v end,
})
PlaceGroup:AddButton({
    Text="지금 모두 배치 ",
    Func=function()
        task.spawn(function()
            local count = M.placeAllInventoryPets()
            Library:Notify({Title="수동 배치 ",Description="배치됨 "..count.."펫.",Time=3})
        end)
    end,
})
PlaceGroup:AddDivider()
PlaceGroup:AddToggle("AutoCollectEnabled",{
    Text="자동 수집 활성화 ",Default=false,
    Callback=function(v)
        M.PenManagerState.AutoCollectEnabled=v
        if v then
            task.spawn(M.autoCollectLoop)
            Library:Notify({Title="자동 수집 ",Description="펜 수익을 수집하는 중... ",Time=2})
        end
    end,
})
PlaceGroup:AddSlider("CollectDelay",{
    Text="수집 간격 (초) ",Default=5,Min=1,Max=60,Rounding=1,Compact=false,
    Callback=function(v) M.PenManagerState.CollectDelay=v end,
})
PlaceGroup:AddButton({
    Text="지금 모두 수집 ",
    Func=function()
        task.spawn(function()
            local count = M.collectAllPenCash()
            Library:Notify({Title="수집 ",Description="다음에서 수집함: "..count.."펫.",Time=3})
        end)
    end,
})
PlaceGroup:AddDivider()
PlaceGroup:AddButton({
    Text="펜 정보 ",
    Func=function()
        local pen = M.findMyPen()
        if not pen then Library:Notify({Title="펜 ",Description="펜을 찾을 수 없습니다! ",Time=2}) return end
        Library:Notify({
            Title="펜 #"..pen.Name.." ",
            Description=string.format("%d placed. %d in backpack. ",#M.getMyPenPets(),#M.getInventoryPets()),
            Time=4,
        })
    end,
})

local TpPenGroup = Tabs.Teleports:AddLeftGroupbox("펜 ")
TpPenGroup:AddButton({
    Text="→ 내 펜 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local pen = M.findMyPen()
        if pen then
            local pos = M.getPenBasePosition()
            if pos then root.CFrame=CFrame.new(pos) end
            Library:Notify({Title="텔레포트됨 ",Description="펜 #"..pen.Name.." ",Time=2})
        else
            Library:Notify({Title="오류 ",Description="펜을 찾을 수 없습니다! ",Time=2})
        end
    end,
})
TpPenGroup:AddButton({
    Text="→ 첫 번째 펜 펫 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local pets = M.getMyPenPets()
        if #pets==0 then Library:Notify({Title="펜 ",Description="펜에 펫이 없습니다. ",Time=2}) return end
        local part = pets[1]:FindFirstChildWhichIsA("BasePart")
        if part then
            root.CFrame=CFrame.new(part.Position+Vector3.new(0,3,0))
            local info = {name=pets[1]:GetAttribute("Name") or pets[1].Name}
            Library:Notify({Title="텔레포트됨 ",Description=info.name.." ",Time=2})
        end
    end,
})

local IslandTpGroup = Tabs.Teleports:AddLeftGroupbox("섬 ")
IslandTpGroup:AddButton({
    Text="→ 드래곤 섬 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Vector3.new(-43.46, 822.68, -2767.61))
            Library:Notify({Title="텔레포트됨 ",Description="드래곤 섬 ",Time=2})
        end
    end,
})
IslandTpGroup:AddButton({
    Text="→ 동굴 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Vector3.new(-3172.37, 10.00, -2850.28))
            Library:Notify({Title="텔레포트됨 ",Description="동굴 ",Time=2})
        end
    end,
})
IslandTpGroup:AddButton({
    Text="→ 잊혀진 심연 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Vector3.new(18.82, 11.02, -4814.16))
            Library:Notify({Title="텔레포트됨 ",Description="잊혀진 심연 ",Time=2})
        end
    end,
})
IslandTpGroup:AddButton({
    Text="→ 벌 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Vector3.new(101.23, 11.75, -1205.76))
            Library:Notify({Title="텔레포트됨 ",Description="벌 ",Time=2})
        end
    end,
})
IslandTpGroup:AddButton({
    Text="→ 사파리 섬 ",
    Func=function()
        local root = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Vector3.new(-2164.65, 10.00, -2727.22))
            Library:Notify({Title="텔레포트됨 ",Description="사파리 섬 ",Time=2})
        end
    end,
})

local RarityTpGroup = Tabs.Teleports:AddRightGroupbox("희귀도별 텔레포트 ")
local rarityOrder = {"Divine","Boss","Mythical","Legendary","Epic","Rare","Uncommon","Common"}
for _, rarity in ipairs(rarityOrder) do
    RarityTpGroup:AddButton({
        Text="→ 가장 가까운 "..rarity.." ",
        Func=function() M.teleportToRarity(rarity) end,
    })
end

local CodesGroup = Tabs.Codes:AddLeftGroupbox("클레임 코드")
CodesGroup:AddButton({
    Text="모든 코드 복사 ",
    Func=function()
        local codes = "BEEISLAND, SPACEEVENT, EASTERUPDATE, ABDUCTED, CANDYBLOSSOM, SAFARIUPDATE, STEAMPUNK"
        setclipboard(codes)
        Library:Notify({Title="코드 복사됨 ",Description="모든 코드가 클립보드에 복사되었습니다! ",Time=3})
    end,
})
CodesGroup:AddDivider()
CodesGroup:AddButton({
    Text="코드 사용 ",
    Func=function()
        local codes = {"BEEISLAND","SPACEEVENT","EASTERUPDATE","ABDUCTED","CANDYBLOSSOM","SAFARIUPDATE","STEAMPUNK"}
        task.spawn(function()
            for _, code in ipairs(codes) do
                M.redeemCode(code)
                task.wait(0.5)
            end
            Library:Notify({Title="코드 ",Description="코드 사용이 완료되었습니다!",Time=3})
        end)
    end,
})

local UtilityGroup = Tabs.Misc:AddLeftGroupbox("유틸리티 ")
UtilityGroup:AddButton({
    Text="플레이어 데이터 동기화 ",
    Func=function()
        local remote = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):FindFirstChild("retrieveData")
        if remote then
            remote:InvokeServer()
            Library:Notify({Title="데이터 ",Description="플레이어 데이터가 동기화되었습니다! ",Time=2})
        end
    end,
})
UtilityGroup:AddButton({
    Text="펜 인덱스 가져오기 ",
    Func=function()
        local rf = game:GetService("ReplicatedStorage"):WaitForChild("Packages",5)
            :WaitForChild("_Index",5):WaitForChild("sleitnick_knit@1.7.0",5)
            :WaitForChild("knit",5):WaitForChild("Services",5)
            :WaitForChild("PenService",5):WaitForChild("RF",5):FindFirstChild("getPenIndex")
        if rf then
            local ok, result = pcall(function() return rf:InvokeServer() end)
            if ok then Library:Notify({Title="펜 인덱스 ",Description=tostring(result).." ",Time=3}) end
        end
    end,
})
UtilityGroup:AddDivider()
UtilityGroup:AddButton({
    Text="근처 펫 수 세기 ",
    Func=function()
        local pets, _ = M.getNearestIslandPets()
        Library:Notify({Title="근처 펫 ",Description=#pets.."{0}마리의 펫을 찾았습니다.",Time=2})
    end,
})
UtilityGroup:AddButton({
    Text="백팩 펫 수 세기 ",
    Func=function()
        Library:Notify({Title="백팩 ",Description=#M.getInventoryPets().."미배치된 펫",Time=3})
    end,
})

local MovementGroup = Tabs.Misc:AddRightGroupbox("이동 ")
MovementGroup:AddSlider("WalkSpeedSlider",{
    Text="걷기 속도 ",Default=16,Min=16,Max=250,Rounding=0,Compact=false,
    Callback=function(v) M.setWalkSpeed(v) end,
})
MovementGroup:AddDivider()
MovementGroup:AddToggle("FlyEnabled",{
    Text="카메라 비행 활성화 ",Default=false,
    Callback=function(v)
        M.MovementState.FlyEnabled=v
        if v then M.startCameraFly() else M.stopCameraFly() end
    end,
})
MovementGroup:AddSlider("FlySpeedSlider",{
    Text="비행 속도 ",Default=50,Min=10,Max=500,Rounding=0,Compact=false,
    Callback=function(v) M.MovementState.FlySpeed=v end,
})

local ESPGroup = Tabs.Misc:AddRightGroupbox("ESP ")
ESPGroup:AddToggle("ESPEnabled",{
    Text="펫 ESP 활성화 ",Default=false,
    Callback=function(v)
        M.ESPState.Enabled=v
        if v then
            Library:Notify({Title="ESP ",Description="펫 ESP가 활성화되었습니다. ",Time=2})
            task.spawn(function()
                while M.ESPState.Enabled do
                    M.updateESP()
                    task.wait(2)
                end
                M.clearESP()
            end)
        else
            M.clearESP()
        end
    end,
})
ESPGroup:AddLabel("색상은 희귀도 등급에 맞춰집니다. ")

task.spawn(function()
    while true do
        local pets, islandName = M.getNearestIslandPets()
        M.AutoCatchState.CurrentIsland = islandName
        local count = 0
        for _ in pairs(M.AutoCatchState.CaughtPets) do count+=1 end
        local cur    = M.getCurrentWeather()
        local isAct  = cur~="Default" and cur~=""
        local bosses = M.getAllBossesOnMap()
        islandLabel:SetText( "Island: "         ..islandName.." ")
        caughtLabel:SetText( "Session Caught: " ..count.." ")
        nearbyLabel:SetText( "Nearby Pets: "    ..#pets.." ")
        bossMapLabel:SetText("Bosses: "         ..#bosses.." ")
        weatherLabel:SetText("Weather: "        ..cur.." ")
        eventLabel:SetText(  "Active Event: "   ..(isAct and cur or "None").." ")
        bossLabel:SetText(   "Boss: "           ..M.AutoCatchState.BossStatus.." ")
        task.wait(0.5)
    end
end)

local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("메뉴 ")
MenuGroup:AddLabel("메뉴 키바인드 ")
    :AddKeyPicker("MenuKeybind",{Default="RightShift",NoUI=true,Text="메뉴 키바인드 "})
MenuGroup:AddButton("언로드 ",function()
    M.AutoCatchState.Enabled             = false
    M.ManualCatchState.Enabled           = false
    M.PenManagerState.AutoFeedEnabled    = false
    M.PenManagerState.AutoPlaceEnabled   = false
    M.PenManagerState.AutoCollectEnabled = false
    M.AutoBuyState.Enabled               = false
    M.ESPState.Enabled                   = false
    M.MovementState.FlyEnabled           = false
    M.stopCameraFly()
    M.stopWatching()
    M.clearESP()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({"MenuKeybind"})
ThemeManager:SetFolder("COMBOWICK")
SaveManager:SetFolder("COMBOWICK/saves")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])
SaveManager:LoadAutoloadConfig()

Library:Notify({Title="COMBOWICK ",Description="로드됨! ",Time=3})