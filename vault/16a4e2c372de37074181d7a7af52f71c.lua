-- Chinese (Simplified) Translation
-- Script ID: 16a4e2c372de37074181d7a7af52f71c
-- Updated: 2026-05-16T07:16:23.056Z
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library      = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()

local MODULE_URL = "https://raw.githubusercontent.com/checkurasshole/INK/main/KaijuAlphaModule"
local M      = loadstring(game:HttpGet(MODULE_URL))()
local Config = M.Config

local Window = Library:CreateWindow({
    Title            = "COMBOWICK",
    Icon             = 6031071057,
    NotifySide       = "Right",
    ShowCustomCursor = false,
    Size             = UDim2.new(0, 560, 0, 500),
})

local Tabs = { Main = Window:AddTab("Main", "star") }
local Left  = Tabs.Main:AddLeftGroupbox("Fly")
local Right = Tabs.Main:AddRightGroupbox("生成与选项")

Left:AddToggle("FlyEnabled", {
    Text    = "启用飞行  [F]",
    Default = false,
    Callback = function(v)
        if v then M.startFly() else M.stopFly() end
    end,
})

Left:AddDropdown("FlyMode", {
    Text    = "飞行模式",
    Default = "PC",
    Values  = { "PC", "Mobile" },
    Callback = function(v)
        Config.FlyMode = v
        if Config.FlyEnabled then M.startFly() end
    end,
})

Left:AddSlider("FlySpeed", {
    Text    = "飞行速度",
    Default = 22, Min = 5, Max = 50, Rounding = 0,
    Callback = function(v)
        Config.Speed = v
        local hum = M.getHum()
        if hum and Config.FlyEnabled then hum.WalkSpeed = v end
    end,
})

Left:AddToggle("AccelEnabled", {
    Text    = "加速度  （仅限 PC）",
    Default = true,
    Callback = function(v) Config.Acceleration = v end,
})

Left:AddSlider("MaxAccelMult", {
    Text    = "最大加速度倍数",
    Default = 1.5, Min = 1.0, Max = 2.0, Rounding = 1,
    Callback = function(v) Config.MaxAccel = v end,
})

Left:AddDivider()

Left:AddToggle("WSEnabled", {
    Text    = "启用移动速度",
    Default = false,
    Callback = function(v)
        Config.WSEnabled = v
        if v then M.startWalkSpeed() else M.stopWalkSpeed() end
    end,
})

Left:AddSlider("WalkSpeedSlider", {
    Text    = "移动速度",
    Default = 50, Min = 16, Max = 200, Rounding = 0,
    Callback = function(v) Config.WalkSpeed = v end,
})

local kaijuLabel = Right:AddLabel("Kaiju：检测中...")

local function refreshKaijuLabel()
    local name = M.getSelectedKaiju()
    kaijuLabel:SetText("Kaiju: " .. (name ~= "" and name or "None Detected"))
end

refreshKaijuLabel()

local localPlayer = game:GetService("Players").LocalPlayer
local ls = localPlayer:FindFirstChild("leaderstats")
local kv = ls and ls:FindFirstChild("Kaiju")
if kv then kv.Changed:Connect(refreshKaijuLabel) end

Right:AddButton("生成当前 Kaiju", function()
    M.fireSpawn()
end)

Right:AddToggle("AutoSpawnToggle", {
    Text    = "无限技能 / 无限生命",
    Default = true,
    Callback = function(v) Config.AutoSpawn = v end,
})

Right:AddDivider()

local function fullUnload()
    M.coreUnload()
    if Toggles then
        if Toggles.FlyEnabled      then pcall(function() Toggles.FlyEnabled:SetValue(false)     end) end
        if Toggles.AutoSpawnToggle then pcall(function() Toggles.AutoSpawnToggle:SetValue(false) end) end
        if Toggles.AccelEnabled    then pcall(function() Toggles.AccelEnabled:SetValue(false)    end) end
        if Toggles.WSEnabled       then pcall(function() Toggles.WSEnabled:SetValue(false)       end) end
    end
    Library:Unload()
end

Right:AddButton("Unload", function() fullUnload() end)

game:GetService("UserInputService").InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.F then
        if Config.FlyEnabled then M.stopFly() else M.startFly() end
        if Toggles and Toggles.FlyEnabled then
            Toggles.FlyEnabled:SetValue(Config.FlyEnabled)
        end
    end
end)

ThemeManager:SetLibrary(Library)
ThemeManager:SetFolder("KaijuScript")
ThemeManager:ApplyToTab(Tabs.Main)

local function showExpiredPopup(onDone)
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name           = "CW_Expired"
    ScreenGui.ResetOnSpawn   = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    ScreenGui.Parent         = game:GetService("CoreGui")

    local Frame = Instance.new("Frame")
    Frame.Size             = UDim2.fromOffset(280, 140)
    Frame.Position         = UDim2.new(0.5, -140, 0, -150)
    Frame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
    Frame.BorderSizePixel  = 0
    Frame.Parent           = ScreenGui
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 14)

    local Stroke = Instance.new("UIStroke", Frame)
    Stroke.Color     = Color3.fromRGB(80, 80, 100)
    Stroke.Thickness = 1

    local Title = Instance.new("TextLabel", Frame)
    Title.Size               = UDim2.new(1, -20, 0, 28)
    Title.Position           = UDim2.fromOffset(10, 10)
    Title.BackgroundTransparency = 1
    Title.Text               = "免费会话已结束"
    Title.TextColor3         = Color3.fromRGB(255, 255, 255)
    Title.TextSize           = 14
    Title.Font               = Enum.Font.GothamBold
    Title.TextXAlignment     = Enum.TextXAlignment.Left

    local Sub = Instance.new("TextLabel", Frame)
    Sub.Size                 = UDim2.new(1, -20, 0, 34)
    Sub.Position             = UDim2.fromOffset(10, 36)
    Sub.BackgroundTransparency = 1
    Sub.Text                 = "前往 combowick.com 获取密钥\n即可继续使用且无冷却时间。"
    Sub.TextColor3           = Color3.fromRGB(180, 180, 200)
    Sub.TextSize             = 12
    Sub.Font                 = Enum.Font.Gotham
    Sub.TextXAlignment       = Enum.TextXAlignment.Left
    Sub.TextWrapped          = true

    local Button = Instance.new("TextButton", Frame)
    Button.Size             = UDim2.new(1, -20, 0, 24)
    Button.Position         = UDim2.fromOffset(10, 74)
    Button.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
    Button.BorderSizePixel  = 0
    Button.Text             = "复制密钥链接"
    Button.TextColor3       = Color3.fromRGB(255, 255, 255)
    Button.TextSize         = 12
    Button.Font             = Enum.Font.GothamBold
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 6)
    Button.MouseButton1Click:Connect(function()
        pcall(function() setclipboard("https://combowick.com/verify/provider-select") end)
        Button.Text = "已复制！"
        task.wait(1.5)
        Button.Text = "复制密钥链接"
    end)

    local Bar = Instance.new("Frame", Frame)
    Bar.Size             = UDim2.new(1, -20, 0, 3)
    Bar.Position         = UDim2.fromOffset(10, 122)
    Bar.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
    Bar.BorderSizePixel  = 0
    Instance.new("UICorner", Bar).CornerRadius = UDim.new(1, 0)

    Frame:TweenPosition(UDim2.new(0.5, -140, 0, 20), Enum.EasingDirection.Out, Enum.EasingStyle.Quint, 0.4, true)

    local ts = game:GetService("TweenService")
    ts:Create(Bar, TweenInfo.new(20, Enum.EasingStyle.Linear), { Size = UDim2.new(0, 0, 0, 3) }):Play()

    task.delay(20, function()
        Frame:TweenPosition(UDim2.new(0.5, -140, 0, -150), Enum.EasingDirection.In, Enum.EasingStyle.Quint, 0.4, true)
        task.wait(0.5)
        ScreenGui:Destroy()
        if onDone then onDone() end
    end)
end

if _G.COMBOWICK_KEY then
    _G.COMBOWICK_FREE   = false
    _G.COMBOWICK_ACTIVE = nil
    _G.COMBOWICK_KEY    = false
end

if _G.COMBOWICK_FREE then
    _G.COMBOWICK_ACTIVE = true
    task.spawn(function()
        repeat task.wait(1) until _G.COMBOWICK_ACTIVE == false
        showExpiredPopup(function() fullUnload() end)
    end)
end