-- Protected Script (French)
-- Script ID: 90b62ebc8d5a8e7befbd7a2ed4bae4a0
-- Created: 2025-10-20T23:12:52.253Z
-- Source: External API
-- Auto-generated by Lua Script Vault


local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

-- Load Core Module from GitHub (replace with your raw GitHub link)
local Module = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/INK/refs/heads/main/Jurrasicscript"))()

-- Initialize connections
Module.initConnections()

-- Create Window
local Window = Library:CreateWindow({
    Title = "_COMBO WICK",
    Icon = 7734068321,
    NotifySide = "Right",
    ShowCustomCursor = true,
    Size = UDim2.new(0, 500, 0, 400),
})

-- Create Tabs
local Tabs = {
    Main = Window:AddTab("Principales", "zap"),
    Amber = Window:AddTab("Ambre", "gem"),
    Utility = Window:AddTab("Paramètres", "wrench"),
}

-- Main Tab - Walkspeed
local MainWalkspeed = Tabs.Main:AddLeftGroupbox("Vitesse de marche", "activity")
MainWalkspeed:AddToggle("Vitesse de marcheToggle", {
    Text = "Activer la vitesse de marche",
    Default = false,
    Callback = function(Value)
        Module.toggleWalkspeed(Value)
    end,
})
MainWalkspeed:AddSlider("Vitesse de marcheCouloir", {
    Text = "Valeur de vitesse de marche",
    Default = 16,
    Min = 1,
    Max = 150,
    Rounding = 1,
    Callback = function(Value)
        Module.updateWalkspeed(Value)
    end,
})

-- Main Tab - Animal Farm
local MainNPC = Tabs.Main:AddLeftGroupbox("Ferme d'animaux", "crosshair")
MainNPC:AddToggle("ESPToggle", {
    Text = "Dénomination ESP",
    Default = false,
    Callback = function(Value)
        Module.toggleESP(Value)
    end,
})
MainNPC:AddToggle("Mettre en surbrillanceToggle", {
    Text = "Marqueur ESP",
    Default = false,
    Callback = function(Value)
        Module.toggleHighlight(Value)
    end,
})
MainNPC:AddToggle("NPCTeleportToggle", {
    Text = "Téléporter aux chèvres",
    Default = false,
    Callback = function(Value)
        Module.toggleNPCTeleport(Value)
    end,
})
MainNPC:AddSlider("NPCDistance", {
    Text = "Distance derrière",
    Default = 5,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        Module.State.npcBehindDistance = Value
        if Module.State.npcTeleportEnabled and Module.State.currentNPCTarget then
            Module.anchorToNPC(Module.State.currentNPCTarget)
        end
    end,
})
MainNPC:AddButton({
    Text = "NPC suivant",
    Func = function()
        if Module.State.npcTeleportEnabled then
            local newTarget = Module.switchToNextNPC()
            if newTarget then
                Module.State.currentNPCTarget = newTarget
                Module.anchorToNPC(Module.State.currentNPCTarget)
            end
        end
    end,
})

-- Main Tab - Boss Farm
local MainBoss = Tabs.Main:AddLeftGroupbox("Ferme Boss", "skull")
MainBoss:AddToggle("BossTeleportToggle", {
    Text = "Téléporter à D-Rex Boss",
    Default = false,
    Callback = function(Value)
        Module.toggleBossTeleport(Value)
    end,
})
MainBoss:AddSlider("BossDistance", {
    Text = "Distance derrière",
    Default = 5,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        Module.State.bossBehindDistance = Value
        if Module.State.bossTeleportEnabled and Module.State.currentBossTarget then
            Module.anchorToBoss(Module.State.currentBossTarget)
        end
    end,
})
MainBoss:AddButton({
    Text = "Suivant Boss",
    Func = function()
        if Module.State.bossTeleportEnabled then
            local newTarget = Module.switchToNextBoss()
            if newTarget then
                Module.State.currentBossTarget = newTarget
                Module.anchorToBoss(Module.State.currentBossTarget)
            end
        end
    end,
})

-- Main Tab - Combat
local MainCombat = Tabs.Main:AddRightGroupbox("Lutte", "sword")
MainCombat:AddToggle("AutoAttackToggle", {
    Text = "Attaque automatique",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoAttack(Value)
    end,
})
MainCombat:AddToggle("AutoHeavyToggle", {
    Text = "Mode de combat",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoHeavy(Value)
    end,
})
MainCombat:AddButton({
    Text = "Despawn instantané",
    Func = function()
        Module.executeInstantDespawn()
    end,
})

-- Main Tab - Player Farm
local MainPlayer = Tabs.Main:AddRightGroupbox("Ferme des joueurs", "users")
MainPlayer:AddToggle("TéléporterToggle", {
    Text = "Téléporter aux joueurs",
    Default = false,
    Callback = function(Value)
        Module.State.teleporting = Value
        if Module.State.teleporting then
            Module.State.playerIndex = 0
            Module.State.currentTarget = Module.getNextTarget()
            if Module.State.currentTarget then
                Module.anchorToPlayer(Module.State.currentTarget)
            end
        else
            Module.State.currentTarget = nil
        end
    end,
})
MainPlayer:AddToggle("Déplacer la cibleToggle", {
    Text = "Déplacement de la cible",
    Default = true,
    Callback = function(Value)
        Module.State.targetMovingPlayers = Value
        if not Module.State.targetMovingPlayers and not Module.State.targetStillPlayers then
            Module.State.targetStillPlayers = true
        end
    end,
})
MainPlayer:AddToggle("TargetStillToggle", {
    Text = "Cible en attente",
    Default = true,
    Callback = function(Value)
        Module.State.targetStillPlayers = Value
        if not Module.State.targetMovingPlayers and not Module.State.targetStillPlayers then
            Module.State.targetMovingPlayers = true
        end
    end,
})
MainPlayer:AddSlider("Derrière la distance", {
    Text = "Distance derrière",
    Default = 5,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        Module.State.behindDistance = Value
        if Module.State.teleporting and Module.State.currentTarget then
            Module.anchorToPlayer(Module.State.currentTarget)
        end
    end,
})
MainPlayer:AddButton({
    Text = "Cible suivante",
    Func = function()
        if Module.State.teleporting then
            local validPlayers = Module.getValidPlayers()
            if #validPlayers > 0 then
                Module.State.currentTarget = Module.getNextTarget()
                if Module.State.currentTarget then
                    Module.anchorToPlayer(Module.State.currentTarget)
                end
            end
        end
    end,
})

-- Amber Tab - V1
local AmberV1 = Tabs.Amber:AddLeftGroupbox("Ambre V1", "gem")
AmberV1:AddToggle("AmberV1Toggle", {
    Text = "Collection Amber",
    Default = false,
    Callback = function(Value)
        Module.toggleAmberCollectionSwim(Value)
    end,
})

-- Amber Tab - V2
local AmberV2 = Tabs.Amber:AddRightGroupbox("Amber V2", "zap")
AmberV2:AddToggle("AmberV2Toggle", {
    Text = "Recueillir des pièces de dinosaure v2",
    Default = false,
    Callback = function(Value)
        Module.toggleAmberCollection(Value)
    end,
})
AmberV2:AddToggle("AutoSpawnToggle", {
    Text = "Revive automatique",
    Default = false,
    Callback = function(Value)
        Module.State.AUTO_SPAWN_ENABLED = Value
        if Value then
            Module.State.lastVisibleGuis = {}
        end
    end,
})
AmberV2:AddDropdown("DinoSelection", {
    Values = Module.allDinosaurs,
    Default = "Gallimimus",
    Text = "Sélectionner Dinosaur",
    Callback = function(Value)
        Module.State.selectedDino = Value
        Module.State.SPAWN_ARGS = { [1] = Value }
    end,
})
AmberV2:AddButton({
    Text = "Manual Spawn Sélectionné",
    Func = function()
        Module.executeSpawn()
    end,
})
AmberV2:AddSlider("AmberTimeout", {
    Text = "Délai (s)",
    Default = 15,
    Min = 5,
    Max = 60,
    Rounding = 1,
    Callback = function(Value)
        Module.State.amberAttemptTimeout = Value
    end,
})

-- Amber Tab - V3 (Premium)
local AmberV3 = Tabs.Amber:AddLeftGroupbox("Meilleure version plus rapide de l'ambre", "zap")
local linkText = "Buy Premium Best DinoCoin Script here:\nhttps://discord.gg/9FWBQnVXCy"
AmberV3:AddLabel(linkText)
AmberV3:AddButton("Copy Link", function()
    if setclipboard then
        setclipboard("https://discord.gg/9FWBQnVXCy")
    elseif toclipboard then
        toclipboard("https://discord.gg/9FWBQnVXCy")
    else
        warn("Clipboard not supported in this executor")
    end
end)

-- Utility Tab - Anti-Void
local UtilityAntiVoid = Tabs.Utility:AddLeftGroupbox("Anti-Voïde", "shield")
UtilityAntiVoid:AddToggle("AntiVoidToggle", {
    Text = "Contrer le vide",
    Default = false,
    Callback = function(Value)
        if Value then
            Module.enableAntiVoid()
        else
            Module.disableAntiVoid()
        end
    end,
})

-- Utility Tab - Other
local UtilityOther = Tabs.Utility:AddLeftGroupbox("Autres", "zap")
UtilityOther:AddToggle("NoclipToggle", {
    Text = "Noclip",
    Default = false,
    Callback = function(Value)
        if Value then
            Module.enableNoclip()
        else
            Module.disableNoclip()
        end
    end,
})
UtilityOther:AddToggle("Anti-AFKToggle", {
    Text = "Anti-AFK",
    Default = false,
    Callback = function(Value)
        if Value then
            Module.enableAntiAfk()
        else
            Module.disableAntiAfk()
        end
    end,
})
UtilityOther:AddToggle("StaffWatchToggle", {
    Text = "Recherche d'un marteau d'interdiction (montre du personnel)",
    Default = false,
    Callback = function(Value)
        Module.toggleStaffWatch(Value)
    end,
})

-- Utility Tab - Combat
local Combat = Tabs.Utility:AddRightGroupbox("Lutte", "sword")
Combat:AddToggle("AutoAttackToggle", {
    Text = "Attaque automatique",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoAttack(Value)
    end,
})
Combat:AddToggle("AutoHeavyToggle", {
    Text = "Mode de combat",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoHeavy(Value)
    end,
})
Combat:AddButton({
    Text = "Suicide rapide (despion)",
    Func = function()
        Module.executeInstantDespawn()
    end,
})

-- Utility Tab - UI Settings
local UISettings = Tabs.Utility:AddRightGroupbox("Paramètres de l' UI", "settings")
UISettings:AddToggle("KeybindMenuOuvrir", {
    Default = Library.KeybindFrame.Visible,
    Text = "Ouvrir le menu Keybind",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})
UISettings:AddToggle("AfficherCurseurCustom", {
    Text = "Curseur personnalisé",
    Default = true,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})
UISettings:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = "Right",
    Text = "Côté notification",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})
UISettings:AddButton("Unload", function()
    Library:Unload()
end)

-- Setup Theme and Save Manager
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("ComboChronicle")
SaveManager:SetFolder("ComboChronicle")
SaveManager:BuildConfigSection(Tabs.Utility)
ThemeManager:ApplyToTab(Tabs.Utility)

-- Cleanup on close
game:BindToClose(Module.cleanup)
SaveManager:LoadAutoloadConfig()