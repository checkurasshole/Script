-- Portuguese Translation
-- Script ID: 985f84442258962e52709c93373283a6
-- Created: 2026-01-23T21:14:34.531Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Module = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/Script/main/Dinov1"))()

Library.ShowCustomCursor = false

local Window = Library:CreateWindow({
	Title = "COMBO_WICK",
	Footer = "",
	NotifySide = "Right",
	ShowCustomCursor = false,
	Size = UDim2.new(0, 400, 0, 350),
})

local Tabs = {
	Main = Window:AddTab("Principal ", "user"),
	UI = Window:AddTab("Interface do Usuário", "settings"),
}

local FarmBox = Tabs.Main:AddLeftGroupbox("Fazenda ")
local HuntBox = Tabs.Main:AddLeftGroupbox("Caça ")
local MoveBox = Tabs.Main:AddLeftGroupbox("Movimento ")
local BabyBox = Tabs.Main:AddRightGroupbox("Bebê Dinossauro ")
local StatsBox = Tabs.Main:AddRightGroupbox("Estatísticas ")

local growthLabel = StatsBox:AddLabel("Growth : 0")
local rankLabel = StatsBox:AddLabel("Rank : #0")

task.spawn(function()
	while task.wait(0.5) do
		Module.updateStats(growthLabel, rankLabel)
		if Library.Unloaded then break end
	end
end)

FarmBox:AddDropdown("TargetType", {
	Values = {"All", "Eggs", "Meats", "Nests"},
	Default = 1,
	Text = "Target",
	Callback = function(Value)
		Module.targetType = Value
	end,
})

FarmBox:AddToggle("FarmToggle", {
	Text = "Coleta Automática",
	Default = false,
	Callback = function(Value)
		Module.farmActive = Value
		if not Value then
			if Module.lockCamera then
				Module.camera.CameraType = Enum.CameraType.Custom
				Module.cameraCFrame = nil
			end
		end
	end,
})

FarmBox:AddToggle("LockCamera", {
	Text = "Travar Câmera",
	Default = false,
	Callback = function(Value)
		Module.lockCamera = Value
		if not Value then
			Module.camera.CameraType = Enum.CameraType.Custom
			Module.cameraCFrame = nil
		end
	end,
})

HuntBox:AddToggle("HuntToggle", {
	Text = "Caçar Jogadores Mais Fracos",
	Default = false,
	Callback = function(Value)
		Module.huntActive = Value
	end,
})

BabyBox:AddToggle("BabyToggle", {
	Text = "Coletar Bebê Automaticamente",
	Default = false,
	Callback = function(Value)
		Module.babyDinoActive = Value
	end,
})

MoveBox:AddToggle("Flight", {
	Text = "Voo da Câmera",
	Default = false,
	Callback = function(Value)
		Module.flightActive = Value
		if Value then
			Module.enableFlight()
		else
			Module.disableFlight()
		end
	end,
})

MoveBox:AddSlider("FlightSpeed", {
	Text = "Velocidade de Voo",
	Default = 50,
	Min = 16,
	Max = 200,
	Rounding = 0,
	Callback = function(Value)
		Module.flightSpeed = Value
	end,
})

Module.startLoop()

Library:OnUnload(function()
	Module.cleanup()
end)

local MenuGroup = Tabs.UI:AddLeftGroupbox("Menu")
MenuGroup:AddLabel("Menu"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true })
MenuGroup:AddButton("Unload", function() Library:Unload() end)

Library.ToggleKeybind = Library.Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("DinoScript")
SaveManager:SetFolder("DinoScript/config")
SaveManager:BuildConfigSection(Tabs.UI)
ThemeManager:ApplyToTab(Tabs.UI)