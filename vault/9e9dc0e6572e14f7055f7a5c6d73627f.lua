-- Protected Script (German)
-- Script ID: 9e9dc0e6572e14f7055f7a5c6d73627f
-- Created: 2025-10-22T20:35:38.121Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local CoreModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/jurrasiccccc"))()

local Window = Fluent:CreateWindow({
    Title = "KOMB0_WICK",
    SubTitle = "",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    ReadThis = Window:AddTab({ Title = "Lesen Sie das", Icon = "info" }),
    Main = Window:AddTab({ Title = "Hauptseite", Icon = "home" }),
    Shop = Window:AddTab({ Title = "Shoppen", Icon = "shopping-cart" }),
    Gamepass = Window:AddTab({ Title = "Spielübergänge", Icon = "gift" })
}

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "This is for trolling your friends, lol\n:Enabling shop features will not allow you to play!",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "R",
            Content = "T",
            Duration = 2
        })
        task.wait(0.5)
       -- game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

-- Initialize the core module
CoreModule:Init()

-- Main Tab
local CoinInput = Tabs.Main:AddInput("CoinInput", {
    Title = "Münzbetrag",
    Default = "10000",
    Placeholder = "Enter amount",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        local amount = tonumber(Value)
        if amount and amount > 0 then
            CoreModule:SetCoinAmount(amount)
        end
    end
})

local CoinToggle = Tabs.Main:AddToggle("CoinToggle", {
    Title = "Geben Sie Münzen Betrag",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleCoins(Value)
        if Value then
            Fluent:Notify({
                Title = "Erfolg",
                Content = "Münzen geändert auf" .. tostring(CoreModule:GetCoinAmount()),
                Duration = 2
            })
        end
    end
})

-- Shop Tab
local DinosaurPurchaseToggle = Tabs.Shop:AddToggle("DinosaurPurchaseToggle", {
    Title = "Bypass Dinosaur Kauf",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleDinosaurBypass(Value)
        if Value then
            Fluent:Notify({
                Title = "Erfolg",
                Content = "Dinosaur-Kauf-Bypass aktiviert",
                Duration = 2
            })
        end
    end
})

local UnlockAllButton = Tabs.Shop:AddButton({
    Title = "Alle Dinosaurier entsperren",
    Callback = function()
        CoreModule:UnlockAllDinosaurs()
        Fluent:Notify({
            Title = "Erfolg",
            Content = "Alle Dinosaurier sind optisch entriegelt",
            Duration = 3
        })
    end
})

-- Gamepass Tab
local GamepassToggle = Tabs.Gamepass:AddToggle("GamepassToggle", {
    Title = "Bypass Alle Spiel-Pässe",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleAllGamepasses(Value)
        if Value then
            Fluent:Notify({
                Title = "Erfolg",
                Content = "Alle Spielpasse aktiviert",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("SpielbarAI", {
    Title = "Abspielbare KI entsperren",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716086049, Value)
        if Value then
            Fluent:Notify({
                Title = "Erfolg",
                Content = "Spielbare KI entsperrt",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("StealthPass", {
    Title = "Entriegelung des Stealth Pass",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716240706, Value)
        if Value then
            Fluent:Notify({
                Title = "Erfolg",
                Content = "Entriegelter Stealthpass",
                Duration = 2
            })
        end
    end
})

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "Server neu verbinden",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "Rejoining...",
            Content = "Teleportieren Sie zurück zum Spiel",
            Duration = 2
        })
        task.wait(0.5)
        game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

Window:SelectTab(1)