-- Filipino Translation
-- Script ID: 4f22d16afba1f467697b51ad58468648
-- Created: 2026-03-27T03:22:13.835Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

-- ── Fetch core module from GitHub ──────────────────────────────────────────
-- Replace the URL below with your actual raw GitHub link to Module.lua
local M = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/INK/main/module3.lua"))()
-- ───────────────────────────────────────────────────────────────────────────

local Window = Library:CreateWindow({
    Title = "COMBO_WICK",
    Icon = 7734068321,
    NotifySide = "Right",
    ShowCustomCursor = false,
    Size = UDim2.new(0, 500, 0, 400),
})

local Tabs = {
    Main    = Window:AddTab("Pangunahing ",     "zap"),
    Amber   = Window:AddTab("Amber ",    "gem"),
    Utility = Window:AddTab("Mga Setting ", "wrench"),
}

-- ── Main › Walkspeed ───────────────────────────────────────────────────────
local MainWalkspeed = Tabs.Main:AddLeftGroupbox("Bilis ng Paglalakad ", "activity")
MainWalkspeed:AddToggle("WalkspeedToggle", {
    Text    = "I-enable ang Bilis ng Paglalakad",
    Default = false,
    Callback = function(Value)
        M.toggleWalkspeed(Value)
    end,
})
MainWalkspeed:AddSlider("WalkspeedSlider", {
    Text     = "Halaga ng Bilis ng Paglalakad",
    Default  = 16,
    Min      = 1,
    Max      = 150,
    Rounding = 1,
    Callback = function(Value)
        M.updateWalkspeed(Value)
    end,
})

-- ── Main › Animal Farm ─────────────────────────────────────────────────────
local MainNPC = Tabs.Main:AddLeftGroupbox("Bukirin ng mga Hayop", "crosshair")
MainNPC:AddToggle("ESPToggle", {
    Text    = "Pangalan ng ESP",
    Default = false,
    Callback = function(Value)
        M.toggleESP(Value)
    end,
})
MainNPC:AddToggle("HighlightToggle", {
    Text    = "Marka ng ESP",
    Default = false,
    Callback = function(Value)
        M.toggleHighlight(Value)
    end,
})
MainNPC:AddToggle("NPCTeleportToggle", {
    Text    = "I-teleport sa mga Kambing",
    Default = false,
    Callback = function(Value)
        M.toggleNPCTeleport(Value)
    end,
})
MainNPC:AddSlider("NPCDistance", {
    Text     = "Layo sa Likuran",
    Default  = 5,
    Min      = 1,
    Max      = 100,
    Rounding = 1,
    Callback = function(Value)
        M.setNpcBehindDistance(Value)
        if M.getNpcTeleportEnabled() and M.getCurrentNPCTarget() then
            M.anchorToNPC(M.getCurrentNPCTarget())
        end
    end,
})
MainNPC:AddButton({
    Text = "Susunod na NPC",
    Func = function()
        if M.getNpcTeleportEnabled() then
            local newTarget = M.switchToNextNPC()
            if newTarget then
                M.setCurrentNPCTarget(newTarget)
                M.anchorToNPC(newTarget)
            end
        end
    end,
})

-- ── Main › Boss Farm ───────────────────────────────────────────────────────
local MainBoss = Tabs.Main:AddLeftGroupbox("Bukirin ng Boss", "skull")
MainBoss:AddToggle("BossTeleportToggle", {
    Text    = "I-teleport sa D-Rex Boss",
    Default = false,
    Callback = function(Value)
        M.toggleBossTeleport(Value)
    end,
})
MainBoss:AddSlider("BossDistance", {
    Text     = "Layo sa Likuran",
    Default  = 5,
    Min      = 1,
    Max      = 100,
    Rounding = 1,
    Callback = function(Value)
        M.setBossBehindDistance(Value)
        if M.getBossTeleportEnabled() and M.getCurrentBossTarget() then
            M.anchorToBoss(M.getCurrentBossTarget())
        end
    end,
})
MainBoss:AddButton({
    Text = "Susunod na Boss",
    Func = function()
        if M.getBossTeleportEnabled() then
            local newTarget = M.switchToNextBoss()
            if newTarget then
                M.setCurrentBossTarget(newTarget)
                M.anchorToBoss(newTarget)
            end
        end
    end,
})

-- ── Main › Combat ──────────────────────────────────────────────────────────
local MainCombat = Tabs.Main:AddRightGroupbox("Combat", "sword")
MainCombat:AddToggle("AutoAttackToggle", {
    Text    = "Awtomatikong Pag-atake",
    Default = false,
    Callback = function(Value)
        M.toggleAutoAttack(Value)
    end,
})
MainCombat:AddToggle("AutoHeavyToggle", {
    Text    = "Mode ng Labanan",
    Default = false,
    Callback = function(Value)
        M.toggleAutoHeavy(Value)
    end,
})
MainCombat:AddButton({
    Text = "Agad na Pagkawala",
    Func = function()
        M.executeInstantDespawn()
    end,
})

-- ── Main › Player Farm ─────────────────────────────────────────────────────
local MainPlayer = Tabs.Main:AddRightGroupbox("Bukirin ng Manlalaro", "users")
MainPlayer:AddToggle("TeleportToggle", {
    Text    = "I-teleport sa mga Manlalaro",
    Default = false,
    Callback = function(Value)
        M.setTeleporting(Value)
        if Value then
            M.setPlayerIndex(0)
            local target = M.getNextTarget()
            M.setCurrentTarget(target)
            if target then
                M.anchorToPlayer(target)
            end
        else
            M.setCurrentTarget(nil)
        end
    end,
})
MainPlayer:AddToggle("TargetMovingToggle", {
    Text    = "Target na Gumagalaw",
    Default = true,
    Callback = function(Value)
        M.setTargetMovingPlayers(Value)
        if not Value and not M.getTargetStillPlayers() then
            M.setTargetStillPlayers(true)
        end
    end,
})
MainPlayer:AddToggle("TargetStillToggle", {
    Text    = "Target na Nakatayo",
    Default = true,
    Callback = function(Value)
        M.setTargetStillPlayers(Value)
        if not Value and not M.getTargetMovingPlayers() then
            M.setTargetMovingPlayers(true)
        end
    end,
})
MainPlayer:AddSlider("BehindDistance", {
    Text     = "Layo sa Likuran",
    Default  = 5,
    Min      = 1,
    Max      = 100,
    Rounding = 1,
    Callback = function(Value)
        M.setBehindDistance(Value)
        if M.getTeleporting() and M.getCurrentTarget() then
            M.anchorToPlayer(M.getCurrentTarget())
        end
    end,
})
MainPlayer:AddButton({
    Text = "Susunod na Target",
    Func = function()
        if M.getTeleporting() then
            local validPlayers = M.getValidPlayers()
            if #validPlayers > 0 then
                local target = M.getNextTarget()
                M.setCurrentTarget(target)
                if target then
                    M.anchorToPlayer(target)
                end
            end
        end
    end,
})

-- ── Amber › Amber Tracker ──────────────────────────────────────────────────
local AmberLeft = Tabs.Amber:AddLeftGroupbox("Tagasubaybay ng Amber", "gem")
AmberLeft:AddToggle("AmberESPToggle", {
    Text    = "Ipakita ang Handa nang Amber (ESP)",
    Default = false,
    Callback = function(Value)
        M.toggleAmberESP(Value)
    end,
})


-- ── Amber › Shop ──────────────────────────────────────────────────────────
local AmberShop = Tabs.Amber:AddLeftGroupbox("Awtomatikong Pagkolekta", "shopping-cart")
AmberShop:AddLabel("↓")
AmberShop:AddButton({
    Text = "Awtomatikong kolektahin ang Amber. Bilhin na ngayon!",
    Func = function()
        setclipboard("https://combooo-wickshop.vercel.app/")
        Library:Notify("Nakopya ang link ng tindahan sa clipboard!")
    end,
})

-- ── Amber › Auto Revive ────────────────────────────────────────────────────
local AmberRight = Tabs.Amber:AddRightGroupbox("Awtomatikong Muling Pagkabuhay", "zap")
AmberRight:AddToggle("AutoSpawnToggle", {
    Text    = "Awtomatikong Muling Pagkabuhay",
    Default = false,
    Callback = function(Value)
        M.setAutoSpawnEnabled(Value)
        if Value then M.setLastVisibleGuis({}) end
    end,
})
AmberRight:AddDropdown("DinoSelection", {
    Values   = M.allDinosaurs,
    Default  = "Gallimimus",
    Text     = "Pumili ng Dinosaur",
    Callback = function(Value)
        M.setSpawnArgs({ [1] = Value })
    end,
})
AmberRight:AddButton({
    Text = "Manwal na Pag-spawn Ngayon",
    Func = function()
        M.executeSpawn()
    end,
})

-- ── Settings › Anti-Void ───────────────────────────────────────────────────
local UtilityAntiVoid = Tabs.Utility:AddLeftGroupbox("Laban sa Void", "shield")
UtilityAntiVoid:AddToggle("AntiVoidToggle", {
    Text    = "Kontrahin ang Void",
    Default = false,
    Callback = function(Value)
        if Value then
            M.enableAntiVoid()
        else
            M.disableAntiVoid()
        end
    end,
})

-- ── Settings › Other ───────────────────────────────────────────────────────
local UtilityOther = Tabs.Utility:AddLeftGroupbox("Other", "zap")
UtilityOther:AddToggle("NoclipToggle", {
    Text    = "Noclip",
    Default = false,
    Callback = function(Value)
        if Value then
            M.enableNoclip()
        else
            M.disableNoclip()
        end
    end,
})
UtilityOther:AddToggle("AntiAFKToggle", {
    Text    = "Laban sa AFK",
    Default = false,
    Callback = function(Value)
        if Value then
            M.enableAntiAfk()
        else
            M.disableAntiAfk()
        end
    end,
})
UtilityOther:AddToggle("StaffWatchToggle", {
    Text    = "Naghahanap ng ban hammer (bantay ng staff)",
    Default = false,
    Callback = function(Value)
        M.toggleStaffWatch(Value)
    end,
})

-- ── Settings › Combat ──────────────────────────────────────────────────────
local Combat = Tabs.Utility:AddRightGroupbox("Combat", "sword")
Combat:AddToggle("AutoAttackToggle", {
    Text    = "Awtomatikong Pag-atake",
    Default = false,
    Callback = function(Value)
        M.toggleAutoAttack(Value)
    end,
})
Combat:AddToggle("AutoHeavyToggle", {
    Text    = "Mode ng Labanan",
    Default = false,
    Callback = function(Value)
        M.toggleAutoHeavy(Value)
    end,
})
Combat:AddButton({
    Text = "Mabilis na Pagpapakamatay (despawn)",
    Func = function()
        M.executeInstantDespawn()
    end,
})

-- ── Settings › UI Settings ─────────────────────────────────────────────────
local UISettings = Tabs.Utility:AddRightGroupbox("Mga Setting ng UI", "settings")
UISettings:AddToggle("KeybindMenuOpen", {
    Default  = Library.KeybindFrame.Visible,
    Text     = "Buksan ang Menu ng Keybind",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})
UISettings:AddToggle("ShowCustomCursor", {
    Text    = "Custom na Cursor",
    Default = false,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})
UISettings:AddDropdown("NotificationSide", {
    Values   = { "Left", "Right" },
    Default  = "Right",
    Text     = "Gilid ng Notipikasyon",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})
UISettings:AddButton("Unload", function()
    Library:Unload()
end)

-- ── Theme / Save setup ─────────────────────────────────────────────────────
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("ComboChronicle")
SaveManager:SetFolder("ComboChronicle")
SaveManager:BuildConfigSection(Tabs.Utility)
ThemeManager:ApplyToTab(Tabs.Utility)

game:BindToClose(M.cleanup)
SaveManager:LoadAutoloadConfig()