-- Russian Translation
-- Script ID: ab8c671ae4b6cd1d119f8ab0f73079aa
-- Created: 2026-05-12T20:47:28.245Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library      = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager  = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Core = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/Script/main/behair_dupemodule"))()

local Options           = Library.Options
local state             = Core.state
local MUTATIONS         = Core.MUTATIONS
local RARITY_FILTERS    = Core.RARITY_FILTERS
local RARITY_OVERRIDES  = Core.RARITY_OVERRIDES
local getFilteredLabels = Core.getFilteredLabels

local Window = Library:CreateWindow({
	Title            = "COMBOWICK",
	Footer           = "Create",
	ShowCustomCursor = false,
	Size             = UDim2.new(0, 320, 0, 460),
})

local Tabs = {
	Create          = Window:AddTab("Create",      "zap"),
	Auto            = Window:AddTab("Auto",        "repeat"),
	["Настройки интерфейса"] = Window:AddTab("Настройки интерфейса", "settings"),
}

local SelectGroup = Tabs.Create:AddLeftGroupbox("Выбор питомца")

SelectGroup:AddDropdown("RarityFilter", {
	Text     = "Фильтр по редкости",
	Values   = RARITY_FILTERS,
	Default  = "All",
	Callback = function(v)
		state.filterRarity     = v
		state.selectedPetLabel = nil
		Options.PetSelect:SetValues(getFilteredLabels())
	end,
})

SelectGroup:AddDivider()

SelectGroup:AddDropdown("PetSelect", {
	Text     = "Выберите питомца",
	Values   = getFilteredLabels(),
	Default  = nil,
	Callback = function(v)
		state.selectedPetLabel = v
	end,
})

local OptionsGroup = Tabs.Create:AddRightGroupbox("Настройки создания")

OptionsGroup:AddDropdown("MutationSelect", {
	Text     = "Mutation",
	Values   = MUTATIONS,
	Default  = "Normal",
	Callback = function(v)
		state.selectedMutation = v
	end,
})

OptionsGroup:AddDivider()

OptionsGroup:AddDropdown("RarityOverride", {
	Text     = "Изменить редкость",
	Values   = RARITY_OVERRIDES,
	Default  = "Default",
	Callback = function(v)
		state.selectedRarity = v
	end,
})

OptionsGroup:AddDivider()

local StatusLabel = OptionsGroup:AddLabel("Статус: неактивно", true)

OptionsGroup:AddToggle("AutoSpawn", {
	Text     = "Автосоздание",
	Default  = false,
	Callback = function(v)
		if v and not state.selectedPetLabel then
			StatusLabel:SetText("Статус: питомец не выбран.")
			Options.AutoSpawn:SetValue(false)
			return
		end
		state.autoSpawn = v
		StatusLabel:SetText(v and "Status: Creating..." or "Статус: неактивно")
	end,
})

OptionsGroup:AddSlider("SpawnDelay", {
	Text     = "Задержка создания питомца (секунды)",
	Min      = 0.1,
	Max      = 10,
	Default  = 1,
	Rounding = 1,
	Callback = function(v)
		state.spawnDelay = v
	end,
})

local AutoGroup = Tabs.Auto:AddLeftGroupbox("Automation")

AutoGroup:AddToggle("AutoRebirth", {
	Text     = "Автоперерождение",
	Default  = false,
	Callback = function(v)
		state.autoRebirth = v
	end,
})

AutoGroup:AddDivider()

AutoGroup:AddToggle("AutoSpeedUpgrade", {
	Text     = "Скорость автоулучшения",
	Default  = false,
	Callback = function(v)
		state.autoSpeedUpgrade = v
	end,
})

AutoGroup:AddDivider()

AutoGroup:AddToggle("AutoSellAll", {
	Text     = "Автопродажа инвентаря",
	Default  = false,
	Callback = function(v)
		state.autoSellAll = v
	end,
})

local MenuGroup = Tabs["Настройки интерфейса"]:AddLeftGroupbox("Menu")
MenuGroup:AddLabel("Клавиша переключения меню")
	:AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Клавиша переключения меню" })
MenuGroup:AddButton("Выйти из скрипта", function() Library:Unload() end)

Library.ToggleKeybind = Options.MenuKeybind
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("COMBOWICK")
SaveManager:SetFolder("COMBOWICK/saves")
SaveManager:BuildConfigSection(Tabs["Настройки интерфейса"])
ThemeManager:ApplyToTab(Tabs["Настройки интерфейса"])
SaveManager:LoadAutoloadConfig()