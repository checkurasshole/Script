-- Protected Script (Vietnamese)
-- Script ID: 43aa38bbaff538e43444935b58fd31ae
-- Created: 2025-10-22T20:35:40.252Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local CoreModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/jurrasiccccc"))()

local Window = Fluent:CreateWindow({
    Title = "COMB0_BẤC",
    SubTitle = "",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    ReadThis = Window:AddTab({ Title = "Biết chưa?", Icon = "info" }),
    Main = Window:AddTab({ Title = "Chính", Icon = "home" }),
    Shop = Window:AddTab({ Title = "Mua sắm", Icon = "shopping-cart" }),
    Gamepass = Window:AddTab({ Title = "Lượt chơi", Icon = "gift" })
}

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "This is for trolling your friends, lol\n:Enabling shop features will not allow you to play!",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "R",
            Content = "T",
            Duration = 2
        })
        task.wait(0.5)
       -- game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

-- Initialize the core module
CoreModule:Init()

-- Main Tab
local CoinInput = Tabs.Main:AddInput("CoinInput", {
    Title = "Số tiền xu",
    Default = "10000",
    Placeholder = "Enter amount",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        local amount = tonumber(Value)
        if amount and amount > 0 then
            CoreModule:SetCoinAmount(amount)
        end
    end
})

local CoinToggle = Tabs.Main:AddToggle("CoinToggle", {
    Title = "Tặng Xu Số tiền",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleCoins(Value)
        if Value then
            Fluent:Notify({
                Title = "Thành công",
                Content = "Tiền xu đã đổi thành" .. tostring(CoreModule:GetCoinAmount()),
                Duration = 2
            })
        end
    end
})

-- Shop Tab
local DinosaurPurchaseToggle = Tabs.Shop:AddToggle("DinosaurPurchaseToggle", {
    Title = "Bỏ qua mua khủng long",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleDinosaurBypass(Value)
        if Value then
            Fluent:Notify({
                Title = "Thành công",
                Content = "Đã bật bỏ qua mua khủng long",
                Duration = 2
            })
        end
    end
})

local UnlockAllButton = Tabs.Shop:AddButton({
    Title = "Mở khóa tất cả khủng long",
    Callback = function()
        CoreModule:UnlockAllDinosaurs()
        Fluent:Notify({
            Title = "Thành công",
            Content = "Tất cả các loài khủng long được mở khóa trực quan",
            Duration = 3
        })
    end
})

-- Gamepass Tab
local GamepassToggle = Tabs.Gamepass:AddToggle("GamepassToggle", {
    Title = "Bỏ qua tất cả lượt chơi",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleAllGamepasses(Value)
        if Value then
            Fluent:Notify({
                Title = "Thành công",
                Content = "Đã bật tất cả lượt chơi",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("PlayableAI", {
    Title = "Mở khóa AI có thể chơi được",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716086049, Value)
        if Value then
            Fluent:Notify({
                Title = "Thành công",
                Content = "Đã mở khóa AI có thể chơi được",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("StealthPass", {
    Title = "Mở khóa thẻ Stealth Pass",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716240706, Value)
        if Value then
            Fluent:Notify({
                Title = "Thành công",
                Content = "Đã mở khóa Thẻ thông hành tàng hình",
                Duration = 2
            })
        end
    end
})

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "Tham gia lại máy chủ",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "Rejoining...",
            Content = "Dịch chuyển tức thời bạn trở lại trò chơi",
            Duration = 2
        })
        task.wait(0.5)
        game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

Window:SelectTab(1)