-- Russian Translation
-- Script ID: f1a9d32aeadba30954bac7b04ae5531e
-- Created: 2026-05-06T07:48:56.660Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo   = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"

local Library      = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager  = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

local player = game.Players.LocalPlayer
local RS     = game:GetService("ReplicatedStorage")

local Window = Library:CreateWindow({
    Title            = "COMBOWICK",
    Footer           = "Пнуть счастливый блок",
    ShowCustomCursor = false,
    Size             = UDim2.new(0, 340, 0, 460),
})

local Tabs = {
    Tools           = Window:AddTab("Инструменты ",       "zap"),
    ["UI Settings"] = Window:AddTab("Настройки интерфейса ", "settings"),
}

local ToolsState = {
    CollectorEnabled = false,
    KeepPrompts      = false,
    AutoCollectFilled = false,
    SelectedRarities = { Secret = false, Godly = false, Exclusive = false },
}

local priorityMap = { Exclusive = 3, Godly = 2, Secret = 1 }

local function findMyPlot()
    local direct = workspace:FindFirstChild("Plot_" .. player.Name)
    if direct then return direct end
    local directDisplay = workspace:FindFirstChild("Plot_" .. player.DisplayName)
    if directDisplay then return directDisplay end
    for _, obj in pairs(workspace:GetChildren()) do
        if obj.Name:sub(1, 5) == "Plot_" then
            local gui = obj:FindFirstChild("PlotOwner")
                and obj.PlotOwner:FindFirstChild("SurfaceGui")
            if gui then
                local nameLabel = gui:FindFirstChild("Name")
                if nameLabel and (nameLabel.Text == player.Name or nameLabel.Text == player.DisplayName) then
                    return obj
                end
            end
        end
    end
end

local function collectMyPlot()
    local plot = findMyPlot()
    if not plot then return end
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    for _, floor in pairs(plot:GetChildren()) do
        if floor.Name:sub(1, 5) == "Floor" then
            local slots = floor:FindFirstChild("Slots")
            if not slots then continue end
            for _, slot in pairs(slots:GetChildren()) do
                local touch = slot:FindFirstChild("CollectButton")
                    and slot.CollectButton:FindFirstChild("Touch")
                if touch then
                    pcall(function()
                        firetouchinterest(touch, root, 0)
                        task.wait(0.05)
                        firetouchinterest(touch, root, 1)
                    end)
                end
            end
        end
    end
end

local function collectFilledSlots()
    local plot = workspace:FindFirstChild("Plot_" .. player.Name)
        or workspace:FindFirstChild("Plot_" .. player.DisplayName)
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not plot or not root then return end

    -- Equip everything in backpack first
    local backpack = player.Backpack
    for _, tool in pairs(backpack:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Parent = char
        end
    end

    for _, floor in pairs(plot:GetChildren()) do
        if floor.Name:sub(1, 5) == "Floor" then
            local slots = floor:FindFirstChild("Slots")
            if not slots then continue end
            for _, slot in pairs(slots:GetChildren()) do
                local spawn = slot:FindFirstChild("Spawn")
                if not spawn then continue end
                local hasItem = spawn:FindFirstChild("VisualItem")
                local prompt  = spawn:FindFirstChild("ProximityPrompt")
                if hasItem and prompt then
                    root.CFrame = CFrame.new(spawn.Position + Vector3.new(0, 4, 0))
                    task.wait(0.2)
                    pcall(function() fireproximityprompt(prompt) end)
                    task.wait(0.3)
                end
            end
        end
    end

    -- Collect money after grabbing all items
    collectMyPlot()
end

local CollectorGroup = Tabs.Tools:AddLeftGroupbox("Сборщик редкостей ")
local BaseToolsGroup = Tabs.Tools:AddRightGroupbox("Разблокировать всё / Бессмертие ")

CollectorGroup:AddDropdown("CollectorRaritySelect", {
    Text     = "Выбрать редкости ",
    Values   = { "Secret", "Godly", "Exclusive" },
    Default  = {},
    Multi    = true,
    Callback = function(v)
        ToolsState.SelectedRarities.Secret    = v["Secret"]    or false
        ToolsState.SelectedRarities.Godly     = v["Godly"]     or false
        ToolsState.SelectedRarities.Exclusive = v["Exclusive"] or false
    end,
})

CollectorGroup:AddToggle("CollectorEnabled", {
    Text    = "Автоматический мгновенный сбор ",
    Default = false,
    Callback = function(v)
        ToolsState.CollectorEnabled = v
        if v then
            -- Auto unlock all bases when collector starts
            for _, base in pairs(workspace.Bases:GetChildren()) do
                local walls  = base:FindFirstChild("Walls")
                local lasers = base:FindFirstChild("Lasers")
                if walls  then walls:Destroy()  end
                if lasers then lasers:Destroy() end
            end
            for _, npc in pairs(workspace.LocalNPCs:GetChildren()) do
                npc:Destroy()
            end
            Library:Notify({ Title = "Бессмертный", Description = "Все базы разблокированы.", Time = 3 })
            task.spawn(function()
                while ToolsState.CollectorEnabled do
                    local results = {}
                    for _, base in pairs(workspace.Bases:GetChildren()) do
                        local slots = base:FindFirstChild("Slots")
                        if not slots then continue end
                        for _, slot in pairs(slots:GetChildren()) do
                            local spawn       = slot:FindFirstChild("Spawn")
                            local spawnedItem = spawn and spawn:FindFirstChild("SpawnedItem")
                            local part        = spawnedItem and spawnedItem:FindFirstChild("Part")
                            local textLabels  = spawnedItem
                                and spawnedItem:FindFirstChild("InfoGUI")
                                and spawnedItem.InfoGUI:FindFirstChild("TextLabels")
                            local rarityLabel = textLabels and textLabels:FindFirstChild("Rarity")
                            if not (rarityLabel and part) then continue end
                            local rarityText = rarityLabel.Text:lower()
                            for rarity, priority in pairs(priorityMap) do
                                if ToolsState.SelectedRarities[rarity] and rarityText:find(rarity:lower()) then
                                    table.insert(results, { part = part, priority = priority })
                                    break
                                end
                            end
                        end
                    end
                    table.sort(results, function(a, b) return a.priority > b.priority end)
                    local char = player.Character
                    local root = char and char:FindFirstChild("HumanoidRootPart")
                    if root and #results > 0 then
                        for i = 1, math.min(3, #results) do
                            local part = results[i].part
                            root.CFrame = CFrame.new(part.Position + Vector3.new(0, 5, 0))
                            task.wait(0.3)
                            local prompt = part:FindFirstChild("ProximityPrompt")
                            if prompt then pcall(function() fireproximityprompt(prompt) end) end
                            task.wait(1)
                            local returnPart = workspace:FindFirstChild("Map") and workspace.Map:FindFirstChild("Part")
                            if returnPart then
                                root.CFrame = returnPart.CFrame + Vector3.new(0, 5, 0)
                            end
                            task.wait(0.5)
                        end
                    end
                    task.wait(1)
                end
            end)
        end
    end,
})

local promptConnection = nil

BaseToolsGroup:AddButton({
    Text = "Разблокировать все базы (Бессмертие) ",
    Func = function()
        for _, base in pairs(workspace.Bases:GetChildren()) do
            local walls  = base:FindFirstChild("Walls")
            local lasers = base:FindFirstChild("Lasers")
            if walls  then walls:Destroy()  end
            if lasers then lasers:Destroy() end
        end
        for _, npc in pairs(workspace.LocalNPCs:GetChildren()) do
            npc:Destroy()
        end
        -- Steal Anything: force all prompts open
        ToolsState.KeepPrompts = true
        task.spawn(function()
            while ToolsState.KeepPrompts do
                for _, base in pairs(workspace.Bases:GetChildren()) do
                    local slots = base:FindFirstChild("Slots")
                    if not slots then continue end
                    for _, slot in pairs(slots:GetChildren()) do
                        local prompt = slot:FindFirstChild("Spawn")
                            and slot.Spawn:FindFirstChild("SpawnedItem")
                            and slot.Spawn.SpawnedItem:FindFirstChild("Part")
                            and slot.Spawn.SpawnedItem.Part:FindFirstChild("ProximityPrompt")
                        if prompt then
                            prompt.Enabled = true
                            prompt.MaxActivationDistance = 20
                            prompt.RequiresLineOfSight = false
                        end
                    end
                end
                task.wait(1)
            end
        end)
        if promptConnection then promptConnection:Disconnect() end
        promptConnection = workspace.DescendantAdded:Connect(function(desc)
            if ToolsState.KeepPrompts and desc:IsA("ProximityPrompt") then
                task.wait(0.1)
                desc.Enabled = true
                desc.MaxActivationDistance = 20
                desc.RequiresLineOfSight = false
            end
        end)
        Library:Notify({ Title = "Бессмертный", Description = "Все базы разблокированы.", Time = 3 })
    end,
})

BaseToolsGroup:AddDivider()

BaseToolsGroup:AddButton({
    Text = "Собрать всех питомцев ",
    Func = function() collectFilledSlots() end,
})

BaseToolsGroup:AddToggle("AutoCollectFilledSlots", {
    Text    = "Автосбор всех питомцев ",
    Default = false,
    Callback = function(v)
        ToolsState.AutoCollectFilled = v
        if v then
            task.spawn(function()
                while ToolsState.AutoCollectFilled do
                    collectFilledSlots()
                    task.wait(2)
                end
            end)
        end
    end,
})

BaseToolsGroup:AddDivider()

BaseToolsGroup:AddButton({
    Text = "Собрать мой участок ",
    Func = function() collectMyPlot() end,
})

BaseToolsGroup:AddToggle("AutoCollect", {
    Text    = "Автоматический сбор денег ",
    Default = false,
    Callback = function(v)
        ToolsState.AutoCollect = v
        if v then
            task.spawn(function()
                while ToolsState.AutoCollect do
                    collectMyPlot()
                    task.wait(2)
                end
            end)
        end
    end,
})

local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Меню ")

MenuGroup:AddDivider()
MenuGroup:AddLabel("Назначение клавиши меню ")
    :AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Назначение клавиши меню " })

MenuGroup:AddButton("Выгрузить скрипт ", function() Library:Unload() end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("combowick")
SaveManager:SetFolder("combowick/tools")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])
SaveManager:LoadAutoloadConfig()