-- Protected Script (Vietnamese)
-- Script ID: 6a71bdb3a5db4f039e7d628ed03d38b1
-- Created: 2025-12-22T18:57:56.829Z
-- Source: External API
-- Auto-generated by Lua Script Vault

if getgenv().ComboWickLoaded then
    getgenv().ComboWickLoaded:Unload()
end

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()

local Module = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/Trt/refs/heads/main/inf%20money"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ShowCustomCursor = false

getgenv().ComboWickLoaded = Library

local Window = Library:CreateWindow({
    Title = "COMBO_WICK",
    Footer = "By COMBO_WICK",
    ShowCustomCursor = false,
    Position = UDim2.new(0.5, -200, 0.5, -150),
    Size = UDim2.new(0, 400, 0, 300),
})

local Tabs = {
    Main = Window:AddTab("Chính", "shopping-cart"),
    Other = Window:AddTab("Khác", "zap"),
}

local MainBox = Tabs.Main:AddLeftGroupbox("Thống kê", "package")

MainBox:AddButton({
    Text = "Tối đa hóa thống kê",
    Func = function()
        Module.trainStat("9999e15")
        task.wait(0.1)
        Module.winGainStat("9999e15")
    end
})

MainBox:AddDivider()

local customTrainValue = "1"

MainBox:AddInput("CustomTrain", {
    Default = "",
    Numeric = false,
    Finished = true,
    Text = "Sức mạnh",
    Placeholder = "",
    Callback = function(Value)
        customTrainValue = Value
    end,
})

MainBox:AddButton({
    Text = "Tập luyện sức mạnh",
    Func = function()
        Module.trainStat(customTrainValue)
    end
})

MainBox:AddDivider()

local customWinGainValue = "1"

MainBox:AddInput("CustomWinGain", {
    Default = "",
    Numeric = false,
    Finished = true,
    Text = "Tiền",
    Placeholder = "",
    Callback = function(Value)
        customWinGainValue = Value
    end,
})

MainBox:AddButton({
    Text = "Tăng tiền",
    Func = function()
        Module.winGainStat(customWinGainValue)
    end
})

local RightBox = Tabs.Main:AddRightGroupbox("Khác", "zap")

RightBox:AddButton({
    Text = "Phép thuật",
    Func = function()
        Module.enchanted(195500000000, 0.15)
    end
})

RightBox:AddButton({
    Text = "Tăng sức khỏe / Tái sinh",
    Func = function()
        Module.healthAdd(25)
    end
})

local OtherBox = Tabs.Other:AddLeftGroupbox("Thế giới & Vật phẩm", "globe")

OtherBox:AddButton({
    Text = "Mở khóa tất cả thế giới",
    Func = function()
        Module.unlockWorld("W3")
        task.wait(0.1)
        Module.unlockWorld("W2")
        task.wait(0.1)
        Module.unlockWorld("W1")
    end
})

OtherBox:AddDivider()

OtherBox:AddButton({
    Text = "Mua xẻng tốt nhất",
    Func = function()
        Module.buyPower("Water", 39100000000)
    end
})

local PetBox = Tabs.Other:AddRightGroupbox("Thú cưng", "star")

local petData = 900

PetBox:AddInput("PetData", {
    Default = "900",
    Numeric = true,
    Finished = true,
    Text = "Giá trị dữ liệu thú cưng",
    Placeholder = "",
    Callback = function(Value)
        petData = tonumber(Value) or 900
    end,
})

PetBox:AddButton({
    Text = "Nhận thú cưng tốt nhất",
    Func = function()
        Module.hatchPet("Exotic", "Celestia", petData)
    end
})