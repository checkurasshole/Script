-- Protected Script (Japanese)
-- Script ID: 380b8ae878520fd21220a0fc9ba65c2a
-- Created: 2025-10-22T20:35:39.716Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local CoreModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/jurrasiccccc"))()

local Window = Fluent:CreateWindow({
    Title = "COMB 0_WICK",
    SubTitle = "",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    ReadThis = Window:AddTab({ Title = "「確認」は、", Icon = "info" }),
    Main = Window:AddTab({ Title = "メイン", Icon = "home" }),
    Shop = Window:AddTab({ Title = "ショップ", Icon = "shopping-cart" }),
    Gamepass = Window:AddTab({ Title = "ゲームパス", Icon = "gift" })
}

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "This is for trolling your friends, lol\n:Enabling shop features will not allow you to play!",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "R",
            Content = "T",
            Duration = 2
        })
        task.wait(0.5)
       -- game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

-- Initialize the core module
CoreModule:Init()

-- Main Tab
local CoinInput = Tabs.Main:AddInput("CoinInput", {
    Title = "R3Vコイン量",
    Default = "10000",
    Placeholder = "Enter amount",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        local amount = tonumber(Value)
        if amount and amount > 0 then
            CoreModule:SetCoinAmount(amount)
        end
    end
})

local CoinToggle = Tabs.Main:AddToggle("CoinToggle", {
    Title = "コインの金額を贈る",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleCoins(Value)
        if Value then
            Fluent:Notify({
                Title = "成功",
                Content = "コインがに変更されました" .. tostring(CoreModule:GetCoinAmount()),
                Duration = 2
            })
        end
    end
})

-- Shop Tab
local DinosaurPurchaseToggle = Tabs.Shop:AddToggle("恐竜購入トグル", {
    Title = "恐竜購入をバイパス",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleDinosaurBypass(Value)
        if Value then
            Fluent:Notify({
                Title = "成功",
                Content = "恐竜購入バイパス有効",
                Duration = 2
            })
        end
    end
})

local UnlockAllButton = Tabs.Shop:AddButton({
    Title = "すべての恐竜をアンロック",
    Callback = function()
        CoreModule:UnlockAllDinosaurs()
        Fluent:Notify({
            Title = "成功",
            Content = "すべての恐竜が視覚的にロック解除されました",
            Duration = 3
        })
    end
})

-- Gamepass Tab
local GamepassToggle = Tabs.Gamepass:AddToggle("GamepassToggle", {
    Title = "すべてのゲームパスをバイパス",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleAllGamepasses(Value)
        if Value then
            Fluent:Notify({
                Title = "成功",
                Content = "すべてのゲームパスが有効です",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("PlayableAI", {
    Title = "プレイアブルAIのロックを解除",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716086049, Value)
        if Value then
            Fluent:Notify({
                Title = "成功",
                Content = "プレイ可能なAIがアンロックされました",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("StealthPass", {
    Title = "ステルスパスを解放",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716240706, Value)
        if Value then
            Fluent:Notify({
                Title = "成功",
                Content = "ステルスパスが解放されました",
                Duration = 2
            })
        end
    end
})

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "サーバーに再参加する",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "Rejoining...",
            Content = "ゲームにテレポートする",
            Duration = 2
        })
        task.wait(0.5)
        game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

Window:SelectTab(1)