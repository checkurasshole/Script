-- Spanish Translation
-- Script ID: b6f650c44a18105be613705b43f28e20
-- Created: 2026-09-07T07:39:44.102Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local MODULE_URL = "https://raw.githubusercontent.com/checkurasshole/combowick-stealanegg/main/spawner_module.lua"
local Core = loadstring(game:HttpGet(MODULE_URL))()
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/main/Library.lua"))()
local Window = Library:CreateWindow({ Title = "COMBO_WICK", Footer = "", NotifySide = "Right", ShowCustomCursor = false, AutoShow = true, Size = UDim2.fromOffset(640, 520) })
local Opt = Library.Options
Core.bindOpt(Opt)
local categoryList, mutationList, reqmod, child, dir, rarityOf, weightOf, earnRate, makePetItem, fmt = Core.categoryList, Core.mutationList, Core.reqmod, Core.child, Core.dir, Core.rarityOf, Core.weightOf, Core.earnRate, Core.makePetItem, Core.fmt
local spawnPet, activeCount, activeCapacity, placeEggDirect, addBackpackEgg, setMoney, sellFakePets, applyShopSellFlag = Core.spawnPet, Core.activeCount, Core.activeCapacity, Core.placeEggDirect, Core.addBackpackEgg, Core.setMoney, Core.sellFakePets, Core.applyShopSellFlag
local unequipFakePet, clearAll, unlockAllIndexVisual, saveProfile, grantGear, unequipBat, unequipAllRealPets, track = Core.unequipFakePet, Core.clearAll, Core.unlockAllIndexVisual, Core.saveProfile, Core.grantGear, Core.unequipBat, Core.unequipAllRealPets, Core.track
local DataF, PET_LABEL_TO_KEY, FAKE_ROSTER, FAKE_INV, SaveMod, Gears, SHOP, lp, TeleportService = Core.DataF, Core.PET_LABEL_TO_KEY, Core.FAKE_ROSTER, Core.FAKE_INV, Core.SaveMod, Core.Gears, Core.SHOP, Core.lp, Core.TeleportService
do
    local InfoTab = Window:AddTab("Info", "info")
    local ibox = InfoTab:AddLeftGroupbox("Info")
    ibox:AddLabel("Primero quita tus mascotas.")
end
local Tab = Window:AddTab("Generador", "egg")
local PetBox = Tab:AddLeftGroupbox("Mascota")
local cats = categoryList()
PetBox:AddDropdown("sp_cat", { Text = "Animal", Values = cats, Default = (table.find(cats, "Lava Dragon") and "Lava Dragon") or cats[1], Searchable = true })
PetBox:AddDropdown("sp_mut", { Text = "Mutación", Values = mutationList(), Multi = true, AllowNull = true, Default = {} })
PetBox:AddDropdown("sp_pers", { Text = "Personalidad", Values = { "Auto", "Roll", "Normal", "InvertedModel", "Lazy", "Loyal", "Energetic", "Shy", "Scared", "ExtremelyEnergetic", "UltraLoyal", "JumpCrazy" }, Default = "Auto" })
PetBox:AddDropdown("sp_color", { Text = "Color", Values = { "Auto", "Roll", "Base" }, Default = "Auto" })
do
    local skinVals = { "None" }
    local ES = reqmod(child(DataF, "EggSkins")); local dir = (ES and (ES.Directory or ES)) or {}
    if type(dir) == "table" then for k, v in pairs(dir) do if type(k) == "string" and type(v) == "table" and v.ModelName then skinVals[#skinVals + 1] = k end end end
    table.sort(skinVals)
    PetBox:AddDropdown("sp_eggskin", { Text = "Skin del Huevo", Values = skinVals, Default = "None", Searchable = true })
end
if Opt.sp_eggskin then Core.setCW_SKIN(Opt.sp_eggskin.Value or "None"); track(Opt.sp_eggskin:OnChanged(function() Core.setCW_SKIN(Opt.sp_eggskin.Value or "None") end)) end
PetBox:AddSlider("sp_scale", { Text = "Tamaño", Default = 3, Min = 0.2, Max = 50, Rounding = 2 })
PetBox:AddInput("sp_weight", { Text = "Peso (Kg)", Numeric = true, Finished = true, Placeholder = "opcional" })
if Opt.sp_pers then Core.setCW_PERS(Opt.sp_pers.Value or "Auto"); track(Opt.sp_pers:OnChanged(function() Core.setCW_PERS(Opt.sp_pers.Value or "Auto") end)) end
if Opt.sp_color then Core.setCW_COLOR(Opt.sp_color.Value or "Auto"); track(Opt.sp_color:OnChanged(function() Core.setCW_COLOR(Opt.sp_color.Value or "Auto") end)) end
local Info = Tab:AddRightGroupbox("Info")
local lblRarity = Info:AddLabel("Rareza: -")
local lblWeight = Info:AddLabel("Peso: -")
local lblRate = Info:AddLabel("Ingresos: -")
local lblStatus = Info:AddLabel("Estado: inactivo")
local function setStatus(s) pcall(function() lblStatus:SetText("Estado: " .. tostring(s)) end) end
local function curMut()
    local v = Opt.sp_mut and Opt.sp_mut.Value
    local out = {}
    if type(v) == "table" then
        for k, val in pairs(v) do
            local m = (val == true) and k or val
            if type(m) == "string" and m ~= "None" and m ~= "" then out[#out + 1] = m end
        end
    elseif type(v) == "string" and v ~= "None" and v ~= "" then out[#out + 1] = v end
    return out
end
local function curCat()
    local sel = (Opt.sp_cat and Opt.sp_cat.Value) or cats[1]
    return PET_LABEL_TO_KEY[sel] or sel
end
local function curScale()
    local w = tonumber(Opt.sp_weight and Opt.sp_weight.Value)
    if w and w > 0 then
        local e = dir(curCat()); local mw = e and tonumber(e.ModelWeight) or 1
        return math.clamp((w / math.max(mw, 0.0001)) ^ (1 / 3), 0.01, 500)
    end
    return (Opt.sp_scale and Opt.sp_scale.Value) or 1
end
local function refreshInfo()
    local cat, scale, mut = curCat(), curScale(), curMut()
    pcall(function() lblRarity:SetText("Rareza: " .. rarityOf(cat)) end)
    pcall(function() lblWeight:SetText(("Peso: %.2f Kg  (escala %.2f)"):format(weightOf(cat, scale), scale)) end)
    pcall(function() lblRate:SetText("Ingresos: $" .. fmt(earnRate(cat, scale, mut, makePetItem(cat, scale, mut))) .. "/s") end)
end
if Opt.sp_cat then track(Opt.sp_cat:OnChanged(refreshInfo)) end
if Opt.sp_scale then track(Opt.sp_scale:OnChanged(refreshInfo)) end
if Opt.sp_weight then track(Opt.sp_weight:OnChanged(refreshInfo)) end
if Opt.sp_mut then track(Opt.sp_mut:OnChanged(refreshInfo)) end
task.spawn(function() task.wait(1); refreshInfo() end)
local Spawn = Tab:AddLeftGroupbox("Generar")
Spawn:AddButton({ Text = "Generar mascota", Func = function()
    task.spawn(function()
        local uid, rate, note = spawnPet(curCat(), curScale(), curMut())
        if not uid then setStatus(rate) return end
        if note == "capped" then
            setStatus(("límite %d/%d - enviado a la Mochila"):format(activeCount(), activeCapacity()))
            return
        end
        setStatus(("mascota: %s - $%s/s"):format((Opt.sp_cat and Opt.sp_cat.Value) or curCat(), fmt(rate)))
    end)
end })
Spawn:AddButton({ Text = "Generar huevo en la parcela", Func = function()
    task.spawn(function()
        local ok, r = placeEggDirect(curCat(), curScale(), curMut())
        setStatus(ok and "huevo colocado - acércate e incuba" or ("fallo del huevo: " .. tostring(r)))
    end)
end })
Spawn:AddButton({ Text = "Añadir huevo a la Mochila", Func = function()
    local ok, r = addBackpackEgg(curCat(), curScale(), curMut())
    setStatus(ok and "huevo -> mochila" or ("fallo del huevo: " .. tostring(r)))
end })
Spawn:AddToggle("sp_eggtimer", { Text = "Temporizador de crecimiento del huevo", Default = true, Callback = function(v) Core.setEGG_TIMER(v) end })
Spawn:AddToggle("sp_freeskip", { Text = "Saltar crecimiento gratis", Default = false, Tooltip = "ACTIVADO = 'Skip Growth!' es instantáneo y gratis. DESACTIVADO = usa la compra con Robux (saldo falso de la pestaña Tienda) para saltar.", Callback = function(v) Core.setFREE_SKIP(v) end })
local Econ = Tab:AddRightGroupbox("Economía")
Econ:AddInput("sp_money", { Text = "Cantidad", Numeric = true, Finished = true, Placeholder = "cantidad de dinero" })
Econ:AddButton({ Text = "Establecer dinero", Func = function()
    local ok, r = setMoney(Opt.sp_money and Opt.sp_money.Value)
    setStatus(ok and ("dinero -> $" .. fmt(r)) or ("fallo del dinero: " .. tostring(r)))
end })
Econ:AddButton({ Text = "Vender mascotas", Func = function()
    task.spawn(function() local ok, r = sellFakePets(); setStatus(tostring(r)) end)
end })
Econ:AddToggle("sp_shopsell", { Text = "Quitar mascotas de favoritos", Default = true, Callback = function(v)
    if v and not _G.__CW_SHOPSELL then
        setStatus("no disponible (el ejecutor no tiene hookmetamethod) - usa 'Vender mascotas'")
        return
    end
    Core.setSHOP_SELL(v)
    applyShopSellFlag()
    if v then
        local active = {}
        for uid in pairs(FAKE_ROSTER) do active[#active + 1] = uid end
        for _, uid in ipairs(active) do pcall(unequipFakePet, uid) end
        setStatus(("quitado de favoritos - %d mascota(s) movida(s) a la mochila, ahora vendible(s) en la tienda"):format(#active))
    else
        setStatus("vuelto a favoritos - mascotas ocultas de la tienda otra vez")
    end
end })
if Opt.sp_shopsell and Opt.sp_shopsell.Value and _G.__CW_SHOPSELL and not Core.getSHOP_SELL() then Core.setSHOP_SELL(true); pcall(applyShopSellFlag) end
local Sess = Tab:AddRightGroupbox("Sesión")
Sess:AddButton({ Text = "Borrar mascotas generadas", Func = function() clearAll(); setStatus("se borraron todas las mascotas y huevos generados") end })
Sess:AddButton({ Text = "Volver a unirse al servidor", Func = function()
    setStatus("reconectando...")
    task.spawn(function()
        local ok = pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, lp) end)
        if not ok then pcall(function() TeleportService:Teleport(game.PlaceId, lp) end) end
    end)
end })
local Idx = Tab:AddLeftGroupbox("Índice")
Idx:AddToggle("sp_indexauto", { Text = "Indexar al generar", Default = true, Callback = function(v) Core.setINDEX_ON_SPAWN(v) end })
Idx:AddButton({ Text = "Desbloquear todo el índice", Func = function()
    local ok, n = unlockAllIndexVisual()
    setStatus(ok and ("índice: todo desbloqueado y mostrado como reclamado (" .. tostring(n) .. " nuevo)") or ("fallo del índice: " .. tostring(n)))
end })
local BossBox = Tab:AddRightGroupbox("Jefe")
BossBox:AddInput("sp_bosstokens", { Text = "Fichas de Jefe", Numeric = true, Finished = true, Placeholder = "cantidad" })
BossBox:AddButton({ Text = "Establecer fichas de Jefe", Func = function()
    local n = tonumber(Opt.sp_bosstokens and Opt.sp_bosstokens.Value)
    if not (n and SaveMod and SaveMod.ApplyLocalField) then setStatus("fichas de jefe no disponibles"); return end
    pcall(SaveMod.ApplyLocalField, "BossTokens", n); setStatus("fichas de jefe -> " .. fmt(n))
end })
BossBox:AddButton({ Text = "Desbloquear maestría de Jefe", Func = function()
    local prof = saveProfile()
    if not (prof and SaveMod and SaveMod.ApplyLocalField) then setStatus("maestría de jefe no disponible"); return end
    local BM = reqmod(child(DataF, "BossMastery"))
    local state = (type(prof.BossMastery) == "table") and table.clone(prof.BossMastery) or (BM and BM.Blank and BM.Blank()) or {}
    state.Mastery = 30
    local claimed = {}
    local list = BM and (BM.Milestones or BM.List)
    if type(list) == "table" then for _, m in ipairs(list) do if type(m) == "table" and m.Id then claimed[m.Id] = true end end end
    if not next(claimed) then for _, id in ipairs({ "Mastery3", "Mastery5", "Mastery10", "Mastery15", "Mastery20", "Mastery30" }) do claimed[id] = true end end
    state.ClaimedMilestoneIds = claimed
    pcall(SaveMod.ApplyLocalField, "BossMastery", state)
    pcall(function() SaveMod.FieldSignal("BossMastery"):Fire(state, state) end)
    setStatus("maestría de jefe: 30 muertes + hitos mostrados como reclamados (visual)")
end })
local function extraGearList()
    local out = {}
    if Gears and Gears.Directory then
        for id, g in pairs(Gears.Directory) do
            if type(g) == "table" and g.IndexBatTier == nil and id ~= "Bat" then
                out[#out + 1] = id
            end
        end
    end
    table.sort(out)
    if #out == 0 then out = { "BeeLauncher" } end
    return out
end
local GearBox = Tab:AddRightGroupbox("Equipo")
GearBox:AddDropdown("sp_gear", { Text = "Bofetada / Gadget", Values = extraGearList(), Default = extraGearList()[1], Searchable = true })
GearBox:AddButton({ Text = "Equipar equipo", Func = function()
    local id = Opt.sp_gear and Opt.sp_gear.Value
    task.spawn(function() local ok, r = grantGear(id); setStatus(ok and ("equipado " .. tostring(r)) or ("fallo del equipo: " .. tostring(r))) end)
end })
GearBox:AddButton({ Text = "Quitar equipo", Func = function() unequipBat(); setStatus("equipo quitado") end })
local ShopTab = Window:AddTab("Tienda", "shopping-cart")
local ShopBal = ShopTab:AddLeftGroupbox("Saldo de Robux")
ShopBal:AddInput("shop_bal", { Text = "Saldo", Numeric = true, Finished = true, Default = tostring(SHOP.balance), Placeholder = "p. ej. 1000000" })
ShopBal:AddButton({ Text = "Aplicar saldo", Func = function() SHOP.setBalance(Opt.shop_bal and Opt.shop_bal.Value) end })
ShopBal:AddButton({ Text = "+100k", Func = function() SHOP.setBalance(SHOP.getBalance() + 100000); if Opt.shop_bal then pcall(function() Opt.shop_bal:SetValue(tostring(SHOP.getBalance())) end) end end })
ShopBal:AddButton({ Text = "+1M", Func = function() SHOP.setBalance(SHOP.getBalance() + 1000000); if Opt.shop_bal then pcall(function() Opt.shop_bal:SetValue(tostring(SHOP.getBalance())) end) end end })
local ShopCfg = ShopTab:AddRightGroupbox("Compra")
ShopCfg:AddToggle("shop_drop", { Text = "Reducir saldo al comprar", Default = SHOP.dropOnBuy, Tooltip = "Reduce los Robux falsos según el precio del artículo tras cada compra.", Callback = function(v) SHOP.dropOnBuy = v end })
ShopCfg:AddToggle("shop_instant", { Text = "Omitir aviso (compra instantánea)", Default = SHOP.instant, Tooltip = "ACTIVADO = las compras se completan sin ningún aviso. DESACTIVADO = aparece el aviso real y capturamos su Buy.", Callback = function(v) SHOP.instant = v end })
local shopStatus = ShopCfg:AddLabel("Inactivo. Abre un artículo de la tienda y presiona Buy.", true)
SHOP.onStatus = function(msg) pcall(function() shopStatus:SetText(tostring(msg)) end) end
local SettingsTab = Window:AddTab("Ajustes", "settings")
do
    local ok = pcall(function()
        local ThemeManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/main/addons/ThemeManager.lua"))()
        local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/main/addons/SaveManager.lua"))()
        ThemeManager:SetLibrary(Library)
        SaveManager:SetLibrary(Library)
        SaveManager:IgnoreThemeSettings()
        SaveManager:SetIgnoreIndexes({})
        ThemeManager:SetFolder("COMBO_WICK")
        SaveManager:SetFolder("COMBO_WICK/StealAnEgg")
        SaveManager:BuildConfigSection(SettingsTab)
        ThemeManager:ApplyToTab(SettingsTab)
        SaveManager:LoadAutoloadConfig()
    end)
    if not ok then SettingsTab:AddLeftGroupbox("Ajustes"):AddLabel("Error al cargar los complementos de config (¿HttpGet bloqueado?)", true) end
end
local LoadoutBox = SettingsTab:AddRightGroupbox("Equipamiento")
LoadoutBox:AddToggle("sp_unequipreal", { Text = "Quitar todas las mascotas reales", Default = true, Tooltip = "ACTIVADO = quita cada mascota REAL (no falsa) activa mediante el propio flujo del juego (del lado del servidor, afecta tu equipamiento real). Se activa una vez al ejecutar.", Callback = function(v)
    if not v then return end
    task.spawn(function()
        local ok, n = unequipAllRealPets()
        setStatus(ok and ("quitado " .. tostring(n) .. " mascota(s) real(es)") or ("fallo al quitar: " .. tostring(n)))
    end)
end })
LoadoutBox:AddButton({ Text = "Quitar mascotas reales", Func = function()
    task.spawn(function()
        local ok, n = unequipAllRealPets()
        setStatus(ok and ("quitado " .. tostring(n) .. " mascota(s) real(es)") or ("fallo al quitar: " .. tostring(n)))
    end)
end })
task.spawn(function()
    for _ = 1, 20 do if lp.Character and lp.Character:FindFirstChildWhichIsA("Humanoid") then break end task.wait(0.5) end
    local t = Opt.sp_unequipreal
    if t and t.Value == false then return end
    local empties = 0
    for _ = 1, 12 do
        local tt = Opt.sp_unequipreal
        if tt and tt.Value == false then return end
        local ok, n = unequipAllRealPets()
        if ok and n and n > 0 then setStatus("quitado " .. tostring(n) .. " mascota(s) real(es) al inicio"); empties = 0
        else empties = empties + 1 end
        if empties >= 3 then break end
        task.wait(1)
    end
end)
local InterfaceBox = SettingsTab:AddLeftGroupbox("Interfaz")
pcall(function()
    InterfaceBox:AddLabel("Alternar GUI"):AddKeyPicker("MenuToggle", { Default = "RightShift", Text = "Alternar GUI", Mode = "Toggle", NoUI = false })
    Library.ToggleKeybind = Opt.MenuToggle
end)
Library:OnUnload(function()
    if _G.__CW_SPAWN_CLEAN then pcall(_G.__CW_SPAWN_CLEAN) end
    Library.Unloaded = true
end)
Library:Notify({ Title = "COMBO_WICK - Generador", Time = 4 })
