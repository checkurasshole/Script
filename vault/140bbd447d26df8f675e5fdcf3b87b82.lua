-- Korean Translation
-- Script ID: 140bbd447d26df8f675e5fdcf3b87b82
-- Updated: 2025-12-18T18:10:16.122Z
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local PlayerModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/hOL/refs/heads/main/moduleeeeeeee"))()
local GameModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/Trt/refs/heads/main/www"))() -- Replace with your GameModule URL

local Options = Library.Options
local Toggles = Library.Toggles

Library.ShowCustomCursor = false

local Window = Library:CreateWindow({
	Title = "COMBO_WICK",
	Footer = "v1.1",
	Icon = star,
	NotifySide = "Right",
	ShowCustomCursor = false,
	Size = UDim2.new(0, 500, 0, 450),
	Position = UDim2.new(0.5, -250, 0.5, -225),
})

local Tabs = {
	Main = Window:AddTab("메인", "gem"),
	Combat = Window:AddTab("전투", "sword"),
	Player = Window:AddTab("플레이어", "user"),
	["UI Settings"] = Window:AddTab("UI 설정", "settings"),
}

-- MINING TAB
local MiningGroupBox = Tabs.Main:AddLeftGroupbox("채굴", "pickaxe")

MiningGroupBox:AddDropdown("OreType", {
	Values = { 
		"Pebble", "Rock", "Boulder",
		"Basalt Rock", "Basalt Core", "Basalt Vein", "Volcanic Rock"
	},
	Default = 1,
	Text = "광석 유형 선택",
	Callback = function(Value)
		GameModule.SelectedOreType = Value
		GameModule.clearSkippedOres()
	end,
})

MiningGroupBox:AddDivider()

MiningGroupBox:AddToggle("AutoFarm", {
	Text = "자동 채굴 활성화",
	Default = false,
	Callback = function(value)
		if value then
			GameModule.clearSkippedOres()
			GameModule.startAutoMining()
		else
			GameModule.stopAutoMining()
		end
	end,
})

local SettingsGroupBox = Tabs.Main:AddRightGroupbox("설정", "settings")

SettingsGroupBox:AddSlider("MiningSpeed", {
	Text = "속도",
	Default = 50,
	Min = 1,
	Max = 50,
	Rounding = 0,
	Callback = function(Value)
		GameModule.MiningSpeed = Value
	end,
})

SettingsGroupBox:AddSlider("OreTeleportOffset", {
	Text = "광석 위 높이",
	Default = 5,
	Min = 5,
	Max = 50,
	Rounding = 1,
	Callback = function(Value)
		GameModule.OreTeleportOffset = Value
	end,
})

SettingsGroupBox:AddDivider()

SettingsGroupBox:AddToggle("EnableAntiStuck", {
	Text = "위치 초기화",
	Default = true,
	Callback = function(value)
		GameModule.EnableAntiStuck = value
	end,
})

SettingsGroupBox:AddSlider("StuckCheckDelay", {
	Text = "",
	Default = 3,
	Min = 1,
	Max = 10,
	Rounding = 1,
	Callback = function(Value)
		GameModule.StuckCheckDelay = Value
	end,
})

-- COMBAT TAB
local CombatGroupBox = Tabs.Combat:AddLeftGroupbox("적 사냥", "sword")

CombatGroupBox:AddDropdown("MobType", {
	Values = GameModule.MobTypes,
	Default = 1,
	Text = "적 선택",
	Callback = function(Value)
		GameModule.SelectedMobType = Value
		GameModule.clearSkippedMobs()
	end,
})

CombatGroupBox:AddDivider()

CombatGroupBox:AddToggle("자동 공격", {
	Text = "자동 공격 활성화",
	Default = false,
	Callback = function(value)
		if value then
			GameModule.clearSkippedMobs()
			GameModule.startAutoAttack()
		else
			GameModule.stopAutoAttack()
		end
	end,
})

local CombatSettings = Tabs.Combat:AddRightGroupbox("전투 설정", "settings")

CombatSettings:AddSlider("MoveSpeed", {
	Text = "속도",
	Default = 25,
	Min = 1,
	Max = 50,
	Rounding = 0,
	Callback = function(Value)
		GameModule.MoveSpeed = Value
	end,
})

CombatSettings:AddSlider("MobTeleportOffset", {
	Text = "적으로부터의 높이",
	Default = 15,
	Min = 5,
	Max = 50,
	Rounding = 1,
	Callback = function(Value)
		GameModule.MobTeleportOffset = Value
	end,
})

CombatSettings:AddDivider()

CombatSettings:AddToggle("EnableAntiStuck", {
	Text = "위치 초기화",
	Default = true,
	Callback = function(value)
		GameModule.CombatEnableAntiStuck = value
	end,
})

CombatSettings:AddSlider("StuckCheckDelay", {
	Text = "",
	Default = 2,
	Min = 1,
	Max = 10,
	Rounding = 1,
	Callback = function(Value)
		GameModule.CombatStuckCheckDelay = Value
	end,
})

CombatSettings:AddDivider()

local PotionsGroupBox = Tabs.Combat:AddLeftGroupbox("자동 물약")

PotionsGroupBox:AddToggle("AutoDamagePotion", {
	Text = "자동 대미지 물약",
	Default = false,
	Callback = function(value)
		GameModule.AutoUsePotions.Damage = value
		if value then
			GameModule.startAutoPotions()
		end
	end,
})

PotionsGroupBox:AddToggle("오토스피드포션", {
	Text = "대미지의 비약",
	Default = false,
	Callback = function(value)
		GameModule.AutoUsePotions.Speed = value
		if value then
			GameModule.startAutoPotions()
		end
	end,
})

PotionsGroupBox:AddToggle("AutoHealthPotion", {
	Text = "건강의 비약",
	Default = false,
	Callback = function(value)
		GameModule.AutoUsePotions.Health = value
		if value then
			GameModule.startAutoPotions()
		end
	end,
})

PotionsGroupBox:AddToggle("AutoMinerPotion", {
	Text = "광부의 비약",
	Default = false,
	Callback = function(value)
		GameModule.AutoUsePotions.Miner = value
		if value then
			GameModule.startAutoPotions()
		end
	end,
})

PotionsGroupBox:AddToggle("오토럭포션", {
	Text = "자동화된 행운의 물약",
	Default = false,
	Callback = function(value)
		GameModule.AutoUsePotions.Luck = value
		if value then
			GameModule.startAutoPotions()
		end
	end,
})

-- PLAYER TAB
local PlayerGroupBox = Tabs.Player:AddLeftGroupbox("이동")

PlayerGroupBox:AddToggle("플라이", {
	Text = "항공편 모드",
	Default = false,
	Callback = function(value)
		PlayerModule.FlyEnabled = value
		if value then
			PlayerModule.startFly()
		else
			PlayerModule.stopFly()
		end
	end,
})

PlayerGroupBox:AddSlider("FlySpeed", {
	Text = "비행 속도",
	Default = 50,
	Min = 10,
	Max = 200,
	Rounding = 0,
	Callback = function(Value)
		PlayerModule.FlySpeed = Value
	end,
})

PlayerGroupBox:AddDivider()

PlayerGroupBox:AddToggle("InfiniteJump", {
	Text = "끝없는 점프",
	Default = false,
	Callback = function(value)
		PlayerModule.InfiniteJumpEnabled = value
	end,
})

PlayerModule.setupInfiniteJump()

local TeleportGroupBox = Tabs.Player:AddLeftGroupbox("전송류 아이템")

TeleportGroupBox:AddButton({
	Text = "상점",
	Func = function()
		PlayerModule.teleportToLocation(Vector3.new(-152.43, 28.29, 117.26))
	end,
})

TeleportGroupBox:AddButton({
	Text = "인챈터",
	Func = function()
		PlayerModule.teleportToLocation(Vector3.new(-260.09, 20.06, 30.93))
	end,
})

TeleportGroupBox:AddDivider()

for _, location in ipairs(PlayerModule.getProximityLocations()) do
	TeleportGroupBox:AddButton({
		Text = location.name,
		Func = function()
			local position = PlayerModule.getProximityPosition(location.path)
			if position then
				PlayerModule.teleportToLocation(position)
			end
		end,
	})
end

local PlayerSettings = Tabs.Player:AddRightGroupbox("플레이어 통계")

PlayerSettings:AddSlider("WalkSpeed", {
	Text = "걷기 속도",
	Default = 1,
	Min = 1,
	Max = 10,
	Rounding = 1,
	Callback = function(Value)
		PlayerModule.WalkSpeedMultiplier = Value
	end,
})

PlayerModule.setupWalkSpeed()

local ServerGroupBox = Tabs.Player:AddRightGroupbox("서버")

ServerGroupBox:AddButton({
	Text = "서버 홉",
	Func = function()
		PlayerModule.serverHop()
	end,
})

ServerGroupBox:AddButton({
	Text = "서버 재가입",
	Func = function()
		PlayerModule.rejoinServer()
	end,
})

-- SELL SYSTEM
local SellGroupBox = Tabs.Player:AddLeftGroupbox("팔기")

SellGroupBox:AddToggle("AutoSellEnable", {
	Text = "판매 활성화",
	Default = false,
	Callback = function(value)
		PlayerModule.AutoSellEnabled = value
		if value then
			PlayerModule.startAutoSell()
			Library:Notify("Auto Sell Enabled", 2)
		else
			PlayerModule.stopAutoSell()
			Library:Notify("Auto Sell Disabled", 2)
		end
	end,
})

SellGroupBox:AddDivider()

SellGroupBox:AddLabel("Select Items to Auto Sell")

SellGroupBox:AddDropdown("SellOres", {
	Values = PlayerModule.getOres(),
	Default = 1,
	Multi = true,
	Text = "광물",
	Callback = function(value)
		PlayerModule.clearSellItems()
		for itemName, selected in pairs(value) do
			if selected then
				PlayerModule.addSellItem(itemName)
			end
		end
		if Options.SellEssences then
			for itemName, selected in pairs(Options.SellEssences.Value) do
				if selected then
					PlayerModule.addSellItem(itemName)
				end
			end
		end
		if Options.SellMaterials then
			for itemName, selected in pairs(Options.SellMaterials.Value) do
				if selected then
					PlayerModule.addSellItem(itemName)
				end
			end
		end
	end,
})

SellGroupBox:AddDropdown("SellEssences", {
	Values = PlayerModule.getEssences(),
	Default = 1,
	Multi = true,
	Text = "에센스",
	Callback = function(value)
		PlayerModule.clearSellItems()
		if Options.SellOres then
			for itemName, selected in pairs(Options.SellOres.Value) do
				if selected then
					PlayerModule.addSellItem(itemName)
				end
			end
		end
		for itemName, selected in pairs(value) do
			if selected then
				PlayerModule.addSellItem(itemName)
			end
		end
		if Options.SellMaterials then
			for itemName, selected in pairs(Options.SellMaterials.Value) do
				if selected then
					PlayerModule.addSellItem(itemName)
				end
			end
		end
	end,
})

SellGroupBox:AddDropdown("SellMaterials", {
	Values = PlayerModule.getMaterials(),
	Default = 1,
	Multi = true,
	Text = "소재",
	Callback = function(value)
		PlayerModule.clearSellItems()
		if Options.SellOres then
			for itemName, selected in pairs(Options.SellOres.Value) do
				if selected then
					PlayerModule.addSellItem(itemName)
				end
			end
		end
		if Options.SellEssences then
			for itemName, selected in pairs(Options.SellEssences.Value) do
				if selected then
					PlayerModule.addSellItem(itemName)
				end
			end
		end
		for itemName, selected in pairs(value) do
			if selected then
				PlayerModule.addSellItem(itemName)
			end
		end
	end,
})

SellGroupBox:AddDivider()

SellGroupBox:AddButton({
	Text = "선택 지우기",
	Func = function()
		PlayerModule.clearSellItems()
		Options.SellOres:SetValue({})
		Options.SellEssences:SetValue({})
		Options.SellMaterials:SetValue({})
		Library:Notify("Selection cleared", 2)
	end,
})

SellGroupBox:AddButton({
	Text = "자동 판매 수정",
	Func = function()
		local success, message = PlayerModule.fixAutoSell()
		if success then
			Library:Notify(message, 2)
		else
			Library:Notify(message or "Failed to fix auto sell", 3)
		end
	end,
})

-- UI SETTINGS TAB
local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("메뉴", "wrench")

MenuGroup:AddToggle("KeybindMenuOpen", {
	Default = false,
	Text = "키바인드 메뉴 열기",
	Callback = function(value)
		Library.KeybindFrame.Visible = value
	end,
})

MenuGroup:AddDropdown("NotificationSide", {
	Values = { "Left", "Right" },
	Default = "Right",
	Text = "알림 측면",
	Callback = function(Value)
		Library:SetNotifySide(Value)
	end,
})

MenuGroup:AddDivider()
MenuGroup:AddLabel("Menu bind"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "메뉴 키 바인드" })

MenuGroup:AddButton("Unload", function()
	GameModule.cleanup()
	Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })

ThemeManager:SetFolder("AutoFarmHub")
SaveManager:SetFolder("AutoFarmHub")

SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])

Library:OnUnload(function()
	GameModule.cleanup()
end)