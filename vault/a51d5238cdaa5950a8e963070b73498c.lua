-- Portuguese Translation
-- Script ID: a51d5238cdaa5950a8e963070b73498c
-- Created: 2026-05-15T22:05:23.176Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()

local moduleUrl = "https://raw.githubusercontent.com/checkurasshole/INK/main/KaijuAlphaModule"
local FlyModule = loadstring(game:HttpGet(moduleUrl))()

FlyModule.init()

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local Window = Library:CreateWindow({
    Title            = "COMBOWICK",
    Icon             = 6031071057,
    NotifySide       = "Right",
    ShowCustomCursor = false,
    Size             = UDim2.new(0, 560, 0, 500),
})

local Tabs = {
    Main = Window:AddTab("Main", "star"),
}

local Left  = Tabs.Main:AddLeftGroupbox("Fly")
local Right = Tabs.Main:AddRightGroupbox("Spawn e Opções")

Left:AddToggle("FlyEnabled", {
    Text    = "Ativar Voo  [F]",
    Default = false,
    Callback = function(v)
        if v then FlyModule.startFly() else FlyModule.stopFly() end
    end,
})

Left:AddDropdown("FlyMode", {
    Text    = "Modo de Voo",
    Default = "PC",
    Values  = { "PC", "Mobile" },
    Callback = function(v)
        FlyModule.Config.FlyMode = v
        if FlyModule.Config.FlyEnabled then FlyModule.startFly() end
    end,
})

Left:AddSlider("FlySpeed", {
    Text     = "Velocidade de Voo",
    Default  = 22, Min = 5, Max = 50, Rounding = 0,
    Callback = function(v)
        FlyModule.Config.Speed = v
        local hum = FlyModule.getHum()
        if hum and FlyModule.Config.FlyEnabled then hum.WalkSpeed = v end
    end,
})

Left:AddToggle("AccelEnabled", {
    Text    = "Aceleração  (Somente PC)",
    Default = true,
    Callback = function(v) FlyModule.Config.Acceleration = v end,
})

Left:AddSlider("MaxAccelMult", {
    Text    = "Multiplicador Máximo de Aceleração",
    Default = 1.5, Min = 1.0, Max = 2.0, Rounding = 1,
    Callback = function(v) FlyModule.Config.MaxAccel = v end,
})

local kaijuLabel = Right:AddLabel("Kaiju: Detectando...")

local function refreshKaijuLabel()
    local name = FlyModule.getSelectedKaiju()
    kaijuLabel:SetText("Kaiju: " .. (name ~= "" and name or "None Detected"))
end

refreshKaijuLabel()

local ls = localPlayer:FindFirstChild("leaderstats")
local kv = ls and ls:FindFirstChild("Kaiju")
if kv then
    kv.Changed:Connect(refreshKaijuLabel)
end

Right:AddButton("Spawnar Kaiju Atual", function()
    FlyModule.fireSpawn()
end)

Right:AddToggle("AutoSpawnToggle", {
    Text    = "Habilidade Infinita / Vida Infinita",
    Default = true,
    Callback = function(v) FlyModule.Config.AutoSpawn = v end,
})

Right:AddDivider()

Right:AddButton("Unload", function()
    FlyModule.stopFly()
    FlyModule.stopFly2()
    Library:Unload()
end)

ThemeManager:SetLibrary(Library)
ThemeManager:SetFolder("KaijuScript")
ThemeManager:ApplyToTab(Tabs.Main)

local function showExpiredPopup(onDone)
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "CW_Expired"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    ScreenGui.Parent = game:GetService("CoreGui")

    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.fromOffset(280, 140)
    Frame.Position = UDim2.new(0.5, -140, 0, -150)
    Frame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
    Frame.BorderSizePixel = 0
    Frame.Parent = ScreenGui

    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 14)

    local Stroke = Instance.new("UIStroke", Frame)
    Stroke.Color = Color3.fromRGB(80, 80, 100)
    Stroke.Thickness = 1

    local Title = Instance.new("TextLabel", Frame)
    Title.Size = UDim2.new(1, -20, 0, 28)
    Title.Position = UDim2.fromOffset(10, 10)
    Title.BackgroundTransparency = 1
    Title.Text = "Sessão Gratuita Encerrada"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextSize = 14
    Title.Font = Enum.Font.GothamBold
    Title.TextXAlignment = Enum.TextXAlignment.Left

    local Sub = Instance.new("TextLabel", Frame)
    Sub.Size = UDim2.new(1, -20, 0, 34)
    Sub.Position = UDim2.fromOffset(10, 36)
    Sub.BackgroundTransparency = 1
    Sub.Text = "Obtenha uma chave em combowick.com\npara continuar sem tempos de recarga."
    Sub.TextColor3 = Color3.fromRGB(180, 180, 200)
    Sub.TextSize = 12
    Sub.Font = Enum.Font.Gotham
    Sub.TextXAlignment = Enum.TextXAlignment.Left
    Sub.TextWrapped = true

    local Button = Instance.new("TextButton", Frame)
    Button.Size = UDim2.new(1, -20, 0, 24)
    Button.Position = UDim2.fromOffset(10, 74)
    Button.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
    Button.BorderSizePixel = 0
    Button.Text = "Copiar Link da Chave"
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 12
    Button.Font = Enum.Font.GothamBold
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 6)
    Button.MouseButton1Click:Connect(function()
        pcall(function() setclipboard("https://combowick.com/verify/provider-select") end)
        Button.Text = "Copiado!"
        task.wait(1.5)
        Button.Text = "Copiar Link da Chave"
    end)

    local Bar = Instance.new("Frame", Frame)
    Bar.Size = UDim2.new(1, -20, 0, 3)
    Bar.Position = UDim2.fromOffset(10, 122)
    Bar.BackgroundColor3 = Color3.fromRGB(100, 100, 200)
    Bar.BorderSizePixel = 0
    Instance.new("UICorner", Bar).CornerRadius = UDim.new(1, 0)

    Frame:TweenPosition(UDim2.new(0.5, -140, 0, 20), Enum.EasingDirection.Out, Enum.EasingStyle.Quint, 0.4, true)

    local ts = game:GetService("TweenService")
    ts:Create(Bar, TweenInfo.new(20, Enum.EasingStyle.Linear), {Size = UDim2.new(0, 0, 0, 3)}):Play()

    task.delay(20, function()
        Frame:TweenPosition(UDim2.new(0.5, -140, 0, -150), Enum.EasingDirection.In, Enum.EasingStyle.Quint, 0.4, true)
        task.wait(0.5)
        ScreenGui:Destroy()
        if onDone then onDone() end
    end)
end

if _G.COMBOWICK_KEY then
    _G.COMBOWICK_FREE   = false
    _G.COMBOWICK_ACTIVE = nil
    _G.COMBOWICK_KEY    = false
end

if _G.COMBOWICK_FREE then
    _G.COMBOWICK_ACTIVE = true
    task.spawn(function()
        repeat task.wait(1) until _G.COMBOWICK_ACTIVE == false
        showExpiredPopup(function()
            Library:Unload()
        end)
    end)
end