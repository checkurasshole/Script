-- Protected Script (Portuguese)
-- Script ID: 50c97e3c93159b3734a61459e552c446
-- Created: 2025-10-22T20:35:37.317Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local CoreModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/jurrasiccccc"))()

local Window = Fluent:CreateWindow({
    Title = "COMB0_PAVIO",
    SubTitle = "",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    ReadThis = Window:AddTab({ Title = "Leia isto", Icon = "info" }),
    Main = Window:AddTab({ Title = "Principal", Icon = "home" }),
    Shop = Window:AddTab({ Title = "Loja", Icon = "shopping-cart" }),
    Gamepass = Window:AddTab({ Title = "Passes de Jogo", Icon = "gift" })
}

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "This is for trolling your friends, lol\n:Enabling shop features will not allow you to play!",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "R",
            Content = "T",
            Duration = 2
        })
        task.wait(0.5)
       -- game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

-- Initialize the core module
CoreModule:Init()

-- Main Tab
local CoinInput = Tabs.Main:AddInput("CoinInput", {
    Title = "Valor da moeda",
    Default = "10000",
    Placeholder = "Enter amount",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        local amount = tonumber(Value)
        if amount and amount > 0 then
            CoreModule:SetCoinAmount(amount)
        end
    end
})

local CoinToggle = Tabs.Main:AddToggle("CoinToggle", {
    Title = "Dê o valor das moedas",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleCoins(Value)
        if Value then
            Fluent:Notify({
                Title = "Operação concluída com êxito",
                Content = "Moedas alteradas para" .. tostring(CoreModule:GetCoinAmount()),
                Duration = 2
            })
        end
    end
})

-- Shop Tab
local DinosaurPurchaseToggle = Tabs.Shop:AddToggle("DinosaurPurchaseToggle", {
    Title = "Ignorar compra de dinossauro",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleDinosaurBypass(Value)
        if Value then
            Fluent:Notify({
                Title = "Operação concluída com êxito",
                Content = "Bypass de compra de dinossauros ativado",
                Duration = 2
            })
        end
    end
})

local UnlockAllButton = Tabs.Shop:AddButton({
    Title = "Desbloquear todos os dinossauros",
    Callback = function()
        CoreModule:UnlockAllDinosaurs()
        Fluent:Notify({
            Title = "Operação concluída com êxito",
            Content = "Todos os dinossauros desbloqueados visualmente",
            Duration = 3
        })
    end
})

-- Gamepass Tab
local GamepassToggle = Tabs.Gamepass:AddToggle("GamepassToggle", {
    Title = "Ignorar todos os passes de jogo",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleAllGamepasses(Value)
        if Value then
            Fluent:Notify({
                Title = "Operação concluída com êxito",
                Content = "Todos os passes de jogo ativados",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("PlayableAI", {
    Title = "Desbloqueie IA jogável",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716086049, Value)
        if Value then
            Fluent:Notify({
                Title = "Operação concluída com êxito",
                Content = "IA jogável desbloqueada",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("StealthPass", {
    Title = "Desbloquear Passe Furtivo",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716240706, Value)
        if Value then
            Fluent:Notify({
                Title = "Operação concluída com êxito",
                Content = "Passe Furtivo desbloqueado",
                Duration = 2
            })
        end
    end
})

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "Aderir novamente ao servidor",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "Rejoining...",
            Content = "Teletransportá-lo de volta ao jogo",
            Duration = 2
        })
        task.wait(0.5)
        game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

Window:SelectTab(1)