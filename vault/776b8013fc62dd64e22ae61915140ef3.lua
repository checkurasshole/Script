-- Protected Script (Russian)
-- Script ID: 776b8013fc62dd64e22ae61915140ef3
-- Created: 2025-10-22T20:35:36.321Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local CoreModule = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/jurrasiccccc"))()

local Window = Fluent:CreateWindow({
    Title = "COMB0_WICK",
    SubTitle = "",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    ReadThis = Window:AddTab({ Title = "Прочти это", Icon = "info" }),
    Main = Window:AddTab({ Title = "Главное", Icon = "home" }),
    Shop = Window:AddTab({ Title = "Магазин", Icon = "shopping-cart" }),
    Gamepass = Window:AddTab({ Title = "Проходы игры", Icon = "gift" })
}

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "This is for trolling your friends, lol\n:Enabling shop features will not allow you to play!",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "R",
            Content = "T",
            Duration = 2
        })
        task.wait(0.5)
       -- game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

-- Initialize the core module
CoreModule:Init()

-- Main Tab
local CoinInput = Tabs.Main:AddInput("CoinInput", {
    Title = "Сумма",
    Default = "10000",
    Placeholder = "Enter amount",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        local amount = tonumber(Value)
        if amount and amount > 0 then
            CoreModule:SetCoinAmount(amount)
        end
    end
})

local CoinToggle = Tabs.Main:AddToggle("Связи", {
    Title = "Сделайте размер койко-мест",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleCoins(Value)
        if Value then
            Fluent:Notify({
                Title = "Успех",
                Content = "Кольцо изменено на &lt; &lt;" .. tostring(CoreModule:GetCoinAmount()),
                Duration = 2
            })
        end
    end
})

-- Shop Tab
local DinosaurPurchaseToggle = Tabs.Shop:AddToggle("DinosaurPurchaseToggle", {
    Title = "Закупка динозавров",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleDinosaurBypass(Value)
        if Value then
            Fluent:Notify({
                Title = "Успех",
                Content = "Возможность шунтирования покупки динозавров",
                Duration = 2
            })
        end
    end
})

local UnlockAllButton = Tabs.Shop:AddButton({
    Title = "Отключить всех динозавров",
    Callback = function()
        CoreModule:UnlockAllDinosaurs()
        Fluent:Notify({
            Title = "Успех",
            Content = "Все динозавры открыты визуально",
            Duration = 3
        })
    end
})

-- Gamepass Tab
local GamepassToggle = Tabs.Gamepass:AddToggle("Геймпассвязь", {
    Title = "Проход всех игр",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleAllGamepasses(Value)
        if Value then
            Fluent:Notify({
                Title = "Успех",
                Content = "Включено все пропуски игры",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("Воспроизведение AI", {
    Title = "Отключить воспроизводимый AI",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716086049, Value)
        if Value then
            Fluent:Notify({
                Title = "Успех",
                Content = "Проигрываемый АИ открыт",
                Duration = 2
            })
        end
    end
})

Tabs.Gamepass:AddToggle("Украсть пасс", {
    Title = "Откройте проход по вертикали",
    Default = false,
    Callback = function(Value)
        CoreModule:ToggleGamepass(716240706, Value)
        if Value then
            Fluent:Notify({
                Title = "Успех",
                Content = "Пропуск взлома открыт",
                Duration = 2
            })
        end
    end
})

-- Rejoin Button
Tabs.ReadThis:AddButton({
    Title = "Вернуться в сервер",
    Description = "",
    Callback = function()
        Fluent:Notify({
            Title = "Rejoining...",
            Content = "Телепортирую тебя обратно в игру.",
            Duration = 2
        })
        task.wait(0.5)
        game:GetService("TeleportService"):Teleport(game.PlaceId, game:GetService("Players").LocalPlayer)
    end
})

Window:SelectTab(1)