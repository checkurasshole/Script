-- Steal Fish Eggs Module
-- Script ID: a4d7437234f9ca565c0a53969a2b374a
-- Created: 2026-09-11T04:57:43.834Z
-- Source: External API
-- Auto-generated by Lua Script Vault

--!nocheck
if _G.__CW_CLEAN then pcall(_G.__CW_CLEAN) end
local RUNNING = true
local CONNS, CLEANERS = {}, {}
local function track(c) if c then CONNS[#CONNS + 1] = c end return c end
local function onClean(fn) CLEANERS[#CLEANERS + 1] = fn end
_G.__CW_CLEAN = function()
    RUNNING = false
    for _, c in ipairs(CONNS) do pcall(function() c:Disconnect() end) end
    for _, fn in ipairs(CLEANERS) do pcall(fn) end
    CONNS, CLEANERS = {}, {}
end
local cloneref        = cloneref or clonereference or function(x) return x end
local Players         = cloneref(game:GetService("Players"))
local RunService      = cloneref(game:GetService("RunService"))
local Workspace       = cloneref(game:GetService("Workspace"))
local UIS             = cloneref(game:GetService("UserInputService"))
local VIM             = cloneref(game:GetService("VirtualInputManager"))
local TeleportService = cloneref(game:GetService("TeleportService"))
local HttpService     = cloneref(game:GetService("HttpService"))
local Lighting        = cloneref(game:GetService("Lighting"))
local Pathfinder      = cloneref(game:GetService("PathfindingService"))
local RS              = cloneref(game:GetService("ReplicatedStorage"))
local lp              = Players.LocalPlayer
local Opt, Toggles
local RARITY_LIST = { "Basic", "Rare", "Epic", "Legendary", "Mythic", "Abyssal", "Astral" }
local BIOME_LIST  = { "CoralReef", "DeepOcean", "PearlLagoon", "SnowySea", "VolcanicSea", "JellyOcean", "SunkenRuins", "Atlantis" }
local RARITY_COLOR = {
    Basic = Color3.fromRGB(190, 190, 190), Rare = Color3.fromRGB(60, 150, 255), Epic = Color3.fromRGB(170, 80, 255),
    Legendary = Color3.fromRGB(255, 170, 40), Mythic = Color3.fromRGB(255, 70, 70),
    Abyssal = Color3.fromRGB(70, 90, 160), Astral = Color3.fromRGB(80, 255, 220),
}
local NAMECHARS = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
local function randomName()
    local out = {}
    for i = 1, math.random(10, 18) do local k = math.random(1, #NAMECHARS); out[i] = NAMECHARS:sub(k, k) end
    return table.concat(out)
end
local function hiddenParent(gui)
    local h = gethui or get_hidden_gui
    if type(h) == "function" then local ok, dest = pcall(h); if ok and dest then gui.Parent = dest; return end end
    if syn and type(syn.protect_gui) == "function" then pcall(function() syn.protect_gui(gui) end); gui.Parent = cloneref(game:GetService("CoreGui")); return end
    if protectgui then pcall(function() protectgui(gui) end) end
    gui.Parent = lp:WaitForChild("PlayerGui")
end
local function hrp() local c = lp.Character; return c and c:FindFirstChild("HumanoidRootPart") end
local function humanoid() local c = lp.Character; return c and c:FindFirstChildWhichIsA("Humanoid") end
local function carrying() return lp:GetAttribute("CarryingEgg") == true end
local function commas(n)
    local s = string.format("%.0f", math.max(0, n or 0))
    return (s:reverse():gsub("(%d%d%d)", "%1,"):reverse():gsub("^,", ""))
end
local EggConfig
pcall(function()
    local es = RS:FindFirstChild("EggSystem")
    local ec = es and es:FindFirstChild("EggConfig")
    if ec then EggConfig = require(ec) end
end)
local function eggKg(m)
    local sc = tonumber(m:GetAttribute("Scale"))
    if EggConfig and EggConfig.GetKgFromScale and sc then
        local ok, kg = pcall(EggConfig.GetKgFromScale, sc); if ok and type(kg) == "number" then return kg end
    end
    return tonumber(m:GetAttribute("Kg")) or 0
end
local function eggName(m)
    local n = m:GetAttribute("EggType")
    if type(n) == "string" and n ~= "" then return n end
    return (tostring(m.Name):gsub("Model.*$", ""))
end
local function myBasePos()
    local bases = Workspace:FindFirstChild("Bases")
    if bases then
        for _, b in ipairs(bases:GetChildren()) do
            if tonumber(b:GetAttribute("OwnerUserId")) == lp.UserId then
                local sign = b:FindFirstChild("HomeIconSign")
                if sign and sign:IsA("BasePart") then return sign.Position end
                local ok, cf = pcall(function() return b:GetPivot() end); if ok and cf then return cf.Position end
            end
        end
    end
    if lp.RespawnLocation then return lp.RespawnLocation.Position end
    local r = hrp(); return r and r.Position or nil
end

local function selSet(key)
    local dd = Opt[key]; local v = dd and dd.Value
    if type(v) ~= "table" then return nil end
    local set, any = {}, false
    for k, val in pairs(v) do
        if val == true then set[k] = true; any = true elseif type(val) == "string" then set[val] = true; any = true end
    end
    return any and set or nil
end
local function matchesEgg(m)
    local rs = selSet("fe_rarities")
    if rs then local r = m:GetAttribute("Rarity"); if not (r and rs[r]) then return false end end
    local as = selSet("fe_areas")
    if as then local a = m:GetAttribute("Biome"); if not (a and as[a]) then return false end end
    return true
end
local function eggHost(m)
    local p = m:FindFirstChild("EggPrompt", true) or m:FindFirstChild("DroppedEggPrompt", true)
    local host = p and p.Parent
    if p and host and host:IsA("BasePart") then return p, host end
    return nil
end
local function myDroppedEgg()
    local f = Workspace:FindFirstChild("DroppedFishEggs"); if not f then return nil end
    local prefix = lp.Name .. "_Dropped_"
    local r = hrp(); local myPos = r and r.Position
    local best, bestD
    for _, m in ipairs(f:GetChildren()) do
        if m:IsA("Model") and m.Name:sub(1, #prefix) == prefix and eggHost(m) then
            local _, host = eggHost(m)
            local d = (host and myPos) and (host.Position - myPos).Magnitude or 0
            if not bestD or d < bestD then best, bestD = m, d end
        end
    end
    return best
end
local function nearestEggNear(pos, radius, includeCarried)
    local folders = { "SpawnedEggs" }
    if includeCarried then folders[#folders + 1] = "CarriedEggs" end
    local best, bestD
    for _, fn in ipairs(folders) do
        local f = Workspace:FindFirstChild(fn)
        if f then
            for _, m in ipairs(f:GetChildren()) do
                if m:IsA("Model") and matchesEgg(m) then
                    local _, host = eggHost(m)
                    if host then
                        local d = (host.Position - pos).Magnitude
                        if d <= (radius or math.huge) and d < (bestD or math.huge) then best, bestD = m, d end
                    end
                end
            end
        end
    end
    return best
end
local function nearestEgg()
    local r = hrp(); local p = r and r.Position; if not p then return nil end
    return nearestEggNear(p, math.huge, false)
end
local function pickTarget()
    if ((Opt.fe_priority and Opt.fe_priority.Value) or "Nearest") ~= "Highest Weight" then return nearestEgg() end
    local sf = Workspace:FindFirstChild("SpawnedEggs"); if not sf then return nil end
    local best, bestKg
    for _, m in ipairs(sf:GetChildren()) do
        if m:IsA("Model") and matchesEgg(m) and eggHost(m) then
            local kg = eggKg(m)
            if not bestKg or kg > bestKg then best, bestKg = m, kg end
        end
    end
    return best
end
local function getRemote(...)
    local o = RS
    for _, n in ipairs({ ... }) do o = o and o:FindFirstChild(n) end
    return o
end
local function farmOn() local t = Toggles and Toggles.fe_farm; return t and t.Value end
local function moveSpeed() return (Opt.fe_movespeed and Opt.fe_movespeed.Value) or 240 end
local STEP_CAP = 6
local function driveTo(pos, stopDist, keep, timeout)
    if typeof(pos) ~= "Vector3" then return false end
    local deadline = os.clock() + (timeout or 30)
    while (not keep or keep()) and os.clock() < deadline do
        local r = hrp(); if not r then return false end
        local hum = humanoid()
        if hum then pcall(function() hum:ChangeState(Enum.HumanoidStateType.Swimming); hum.Sit = false; hum.PlatformStand = false end) end
        local cur = r.Position
        local delta = pos - cur
        local dist = delta.Magnitude
        if dist <= (stopDist or 6) then r.AssemblyLinearVelocity = Vector3.zero; return true end
        local dt = RunService.Heartbeat:Wait()
        if typeof(dt) ~= "number" or dt <= 0 then dt = 1 / 60 end
        local dir = delta.Unit
        local step = math.min(dist, moveSpeed() * dt, STEP_CAP)
        local newPos = cur + dir * step
        local flat = Vector3.new(dir.X, 0, dir.Z)
        if flat.Magnitude > 0.01 then r.CFrame = CFrame.lookAt(newPos, newPos + flat.Unit)
        else r.CFrame = CFrame.new(newPos) * (r.CFrame - r.CFrame.Position) end
        r.AssemblyLinearVelocity = Vector3.zero
        r.AssemblyAngularVelocity = Vector3.zero
    end
    return false
end
local function computePath(toPos)
    local h = hrp(); if not h or typeof(toPos) ~= "Vector3" then return nil end
    local path
    local okc = pcall(function()
        path = Pathfinder:CreatePath({ AgentRadius = 4, AgentHeight = 6, AgentCanJump = false, WaypointSpacing = 8 })
    end)
    if not okc or not path then return nil end
    local ok = pcall(function() path:ComputeAsync(h.Position, toPos) end)
    if not ok or path.Status ~= Enum.PathStatus.Success then return nil end
    local pts = {}
    for _, wp in ipairs(path:GetWaypoints()) do pts[#pts + 1] = wp.Position end
    return (#pts > 1) and pts or nil
end
local function navTo(pos, stopDist, keep, timeout)
    if typeof(pos) ~= "Vector3" then return false end
    local pts = computePath(pos)
    if not pts then return driveTo(pos, stopDist, keep, timeout) end
    for i = 1, #pts do
        if keep and not keep() then return false end
        local sd = (i == #pts) and (stopDist or 6) or 5
        driveTo(pts[i], sd, keep, 12)
    end
    if keep and not keep() then return false end
    return driveTo(pos, stopDist, keep, 12)
end
local function nightBlocked()
    local nr = Workspace:FindFirstChild("NightRuntime")
    return nr ~= nil and nr:FindFirstChild("Blocker") ~= nil
end
local function stealEgg(m)
    local prompt, host = eggHost(m); if not (prompt and host) then return false end
    local reach = function() return RUNNING and farmOn() and m.Parent and matchesEgg(m) and not carrying() end
    if not navTo(host.Position, 8, reach, 25) then return false end
    local r = hrp(); if r then r.AssemblyLinearVelocity = Vector3.zero end
    task.wait(0.12)
    local key = prompt.KeyboardKeyCode
    if key == Enum.KeyCode.Unknown then key = Enum.KeyCode.E end
    local hold = math.max(prompt.HoldDuration, 0.05)
    pcall(function() VIM:SendKeyEvent(true, key, false, game) end)
    local t0 = os.clock()
    while os.clock() - t0 < hold + 0.6 do
        if carrying() then break end
        RunService.Heartbeat:Wait()
    end
    pcall(function() VIM:SendKeyEvent(false, key, false, game) end)
    return carrying()
end

local fps = 60
do
    local frames, t0 = 0, os.clock()
    track(RunService.RenderStepped:Connect(function()
        frames += 1
        local now = os.clock()
        if now - t0 >= 0.5 then fps = math.floor(frames / (now - t0) + 0.5); frames = 0; t0 = now end
    end))
end

local function lagOn() local t = Toggles and Toggles.fe_antilag; return t and t.Value end
local function stripEffects()
    for _, v in ipairs(Workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") then
            pcall(function() v.Enabled = false end)
        elseif v:IsA("PointLight") or v:IsA("SpotLight") or v:IsA("SurfaceLight") then
            pcall(function() v.Enabled = false end)
        elseif v:IsA("Decal") or v:IsA("Texture") then
            pcall(function() v.Transparency = 1 end)
        end
    end
end
local function applyAntiLag()
    pcall(function() Lighting.GlobalShadows = false end)
    pcall(function() Lighting.FogEnd = 1e9 end)
    pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
    pcall(function()
        local t = Workspace.Terrain
        t.WaterWaveSize = 0; t.WaterWaveSpeed = 0; t.WaterReflectance = 0; t.WaterTransparency = 1
    end)
    for _, n in ipairs({ "LocalChaserFishVisuals", "LocalPlayerFishVisuals", "LocalCarriedEggVisuals" }) do
        local f = Workspace:FindFirstChild(n)
        if f then for _, c in ipairs(f:GetChildren()) do pcall(function() c:Destroy() end) end end
    end
    stripEffects()
end
task.spawn(function()
    while RUNNING do
        if lagOn() then pcall(applyAntiLag) end
        task.wait(4)
    end
end)

local swimActive = false
track(RunService.RenderStepped:Connect(function()
    if not (RUNNING and swimActive) then return end
    local h = hrp(); local hum = humanoid(); if not (h and hum and hum.Health > 0) then return end
    pcall(function() hum:ChangeState(Enum.HumanoidStateType.Swimming) end)
    local cam = Workspace.CurrentCamera; if not cam then return end
    local cf = cam.CFrame
    local mode = (Opt.fe_swimmode and Opt.fe_swimmode.Value) or "Auto"
    if mode == "Auto" then mode = UIS.TouchEnabled and "Camera" or "WASD" end
    local dir = Vector3.zero
    if mode == "WASD" then
        if UIS:IsKeyDown(Enum.KeyCode.W) then dir += cf.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.S) then dir -= cf.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.A) then dir -= cf.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.D) then dir += cf.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.Space) then dir += Vector3.yAxis end
        if UIS:IsKeyDown(Enum.KeyCode.LeftShift) or UIS:IsKeyDown(Enum.KeyCode.LeftControl) then dir -= Vector3.yAxis end
        if dir.Magnitude < 0.1 and hum.MoveDirection.Magnitude > 0.1 then dir = hum.MoveDirection end
    elseif mode == "Camera" then
        if hum.MoveDirection.Magnitude > 0.1 then dir = cf.LookVector end
    else
        if hum.MoveDirection.Magnitude > 0.1 then dir = hum.MoveDirection end
    end
    if dir.Magnitude > 0.1 then
        local sp = (Opt.fe_swimspeed and Opt.fe_swimspeed.Value) or 60
        h.AssemblyLinearVelocity = dir.Unit * sp
    end
end))


-- cross-boundary bindings
local function _setSwim(v) swimActive = v end
local M = {}
M.track=track M.onClean=onClean
M.BIOME_LIST=BIOME_LIST M.HttpService=HttpService M.Lighting=Lighting M.RARITY_COLOR=RARITY_COLOR M.RARITY_LIST=RARITY_LIST M.RunService=RunService
M.TeleportService=TeleportService M.Workspace=Workspace M.applyAntiLag=applyAntiLag M.carrying=carrying M.cloneref=cloneref M.commas=commas
M.eggHost=eggHost M.eggKg=eggKg M.eggName=eggName M.getRemote=getRemote M.hiddenParent=hiddenParent M.hrp=hrp
M.lp=lp M.myBasePos=myBasePos M.myDroppedEgg=myDroppedEgg M.navTo=navTo M.nightBlocked=nightBlocked M.pickTarget=pickTarget
M.randomName=randomName M.selSet=selSet M.stealEgg=stealEgg
M.bindOpt=function(o,t) Opt=o; Toggles=t end
M.setSwim=_setSwim
M.getFps=function() return fps end
M.isRunning=function() return RUNNING end
M.cleanup=function() if _G.__CW_CLEAN then pcall(_G.__CW_CLEAN) end end
return M
