-- Protected Script (Russian)
-- Script ID: 30220a5b78defe17b3200846fcad4c50
-- Created: 2025-10-20T23:12:52.848Z
-- Source: External API
-- Auto-generated by Lua Script Vault


local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

-- Load Core Module from GitHub (replace with your raw GitHub link)
local Module = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/INK/refs/heads/main/Jurrasicscript"))()

-- Initialize connections
Module.initConnections()

-- Create Window
local Window = Library:CreateWindow({
    Title = "COMBO_WICK",
    Icon = 7734068321,
    NotifySide = "Right",
    ShowCustomCursor = true,
    Size = UDim2.new(0, 500, 0, 400),
})

-- Create Tabs
local Tabs = {
    Main = Window:AddTab("Главное", "zap"),
    Amber = Window:AddTab("Автожелтый", "gem"),
    Utility = Window:AddTab("Параметры", "wrench"),
}

-- Main Tab - Walkspeed
local MainWalkspeed = Tabs.Main:AddLeftGroupbox("Скорость движения", "activity")
MainWalkspeed:AddToggle("Скорость движения", {
    Text = "Использовать скорость ходьбы",
    Default = false,
    Callback = function(Value)
        Module.toggleWalkspeed(Value)
    end,
})
MainWalkspeed:AddSlider("Слайдер", {
    Text = "Скорость ходьбы",
    Default = 16,
    Min = 1,
    Max = 150,
    Rounding = 1,
    Callback = function(Value)
        Module.updateWalkspeed(Value)
    end,
})

-- Main Tab - Animal Farm
local MainNPC = Tabs.Main:AddLeftGroupbox("Ферма животных", "crosshair")
MainNPC:AddToggle("ЭСПТоггл", {
    Text = "Название ESP",
    Default = false,
    Callback = function(Value)
        Module.toggleESP(Value)
    end,
})
MainNPC:AddToggle("Подсветка", {
    Text = "Маркировочный знак ESP",
    Default = false,
    Callback = function(Value)
        Module.toggleHighlight(Value)
    end,
})
MainNPC:AddToggle("NCCTeleportToggle", {
    Text = "Телепорт для козлов",
    Default = false,
    Callback = function(Value)
        Module.toggleNPCTeleport(Value)
    end,
})
MainNPC:AddSlider("ВНП Расстояние", {
    Text = "Расстояние позади",
    Default = 5,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        Module.State.npcBehindDistance = Value
        if Module.State.npcTeleportEnabled and Module.State.currentNPCTarget then
            Module.anchorToNPC(Module.State.currentNPCTarget)
        end
    end,
})
MainNPC:AddButton({
    Text = "Следующий НПК",
    Func = function()
        if Module.State.npcTeleportEnabled then
            local newTarget = Module.switchToNextNPC()
            if newTarget then
                Module.State.currentNPCTarget = newTarget
                Module.anchorToNPC(Module.State.currentNPCTarget)
            end
        end
    end,
})

-- Main Tab - Boss Farm
local MainBoss = Tabs.Main:AddLeftGroupbox("Босс Ферма", "skull")
MainBoss:AddToggle("Босс Телепорт Тоггл", {
    Text = "Телепорт в D-Rex Boss",
    Default = false,
    Callback = function(Value)
        Module.toggleBossTeleport(Value)
    end,
})
MainBoss:AddSlider("Расстояние боссов", {
    Text = "Расстояние позади",
    Default = 5,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        Module.State.bossBehindDistance = Value
        if Module.State.bossTeleportEnabled and Module.State.currentBossTarget then
            Module.anchorToBoss(Module.State.currentBossTarget)
        end
    end,
})
MainBoss:AddButton({
    Text = "Следующий босс",
    Func = function()
        if Module.State.bossTeleportEnabled then
            local newTarget = Module.switchToNextBoss()
            if newTarget then
                Module.State.currentBossTarget = newTarget
                Module.anchorToBoss(Module.State.currentBossTarget)
            end
        end
    end,
})

-- Main Tab - Combat
local MainCombat = Tabs.Main:AddRightGroupbox("Боевые действия", "sword")
MainCombat:AddToggle("Автоматический табло", {
    Text = "Автоатакование",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoAttack(Value)
    end,
})
MainCombat:AddToggle("Автомобильный Toggle", {
    Text = "Боевой режим",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoHeavy(Value)
    end,
})
MainCombat:AddButton({
    Text = "Незамедлительное отключение",
    Func = function()
        Module.executeInstantDespawn()
    end,
})

-- Main Tab - Player Farm
local MainPlayer = Tabs.Main:AddRightGroupbox("Ферма игроков", "users")
MainPlayer:AddToggle("Телепортационная связь", {
    Text = "Телепорт для игроков",
    Default = false,
    Callback = function(Value)
        Module.State.teleporting = Value
        if Module.State.teleporting then
            Module.State.playerIndex = 0
            Module.State.currentTarget = Module.getNextTarget()
            if Module.State.currentTarget then
                Module.anchorToPlayer(Module.State.currentTarget)
            end
        else
            Module.State.currentTarget = nil
        end
    end,
})
MainPlayer:AddToggle("Целенаправленные задачи", {
    Text = "Перемещение цели",
    Default = true,
    Callback = function(Value)
        Module.State.targetMovingPlayers = Value
        if not Module.State.targetMovingPlayers and not Module.State.targetStillPlayers then
            Module.State.targetStillPlayers = true
        end
    end,
})
MainPlayer:AddToggle("Целесообразность", {
    Text = "Стационарный целевой показатель",
    Default = true,
    Callback = function(Value)
        Module.State.targetStillPlayers = Value
        if not Module.State.targetMovingPlayers and not Module.State.targetStillPlayers then
            Module.State.targetMovingPlayers = true
        end
    end,
})
MainPlayer:AddSlider("Задняя дистанция", {
    Text = "Расстояние позади",
    Default = 5,
    Min = 1,
    Max = 100,
    Rounding = 1,
    Callback = function(Value)
        Module.State.behindDistance = Value
        if Module.State.teleporting and Module.State.currentTarget then
            Module.anchorToPlayer(Module.State.currentTarget)
        end
    end,
})
MainPlayer:AddButton({
    Text = "Следующая цель",
    Func = function()
        if Module.State.teleporting then
            local validPlayers = Module.getValidPlayers()
            if #validPlayers > 0 then
                Module.State.currentTarget = Module.getNextTarget()
                if Module.State.currentTarget then
                    Module.anchorToPlayer(Module.State.currentTarget)
                end
            end
        end
    end,
})

-- Amber Tab - V1
local AmberV1 = Tabs.Amber:AddLeftGroupbox("Автожелтый V1", "gem")
AmberV1:AddToggle("AmberV1 Toggle", {
    Text = "Коллекция автожелтых",
    Default = false,
    Callback = function(Value)
        Module.toggleAmberCollectionSwim(Value)
    end,
})

-- Amber Tab - V2
local AmberV2 = Tabs.Amber:AddRightGroupbox("Автожелтый V2", "zap")
AmberV2:AddToggle("Автожелтый V2 Toggle", {
    Text = "Коллекционирование динозавров v2",
    Default = false,
    Callback = function(Value)
        Module.toggleAmberCollection(Value)
    end,
})
AmberV2:AddToggle("АвтоSpaonToggle", {
    Text = "Автоматическое восстановление",
    Default = false,
    Callback = function(Value)
        Module.State.AUTO_SPAWN_ENABLED = Value
        if Value then
            Module.State.lastVisibleGuis = {}
        end
    end,
})
AmberV2:AddDropdown("DinoSelection", {
    Values = Module.allDinosaurs,
    Default = "Gallimimus",
    Text = "Выбрать динозавров",
    Callback = function(Value)
        Module.State.selectedDino = Value
        Module.State.SPAWN_ARGS = { [1] = Value }
    end,
})
AmberV2:AddButton({
    Text = "Выбранный ручной спаун",
    Func = function()
        Module.executeSpawn()
    end,
})
AmberV2:AddSlider("Автомобиль &quot; EmberTimeout &quot;", {
    Text = "Время ожидания (s)",
    Default = 15,
    Min = 5,
    Max = 60,
    Rounding = 1,
    Callback = function(Value)
        Module.State.amberAttemptTimeout = Value
    end,
})

-- Amber Tab - V3 (Premium)
local AmberV3 = Tabs.Amber:AddLeftGroupbox("Более быстрая версия автожелтого цвета", "zap")
local linkText = "Buy Premium Best DinoCoin Script here:\nhttps://discord.gg/9FWBQnVXCy"
AmberV3:AddLabel(linkText)
AmberV3:AddButton("Copy Link", function()
    if setclipboard then
        setclipboard("https://discord.gg/9FWBQnVXCy")
    elseif toclipboard then
        toclipboard("https://discord.gg/9FWBQnVXCy")
    else
        warn("Clipboard not supported in this executor")
    end
end)

-- Utility Tab - Anti-Void
local UtilityAntiVoid = Tabs.Utility:AddLeftGroupbox("Противоядие", "shield")
UtilityAntiVoid:AddToggle("Антивидение", {
    Text = "Противодействие Пустоте",
    Default = false,
    Callback = function(Value)
        if Value then
            Module.enableAntiVoid()
        else
            Module.disableAntiVoid()
        end
    end,
})

-- Utility Tab - Other
local UtilityOther = Tabs.Utility:AddLeftGroupbox("Прочее", "zap")
UtilityOther:AddToggle("NoclipToggle", {
    Text = "Ноклип",
    Default = false,
    Callback = function(Value)
        if Value then
            Module.enableNoclip()
        else
            Module.disableNoclip()
        end
    end,
})
UtilityOther:AddToggle("АнтиAFKToggle", {
    Text = "АнтиАФК",
    Default = false,
    Callback = function(Value)
        if Value then
            Module.enableAntiAfk()
        else
            Module.disableAntiAfk()
        end
    end,
})
UtilityOther:AddToggle("Организация &lt; &lt; Наблюдение за персоналом &gt; &gt;", {
    Text = "Ищу банальный молоток (кадровые часы)",
    Default = false,
    Callback = function(Value)
        Module.toggleStaffWatch(Value)
    end,
})

-- Utility Tab - Combat
local Combat = Tabs.Utility:AddRightGroupbox("Боевые действия", "sword")
Combat:AddToggle("Автоматический табло", {
    Text = "Автоатакование",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoAttack(Value)
    end,
})
Combat:AddToggle("Автомобильный Toggle", {
    Text = "Боевой режим",
    Default = false,
    Callback = function(Value)
        Module.toggleAutoHeavy(Value)
    end,
})
Combat:AddButton({
    Text = "Быстрое самоубийство (опустошённое)",
    Func = function()
        Module.executeInstantDespawn()
    end,
})

-- Utility Tab - UI Settings
local UISettings = Tabs.Utility:AddRightGroupbox("Параметры UI", "settings")
UISettings:AddToggle("Keybind Menu Open", {
    Default = Library.KeybindFrame.Visible,
    Text = "Открыть меню ключей",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})
UISettings:AddToggle("Показать CustomCoursor", {
    Text = "Обычный куратор",
    Default = true,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})
UISettings:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = "Right",
    Text = "Обратная сторона уведомления",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})
UISettings:AddButton("Unload", function()
    Library:Unload()
end)

-- Setup Theme and Save Manager
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("ComboChronicle")
SaveManager:SetFolder("ComboChronicle")
SaveManager:BuildConfigSection(Tabs.Utility)
ThemeManager:ApplyToTab(Tabs.Utility)

-- Cleanup on close
game:BindToClose(Module.cleanup)
SaveManager:LoadAutoloadConfig()