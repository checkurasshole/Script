-- English (Original)
-- Script ID: e80f181933df1a7765bcf356f345d008
-- Created: 2026-09-07T07:39:22.514Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local MODULE_URL = "https://raw.githubusercontent.com/checkurasshole/combowick-stealanegg/main/spawner_module.lua"
local Core = loadstring(game:HttpGet(MODULE_URL))()
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/main/Library.lua"))()
local Window = Library:CreateWindow({ Title = "COMBO_WICK", Footer = "", NotifySide = "Right", ShowCustomCursor = false, AutoShow = true, Size = UDim2.fromOffset(640, 520) })
local Opt = Library.Options
Core.bindOpt(Opt)
local categoryList, mutationList, reqmod, child, dir, rarityOf, weightOf, earnRate, makePetItem, fmt = Core.categoryList, Core.mutationList, Core.reqmod, Core.child, Core.dir, Core.rarityOf, Core.weightOf, Core.earnRate, Core.makePetItem, Core.fmt
local spawnPet, activeCount, activeCapacity, placeEggDirect, addBackpackEgg, setMoney, sellFakePets, applyShopSellFlag = Core.spawnPet, Core.activeCount, Core.activeCapacity, Core.placeEggDirect, Core.addBackpackEgg, Core.setMoney, Core.sellFakePets, Core.applyShopSellFlag
local unequipFakePet, clearAll, unlockAllIndexVisual, saveProfile, grantGear, unequipBat, unequipAllRealPets, track = Core.unequipFakePet, Core.clearAll, Core.unlockAllIndexVisual, Core.saveProfile, Core.grantGear, Core.unequipBat, Core.unequipAllRealPets, Core.track
local DataF, PET_LABEL_TO_KEY, FAKE_ROSTER, FAKE_INV, SaveMod, Gears, SHOP, lp, TeleportService = Core.DataF, Core.PET_LABEL_TO_KEY, Core.FAKE_ROSTER, Core.FAKE_INV, Core.SaveMod, Core.Gears, Core.SHOP, Core.lp, Core.TeleportService
do
    local InfoTab = Window:AddTab("Info", "info")
    local ibox = InfoTab:AddLeftGroupbox("Info")
    ibox:AddLabel("Unequip your pets first.")
end
local Tab = Window:AddTab("Spawner", "egg")
local PetBox = Tab:AddLeftGroupbox("Pet")
local cats = categoryList()
PetBox:AddDropdown("sp_cat", { Text = "Animal", Values = cats, Default = (table.find(cats, "Lava Dragon") and "Lava Dragon") or cats[1], Searchable = true })
PetBox:AddDropdown("sp_mut", { Text = "Mutation", Values = mutationList(), Multi = true, AllowNull = true, Default = {} })
PetBox:AddDropdown("sp_pers", { Text = "Personality", Values = { "Auto", "Roll", "Normal", "InvertedModel", "Lazy", "Loyal", "Energetic", "Shy", "Scared", "ExtremelyEnergetic", "UltraLoyal", "JumpCrazy" }, Default = "Auto" })
PetBox:AddDropdown("sp_color", { Text = "Color", Values = { "Auto", "Roll", "Base" }, Default = "Auto" })
do
    local skinVals = { "None" }
    local ES = reqmod(child(DataF, "EggSkins")); local dir = (ES and (ES.Directory or ES)) or {}
    if type(dir) == "table" then for k, v in pairs(dir) do if type(k) == "string" and type(v) == "table" and v.ModelName then skinVals[#skinVals + 1] = k end end end
    table.sort(skinVals)
    PetBox:AddDropdown("sp_eggskin", { Text = "Egg Skin", Values = skinVals, Default = "None", Searchable = true })
end
if Opt.sp_eggskin then Core.setCW_SKIN(Opt.sp_eggskin.Value or "None"); track(Opt.sp_eggskin:OnChanged(function() Core.setCW_SKIN(Opt.sp_eggskin.Value or "None") end)) end
PetBox:AddSlider("sp_scale", { Text = "Size", Default = 3, Min = 0.2, Max = 50, Rounding = 2 })
PetBox:AddInput("sp_weight", { Text = "Weight (Kg)", Numeric = true, Finished = true, Placeholder = "optional" })
if Opt.sp_pers then Core.setCW_PERS(Opt.sp_pers.Value or "Auto"); track(Opt.sp_pers:OnChanged(function() Core.setCW_PERS(Opt.sp_pers.Value or "Auto") end)) end
if Opt.sp_color then Core.setCW_COLOR(Opt.sp_color.Value or "Auto"); track(Opt.sp_color:OnChanged(function() Core.setCW_COLOR(Opt.sp_color.Value or "Auto") end)) end
local Info = Tab:AddRightGroupbox("Info")
local lblRarity = Info:AddLabel("Rarity: -")
local lblWeight = Info:AddLabel("Weight: -")
local lblRate = Info:AddLabel("Income: -")
local lblStatus = Info:AddLabel("Status: idle")
local function setStatus(s) pcall(function() lblStatus:SetText("Status: " .. tostring(s)) end) end
local function curMut()
    local v = Opt.sp_mut and Opt.sp_mut.Value
    local out = {}
    if type(v) == "table" then
        for k, val in pairs(v) do
            local m = (val == true) and k or val
            if type(m) == "string" and m ~= "None" and m ~= "" then out[#out + 1] = m end
        end
    elseif type(v) == "string" and v ~= "None" and v ~= "" then out[#out + 1] = v end
    return out
end
local function curCat()
    local sel = (Opt.sp_cat and Opt.sp_cat.Value) or cats[1]
    return PET_LABEL_TO_KEY[sel] or sel
end
local function curScale()
    local w = tonumber(Opt.sp_weight and Opt.sp_weight.Value)
    if w and w > 0 then
        local e = dir(curCat()); local mw = e and tonumber(e.ModelWeight) or 1
        return math.clamp((w / math.max(mw, 0.0001)) ^ (1 / 3), 0.01, 500)
    end
    return (Opt.sp_scale and Opt.sp_scale.Value) or 1
end
local function refreshInfo()
    local cat, scale, mut = curCat(), curScale(), curMut()
    pcall(function() lblRarity:SetText("Rarity: " .. rarityOf(cat)) end)
    pcall(function() lblWeight:SetText(("Weight: %.2f Kg  (scale %.2f)"):format(weightOf(cat, scale), scale)) end)
    pcall(function() lblRate:SetText("Income: $" .. fmt(earnRate(cat, scale, mut, makePetItem(cat, scale, mut))) .. "/s") end)
end
if Opt.sp_cat then track(Opt.sp_cat:OnChanged(refreshInfo)) end
if Opt.sp_scale then track(Opt.sp_scale:OnChanged(refreshInfo)) end
if Opt.sp_weight then track(Opt.sp_weight:OnChanged(refreshInfo)) end
if Opt.sp_mut then track(Opt.sp_mut:OnChanged(refreshInfo)) end
task.spawn(function() task.wait(1); refreshInfo() end)
local Spawn = Tab:AddLeftGroupbox("Spawn")
Spawn:AddButton({ Text = "Spawn Pet", Func = function()
    task.spawn(function()
        local uid, rate, note = spawnPet(curCat(), curScale(), curMut())
        if not uid then setStatus(rate) return end
        if note == "capped" then
            setStatus(("cap %d/%d - sent to Backpack"):format(activeCount(), activeCapacity()))
            return
        end
        setStatus(("pet: %s - $%s/s"):format((Opt.sp_cat and Opt.sp_cat.Value) or curCat(), fmt(rate)))
    end)
end })
Spawn:AddButton({ Text = "Spawn Egg on Plot", Func = function()
    task.spawn(function()
        local ok, r = placeEggDirect(curCat(), curScale(), curMut())
        setStatus(ok and "egg placed - walk up & Hatch" or ("egg failed: " .. tostring(r)))
    end)
end })
Spawn:AddButton({ Text = "Add Egg to Backpack", Func = function()
    local ok, r = addBackpackEgg(curCat(), curScale(), curMut())
    setStatus(ok and "egg -> backpack" or ("egg failed: " .. tostring(r)))
end })
Spawn:AddToggle("sp_eggtimer", { Text = "Egg growth timer", Default = true, Callback = function(v) Core.setEGG_TIMER(v) end })
Spawn:AddToggle("sp_freeskip", { Text = "Free skip growth", Default = false, Tooltip = "ON = 'Skip Growth!' is instant & free. OFF = use the Robux purchase (Shop tab spoofed balance) to skip.", Callback = function(v) Core.setFREE_SKIP(v) end })
local Econ = Tab:AddRightGroupbox("Economy")
Econ:AddInput("sp_money", { Text = "Amount", Numeric = true, Finished = true, Placeholder = "money amount" })
Econ:AddButton({ Text = "Set Money", Func = function()
    local ok, r = setMoney(Opt.sp_money and Opt.sp_money.Value)
    setStatus(ok and ("money -> $" .. fmt(r)) or ("money failed: " .. tostring(r)))
end })
Econ:AddButton({ Text = "Sell Pets", Func = function()
    task.spawn(function() local ok, r = sellFakePets(); setStatus(tostring(r)) end)
end })
Econ:AddToggle("sp_shopsell", { Text = "Unfavorite pets", Default = true, Callback = function(v)
    if v and not _G.__CW_SHOPSELL then
        setStatus("unavailable (executor lacks hookmetamethod) - use 'Sell Pets' instead")
        return
    end
    Core.setSHOP_SELL(v)
    applyShopSellFlag()
    if v then
        local active = {}
        for uid in pairs(FAKE_ROSTER) do active[#active + 1] = uid end
        for _, uid in ipairs(active) do pcall(unequipFakePet, uid) end
        setStatus(("unfavorited - %d pet(s) moved to backpack, now sellable at the shop"):format(#active))
    else
        setStatus("re-favorited - pets hidden from the shop again")
    end
end })
if Opt.sp_shopsell and Opt.sp_shopsell.Value and _G.__CW_SHOPSELL and not Core.getSHOP_SELL() then Core.setSHOP_SELL(true); pcall(applyShopSellFlag) end
local Sess = Tab:AddRightGroupbox("Session")
Sess:AddButton({ Text = "Clear Spawned Pets", Func = function() clearAll(); setStatus("cleared all spawned pets & eggs") end })
Sess:AddButton({ Text = "Rejoin Server", Func = function()
    setStatus("rejoining...")
    task.spawn(function()
        local ok = pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, lp) end)
        if not ok then pcall(function() TeleportService:Teleport(game.PlaceId, lp) end) end
    end)
end })
local Idx = Tab:AddLeftGroupbox("Index")
Idx:AddToggle("sp_indexauto", { Text = "Index on spawn", Default = true, Callback = function(v) Core.setINDEX_ON_SPAWN(v) end })
Idx:AddButton({ Text = "Unlock All Index", Func = function()
    local ok, n = unlockAllIndexVisual()
    setStatus(ok and ("index: all unlocked & shown as claimed (" .. tostring(n) .. " new)") or ("index failed: " .. tostring(n)))
end })
local BossBox = Tab:AddRightGroupbox("Boss")
BossBox:AddInput("sp_bosstokens", { Text = "Boss Tokens", Numeric = true, Finished = true, Placeholder = "amount" })
BossBox:AddButton({ Text = "Set Boss Tokens", Func = function()
    local n = tonumber(Opt.sp_bosstokens and Opt.sp_bosstokens.Value)
    if not (n and SaveMod and SaveMod.ApplyLocalField) then setStatus("boss tokens unavailable"); return end
    pcall(SaveMod.ApplyLocalField, "BossTokens", n); setStatus("boss tokens -> " .. fmt(n))
end })
BossBox:AddButton({ Text = "Unlock Boss Mastery", Func = function()
    local prof = saveProfile()
    if not (prof and SaveMod and SaveMod.ApplyLocalField) then setStatus("boss mastery unavailable"); return end
    local BM = reqmod(child(DataF, "BossMastery"))
    local state = (type(prof.BossMastery) == "table") and table.clone(prof.BossMastery) or (BM and BM.Blank and BM.Blank()) or {}
    state.Mastery = 30
    local claimed = {}
    local list = BM and (BM.Milestones or BM.List)
    if type(list) == "table" then for _, m in ipairs(list) do if type(m) == "table" and m.Id then claimed[m.Id] = true end end end
    if not next(claimed) then for _, id in ipairs({ "Mastery3", "Mastery5", "Mastery10", "Mastery15", "Mastery20", "Mastery30" }) do claimed[id] = true end end
    state.ClaimedMilestoneIds = claimed
    pcall(SaveMod.ApplyLocalField, "BossMastery", state)
    pcall(function() SaveMod.FieldSignal("BossMastery"):Fire(state, state) end)
    setStatus("boss mastery: 30 kills + milestones shown claimed (visual)")
end })
local function extraGearList()
    local out = {}
    if Gears and Gears.Directory then
        for id, g in pairs(Gears.Directory) do
            if type(g) == "table" and g.IndexBatTier == nil and id ~= "Bat" then
                out[#out + 1] = id
            end
        end
    end
    table.sort(out)
    if #out == 0 then out = { "BeeLauncher" } end
    return out
end
local GearBox = Tab:AddRightGroupbox("Gear")
GearBox:AddDropdown("sp_gear", { Text = "Slap / Gadget", Values = extraGearList(), Default = extraGearList()[1], Searchable = true })
GearBox:AddButton({ Text = "Equip Gear", Func = function()
    local id = Opt.sp_gear and Opt.sp_gear.Value
    task.spawn(function() local ok, r = grantGear(id); setStatus(ok and ("equipped " .. tostring(r)) or ("gear failed: " .. tostring(r))) end)
end })
GearBox:AddButton({ Text = "Unequip Gear", Func = function() unequipBat(); setStatus("gear unequipped") end })
local ShopTab = Window:AddTab("Shop", "shopping-cart")
local ShopBal = ShopTab:AddLeftGroupbox("Robux Balance")
ShopBal:AddInput("shop_bal", { Text = "Balance", Numeric = true, Finished = true, Default = tostring(SHOP.balance), Placeholder = "e.g. 1000000" })
ShopBal:AddButton({ Text = "Apply Balance", Func = function() SHOP.setBalance(Opt.shop_bal and Opt.shop_bal.Value) end })
ShopBal:AddButton({ Text = "+100k", Func = function() SHOP.setBalance(SHOP.getBalance() + 100000); if Opt.shop_bal then pcall(function() Opt.shop_bal:SetValue(tostring(SHOP.getBalance())) end) end end })
ShopBal:AddButton({ Text = "+1M", Func = function() SHOP.setBalance(SHOP.getBalance() + 1000000); if Opt.shop_bal then pcall(function() Opt.shop_bal:SetValue(tostring(SHOP.getBalance())) end) end end })
local ShopCfg = ShopTab:AddRightGroupbox("Purchase")
ShopCfg:AddToggle("shop_drop", { Text = "Drop balance on buy", Default = SHOP.dropOnBuy, Tooltip = "Reduce the spoofed Robux by the item's price after each purchase.", Callback = function(v) SHOP.dropOnBuy = v end })
ShopCfg:AddToggle("shop_instant", { Text = "Skip prompt (instant buy)", Default = SHOP.instant, Tooltip = "ON = purchases complete with no prompt at all. OFF = the real prompt shows and we catch its Buy.", Callback = function(v) SHOP.instant = v end })
local shopStatus = ShopCfg:AddLabel("Idle. Open a shop item and press Buy.", true)
SHOP.onStatus = function(msg) pcall(function() shopStatus:SetText(tostring(msg)) end) end
local SettingsTab = Window:AddTab("Settings", "settings")
do
    local ok = pcall(function()
        local ThemeManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/main/addons/ThemeManager.lua"))()
        local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/main/addons/SaveManager.lua"))()
        ThemeManager:SetLibrary(Library)
        SaveManager:SetLibrary(Library)
        SaveManager:IgnoreThemeSettings()
        SaveManager:SetIgnoreIndexes({})
        ThemeManager:SetFolder("COMBO_WICK")
        SaveManager:SetFolder("COMBO_WICK/StealAnEgg")
        SaveManager:BuildConfigSection(SettingsTab)
        ThemeManager:ApplyToTab(SettingsTab)
        SaveManager:LoadAutoloadConfig()
    end)
    if not ok then SettingsTab:AddLeftGroupbox("Settings"):AddLabel("Config addons failed to load (HttpGet blocked?)", true) end
end
local LoadoutBox = SettingsTab:AddRightGroupbox("Loadout")
LoadoutBox:AddToggle("sp_unequipreal", { Text = "Unequip all real pets", Default = true, Tooltip = "ON = unequip every REAL (non-fake) active pet via the game's own unequip flow (server-side, affects your actual loadout). Fires once on execution.", Callback = function(v)
    if not v then return end
    task.spawn(function()
        local ok, n = unequipAllRealPets()
        setStatus(ok and ("unequipped " .. tostring(n) .. " real pet(s)") or ("unequip failed: " .. tostring(n)))
    end)
end })
LoadoutBox:AddButton({ Text = "Unequip Real Pets", Func = function()
    task.spawn(function()
        local ok, n = unequipAllRealPets()
        setStatus(ok and ("unequipped " .. tostring(n) .. " real pet(s)") or ("unequip failed: " .. tostring(n)))
    end)
end })
task.spawn(function()
    for _ = 1, 20 do if lp.Character and lp.Character:FindFirstChildWhichIsA("Humanoid") then break end task.wait(0.5) end
    local t = Opt.sp_unequipreal
    if t and t.Value == false then return end
    local empties = 0
    for _ = 1, 12 do
        local tt = Opt.sp_unequipreal
        if tt and tt.Value == false then return end
        local ok, n = unequipAllRealPets()
        if ok and n and n > 0 then setStatus("unequipped " .. tostring(n) .. " real pet(s) on start"); empties = 0
        else empties = empties + 1 end
        if empties >= 3 then break end
        task.wait(1)
    end
end)
local InterfaceBox = SettingsTab:AddLeftGroupbox("Interface")
pcall(function()
    InterfaceBox:AddLabel("Toggle GUI"):AddKeyPicker("MenuToggle", { Default = "RightShift", Text = "Toggle GUI", Mode = "Toggle", NoUI = false })
    Library.ToggleKeybind = Opt.MenuToggle
end)
Library:OnUnload(function()
    if _G.__CW_SPAWN_CLEAN then pcall(_G.__CW_SPAWN_CLEAN) end
    Library.Unloaded = true
end)
Library:Notify({ Title = "COMBO_WICK - Spawner", Time = 4 })
