-- Korean Translation
-- Script ID: 8187edf54d928770947bc7b1f8d41f7f
-- Updated: 2026-09-03T03:12:00.010Z
-- Auto-generated by Lua Script Vault

if _G.COMBOWICK_ACTIVE then return end

local HttpService = game:GetService("HttpService")
local Players     = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

pcall(function() if not game:IsLoaded() then game.Loaded:Wait() end end)
do
    local t = tick()
    while not LocalPlayer and (tick() - t) < 10 do task.wait(); LocalPlayer = Players.LocalPlayer end
end
if not LocalPlayer then return end
if not game.PlaceId or game.PlaceId == 0 then return end

local MODULE_URL = "https://raw.githubusercontent.com/checkurasshole/combowick-loader/main/combowick_module.lua"
local repo       = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"

local M
do
    local ok, m = pcall(function() return loadstring(game:HttpGet(MODULE_URL))() end)
    if ok and type(m) == "table" then M = m end
end
if not M then
    warn("[COMBOWICK] module failed to load from " .. tostring(MODULE_URL))
    return
end

-- [i18n] Translated builds set M.EXPIRED_TITLE / M.EXPIRED_SUB here (English uses module defaults).
M.EXPIRED_TITLE = "무료 세션 종료됨"
M.EXPIRED_SUB   = "combowick.com에서 키를 받아\n대기 시간 없이 계속하세요."

M.queueOnTeleport()

-- Vault whitelist path removed (it auto-loaded INK in every game). Straight to
-- the new key path: if a saved key still validates for this game, load its scripts
-- (auto-run 1, picker for 2+) and skip the free-session flow. Returning key users.
if M.tryNewKey() then return end

local preCheck = M.withRetry(M.checkSession, M.hwid)
if preCheck ~= nil and preCheck.status ~= "game_not_allowed" then
    if M.tryFreeSession(preCheck) then return end
end
preCheck = preCheck or { status = "error", remaining = 0, disabled = false }

do
    pcall(function() if getgenv and getgenv().Library then getgenv().Library:Unload() end end)
    local targets = {}
    if gethui then local ok, h = pcall(gethui); if ok and h then table.insert(targets, h) end end
    table.insert(targets, game:GetService("CoreGui"))
    for _, target in ipairs(targets) do
        for _, v in ipairs(target:GetChildren()) do
            local n = v.Name
            if n == "Obsidian" or n == "ObsidianLoading" or n == "LinoriaGui" then pcall(v.Destroy, v) end
        end
    end
end

local Library      = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager  = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()
local Options      = Library.Options

local VaultModule       = M.VaultModule
local execPremiumScript = M.execPremiumScript

local function safeSet(lbl, text) pcall(function() lbl:SetText(text) end) end

local Window = Library:CreateWindow({
    Title = "COMBOWICK", Footer = "Free Version", NotifySide = "Right",
    ShowCustomCursor = false, AutoShow = true, Size = UDim2.fromOffset(560, 340),
})
local Tabs = {
    Main    = Window:AddTab("메인",        "user"),
    Key     = Window:AddTab("프리미엄 키", "key"),
    Suggest = Window:AddTab("Suggestions", "message-square"),
    UI      = Window:AddTab("설정", "settings"),
}

if M.looksTampered() then
    Library:Notify({Title="⚠️ Tampering", Description="Modified functions were detected in your executor.", Time=6})
end

local preRemaining = tonumber(preCheck.remaining) or 0
local preStatus      = preCheck.status
local gameDisabled   = preCheck.disabled == true
local gameUnsupported = preStatus == "game_not_allowed"

local InfoGroup = Tabs.Main:AddLeftGroupbox("무료 세션", "info")
local lSession  = InfoGroup:AddLabel(
    (not gameDisabled and (preStatus == "allowed" or preStatus == "session_active") and preRemaining > 0)
        and string.format("세션 시간: %s", M.formatTime(preRemaining)) or "세션 시간: —", true)
local lCooldown = InfoGroup:AddLabel(
    (preStatus == "cooldown" and preRemaining > 0)
        and string.format("대기 시간: %s", M.formatTime(preRemaining)) or "대기 시간: —", true)
if gameUnsupported then
    InfoGroup:AddLabel("🚫 이 게임은 아직 지원되지 않습니다.", true)
elseif gameDisabled then
    InfoGroup:AddLabel("⛔ 이 게임에서는 무료 버전을 사용할 수 없습니다.", true)
else
    InfoGroup:AddLabel("무료 버전은 모든 게임에서 제공되지 않습니다.", true)
end
InfoGroup:AddDivider()
InfoGroup:AddLabel("11시간 키로 업그레이드하여 대기 시간을 제거하세요.", true)
InfoGroup:AddButton({
    Text = "키 링크 복사", Tooltip = "combowick.com/verify/provider-select",
    Func = function()
        pcall(function() setclipboard(M.KEY_URL) end)
        Library:Notify({Title="COMBOWICK", Description="링크가 복사되었습니다. 브라우저에 붙여넣으세요.", Time=4})
    end,
})
local StatusGroup = Tabs.Main:AddRightGroupbox("상태", "shield")
local statusLabel = StatusGroup:AddLabel("확인 중...", true)
local timerLabel  = StatusGroup:AddLabel("", true)
local hintLabel   = StatusGroup:AddLabel("", true)


local savedKey = M.loadSavedKey()
local keyInputValue = savedKey

local KeyGroup = Tabs.Key:AddLeftGroupbox("키 입력")
KeyGroup:AddInput("KeyInput", {
    Default          = savedKey,
    Numeric          = false,
    Finished         = false,
    ClearTextOnFocus = false,
    Text             = "키 🔑",
    Placeholder      = "여기에 키를 붙여넣으세요...",
    Callback         = function(v) keyInputValue = v end,
})
local function submitKey(key)
    key = M.cleanKey(key)
    if key == "" then
        Library:Notify({Title="⚠️ 키를 입력하지 않았습니다", Description="키를 입력해 주세요.", Time=3})
        return
    end
    Library:Notify({Title="🔑 확인 중", Description="키 확인 중...", Time=2})

    -- NEW backend: validate the key + get this game's scripts in one call.
    local data, err = M.validateKeyNew(key)
    if type(data) ~= "table" then
        local msg = "유효하지 않은 키입니다. 확인 후 다시 시도하세요."
        if err == "wrong_game" then
            msg = "이 키는 다른 게임용입니다. 이 게임에 맞는 키를 받으세요."
        elseif err == "network" or err == "no_request" then
            msg = "네트워크 오류입니다. 다시 시도해 주세요."
        end
        Library:Notify({Title="❌ 잘못된 키", Description=msg, Time=5})
        return
    end

    local scripts = data.scripts or {}
    if #scripts == 0 then
        Library:Notify({Title="🔓 키 유효", Description="이 게임에 설정된 스크립트가 아직 없습니다.", Time=5})
        return
    end

    -- Valid — remember the key for next time, close this GUI, show the selector.
    M.saveKeyLocal(key)
    Library:Notify({
        Title = "🔓 키 유효",
        Description = (#scripts == 1) and "스크립트 로딩 중..." or "스크립트를 선택하세요...",
        Time = 3,
    })
    task.wait(0.4)
    pcall(function() Library:Unload() end)
    M.showSelector(scripts)
end

local function readKeyFromBox()
    local raw
    pcall(function()
        local holder = Options and Options.KeyInput and Options.KeyInput.Holder
        local box = holder and holder:FindFirstChildOfClass("TextBox")
        if box then raw = box.Text end
    end)
    local key = M.cleanKey(raw)
    if key == "" then key = M.cleanKey(Options and Options.KeyInput and Options.KeyInput.Value) end
    if key == "" then key = M.cleanKey(keyInputValue) end
    return key
end

KeyGroup:AddButton({
    Text = "✅ 키 제출",
    Func = function() submitKey(readKeyFromBox()) end,
})
KeyGroup:AddButton({
    Text = "🔗 키 받기",
    Func = function()
        pcall(function() setclipboard(M.KEY_URL) end)
        Library:Notify({Title="복사됨", Description="키 링크가 복사되었습니다.", Time=2})
    end,
})

local HWIDGroup = Tabs.Key:AddRightGroupbox("내 HWID")
HWIDGroup:AddLabel(M.hwid, true)
HWIDGroup:AddButton({
    Text = "📋 HWID 복사",
    Func = function()
        pcall(function() setclipboard(M.hwid) end)
        Library:Notify({Title="복사됨", Description="HWID가 복사되었습니다.", Time=2})
    end,
})
HWIDGroup:AddButton({
    Text = "🔒 암호화된 HWID 복사",
    Func = function()
        pcall(function() setclipboard(M.encryptedHWID) end)
        Library:Notify({Title="복사됨", Description="암호화된 HWID가 복사되었습니다.", Time=2})
    end,
})

local FEEDBACK_COOLDOWN, lastFeedback = 20, 0
local ERRMAP = {
    empty = "메시지가 비어 있습니다.", no_links = "링크는 허용되지 않습니다.",
    too_long = "메시지가 너무 깁니다 (최대 500자).", rate_limited = "메시지 3개 한도에 도달했습니다. 나중에 다시 시도하세요.",
    busy = "서버가 사용 중입니다. 다시 시도하세요.", banned = "피드백을 보낼 수 없습니다.",
    discord_error = "전송 실패. 다시 시도하세요.", network = "네트워크 오류.",
    no_request = "실행기가 요청을 보낼 수 없습니다.", bad_response = "서버가 예기치 않은 응답을 반환했습니다.",
    unreachable = "실행기가 서버에 연결할 수 없습니다 (TLS 오류). 다른 실행기를 사용해 보세요.",
}
local FG = Tabs.Suggest:AddLeftGroupbox("제안 / 도움말", "message-square")
FG:AddDropdown("FeedbackType", {Text = "Type", Values = {"Suggestion", "Bug", "Help"}, Default = "Suggestion", Callback = function() end})
FG:AddInput("FeedbackMsg", {Default = "", Numeric = false, Finished = false, ClearTextOnFocus = false, Text = "메시지", Placeholder = "메시지를 입력하세요 (최대 500자, 링크 불가)"})
local fbStatus = FG:AddLabel("", true)
FG:AddButton({
    Text = "📨 보내기",
    Func = function()
        local now = tick()
        if now - lastFeedback < FEEDBACK_COOLDOWN then
            safeSet(fbStatus, string.format("%d초 후에 다시 보낼 수 있습니다.", math.ceil(FEEDBACK_COOLDOWN - (now - lastFeedback))))
            return
        end
        local ftype = tostring((Options.FeedbackType and Options.FeedbackType.Value) or "Suggestion"):lower()
        local msg   = M.trim((Options.FeedbackMsg and Options.FeedbackMsg.Value) or "")
        if msg == "" then safeSet(fbStatus, "메시지가 비어 있습니다."); return end
        if #msg > 500 then safeSet(fbStatus, "메시지가 너무 깁니다 (최대 500자)."); return end
        local low = msg:lower()
        if low:find("http", 1, true) or low:find("discord.gg", 1, true) or low:find("www.", 1, true) or low:find(".gg/", 1, true) then
            safeSet(fbStatus, "링크는 허용되지 않습니다."); return
        end
        safeSet(fbStatus, "전송 중...")
        task.spawn(function()
            local sok, err = M.sendFeedback(ftype, msg, (preCheck and preCheck.session_token) or "")
            if sok then
                lastFeedback = tick()
                safeSet(fbStatus, "✅ 전송됨. 감사합니다!")
                pcall(function() Options.FeedbackMsg:SetValue("") end)
                Library:Notify({Title = "COMBOWICK", Description = "피드백이 전송되었습니다. 감사합니다!", Time = 3})
            else
                safeSet(fbStatus, ERRMAP[err] or ("전송 실패: " .. tostring(err)))
            end
        end)
    end,
})
FG:AddLabel("시간당 최대 3개 메시지. 링크나 멘션 불가.", true)

local HelpGroup = Tabs.Suggest:AddRightGroupbox("도움말 / FAQ", "life-buoy")
HelpGroup:AddButton({
    Text = "💬 디스코드 참여",
    Tooltip = M.DISCORD_INVITE,
    Func = function()
        pcall(function() setclipboard(M.DISCORD_INVITE) end)
        Library:Notify({Title = "COMBOWICK", Description = "디스코드 링크가 복사되었습니다. 브라우저에 붙여넣으세요.", Time = 4})
    end,
})
HelpGroup:AddDivider()
HelpGroup:AddLabel("❓ 프리미엄은 어떻게 받나요?\ncombowick.com/verify에서 키를 받아 프리미엄 키 탭에 붙여넣으세요. 한 번만 누르면 됩니다.", true)
HelpGroup:AddLabel("⏳ 왜 대기 시간이 있나요?\n무료 버전은 세션 사이에 대기 시간이 있습니다. 11시간 키는 이를 완전히 제거합니다.", true)
HelpGroup:AddLabel("🎮 내 게임에서 작동하지 않아요.\n무료 버전은 모든 게임에서 제공되지 않습니다. 지원 게임 목록은 디스코드를 확인하세요.", true)
HelpGroup:AddLabel("🔑 내 HWID는 무엇인가요?\n하드웨어 ID입니다. 지원팀이 요청하면 프리미엄 키 탭에서 복사하세요.", true)

task.spawn(function()
    safeSet(statusLabel, "인증 중...")

    local result = M.withRetry(M.checkSession, M.hwid)

    if result == nil then
        safeSet(statusLabel, "상태: 실패")
        Library:Notify({Title="COMBOWICK", Description="세션 확인에 실패했습니다.", Time=5})

    elseif result.status == "allowed" or result.status == "session_active" then
        if result.disabled then
            safeSet(statusLabel, "⛔ 게임 비활성화됨")
            safeSet(hintLabel, "여기서는 무료 버전을 사용할 수 없습니다. 키를 받으세요.")
            Library:Notify({Title="COMBOWICK", Description="이 게임에서는 무료 버전이 비활성화되어 있습니다. 키를 사용하세요.", Time=6})
            return
        end
        local token = result.session_token
        if type(token) ~= "string" or #token < 20 then
            safeSet(statusLabel, "상태: 실패")
        else
            safeSet(statusLabel, "상태: 인증됨")
            Library:Notify({Title="COMBOWICK", Description="인증되었습니다.", Time=3})
            _G.COMBOWICK_FREE   = true
            _G.COMBOWICK_ACTIVE = true
            local remaining = tonumber(result.remaining) or 0
            task.spawn(function()
                while remaining > 0 do
                    local fmt = M.formatTime(remaining)
                    safeSet(timerLabel, string.format("남은 시간: %s", fmt))
                    safeSet(lSession, string.format("세션 시간: %s", fmt))
                    task.wait(1); remaining = remaining - 1
                end
                safeSet(timerLabel, "세션이 만료되었습니다.")
                safeSet(lSession, "세션 시간: 만료됨")
                _G.COMBOWICK_ACTIVE = false
                pcall(function() Library:Unload() end)
                M.showExpiredPopup(function() M.reloadExist() end)
            end)
            local scriptUrl = M.getScriptForGame(token)
            if M.loadFreeScript(scriptUrl, token) then task.wait(2); pcall(function() Library:Unload() end) end
        end

    elseif result.status == "cooldown" then
        local remaining = tonumber(result.remaining) or 0
        safeSet(statusLabel, "대기 중")
        safeSet(hintLabel, "11시간 키를 받거나 대기 시간이 끝날 때까지 기다리세요.")
        Library:Notify({Title="COMBOWICK", Description="대기 중입니다. 키를 받아 대기를 건너뛰세요.", Time=6})
        task.spawn(function()
            while remaining > 0 do
                local fmt = M.formatTime(remaining)
                safeSet(timerLabel, string.format("대기 시간: %s", fmt))
                safeSet(lCooldown, string.format("대기 시간: %s", fmt))
                task.wait(1); remaining = remaining - 1
            end
            safeSet(timerLabel, "대기 시간이 끝났습니다.")
            safeSet(lCooldown, "대기 시간: 완료")
            safeSet(hintLabel, "")
            Library:Notify({Title="COMBOWICK", Description="대기 시간이 끝났습니다. 다시 로드 중...", Time=3})
            pcall(function() Library:Unload() end)
            task.wait(0.5)
            M.reloadExist()
        end)

    elseif result.status == "throttled" then
        local remaining = tonumber(result.remaining) or 0
        safeSet(statusLabel, "요청이 너무 많음")
        safeSet(hintLabel, "요청이 너무 많습니다. 잠시 기다려 주세요.")
        Library:Notify({Title="COMBOWICK", Description="Too many requests. Please wait a moment.", Time=5})
        task.spawn(function()
            while remaining > 0 do
                safeSet(timerLabel, string.format("%s 후 재시도", M.formatTime(remaining)))
                task.wait(1); remaining = remaining - 1
            end
            safeSet(timerLabel, "다시 로드 중...")
            safeSet(hintLabel, "")
            pcall(function() Library:Unload() end)
            task.wait(0.5)
            M.reloadExist()
        end)

    elseif result.status == "disabled" then
        safeSet(statusLabel, "서비스 일시 중지됨")
        safeSet(hintLabel, "COMBOWICK가 일시적으로 오프라인입니다.")
        Library:Notify({Title="COMBOWICK", Description="현재 서비스가 비활성화되어 있습니다. 나중에 다시 시도하세요.", Time=6})

    elseif result.status == "banned" then
        safeSet(statusLabel, "차단됨")
        Library:Notify({Title="COMBOWICK", Description="차단되었습니다.", Time=5})

    elseif result.status == "game_not_allowed" then
        safeSet(statusLabel, "🚫 지원되지 않는 게임")
        safeSet(hintLabel, "이 게임은 아직 지원되지 않습니다. 스크립트가 있으면 프리미엄 키는 계속 작동합니다.")
        Library:Notify({Title="COMBOWICK", Description="이 게임은 아직 지원되지 않습니다.", Time=6})

    else
        safeSet(statusLabel, "Error")
        safeSet(hintLabel, "서버에 연결할 수 없습니다. 스크립트를 다시 실행하세요.")
        Library:Notify({Title="COMBOWICK", Description=tostring(result.message or "알 수 없는 오류가 발생했습니다."), Time=5})
    end
end)

local MenuGroup = Tabs.UI:AddLeftGroupbox("Menu", "wrench")
MenuGroup:AddToggle("ShowCustomCursor", {Text="사용자 지정 커서", Default=false, Callback=function(v) Library.ShowCustomCursor = v end})
MenuGroup:AddLabel("메뉴 단축키"):AddKeyPicker("MenuKeybind", {Default="RightShift", NoUI=true, Text="메뉴 단축키"})
MenuGroup:AddButton({Text="언로드", Func=function() pcall(function() Library:Unload() end) end})
Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({"MenuKeybind"})
ThemeManager:SetFolder("COMBOWICK")
SaveManager:SetFolder("COMBOWICK/game")
SaveManager:BuildConfigSection(Tabs.UI)
ThemeManager:ApplyToTab(Tabs.UI)
SaveManager:LoadAutoloadConfig()
