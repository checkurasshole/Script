-- Protected Script (French)
-- Script ID: 14f62aa96d427845e1d455e5dcc5ef9c
-- Created: 2025-10-27T13:59:28.007Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()


local moduleLink = "https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/AnimeRise"
local Module = loadstring(game:HttpGet(moduleLink))()

local Options = Library.Options
local Toggles = Library.Toggles

local Window = Library:CreateWindow({
    Title = "_COMBO WICK",
    Footer = "Version: 1.0",
    NotifySide = "Right",
    ShowCustomCursor = true,
    Size = UDim2.new(0, 460, 0, 480),
})

local Tabs = {
    Main = Window:AddTab("Principales", "user"),
    Pets = Window:AddTab("Animaux de compagnie", "package"),
    Misc = Window:AddTab("Divers", "sparkles"),
    ["UI Settings"] = Window:AddTab("Paramètres de l' interface", "settings"),
}

local LeftGroupBox = Tabs.Main:AddLeftGroupbox("Élevage automatique")
local RightGroupBox = Tabs.Main:AddRightGroupbox("Sélection NPC")
local MiscGroupBox = Tabs.Misc:AddLeftGroupbox("Divers")
local PetGroupBox = Tabs.Pets:AddLeftGroupbox("Gestion des animaux de compagnie")
local ShadowGroupBox = Tabs.Pets:AddRightGroupbox("Essence d'ombre")

LeftGroupBox:AddToggle("AutoRank", {
    Text = "Classement automatique vers le haut",
    Default = false,
    Callback = function(Value)
        getgenv().AutoRank = Value
    end,
})

LeftGroupBox:AddToggle("AutoAttack", {
    Text = "Attaque automatique",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAttack = Value
    end,
})

LeftGroupBox:AddToggle("GrabDrops", {
    Text = "Recueillir les éléments déposés",
    Default = false,
    Callback = function(Value)
        getgenv().GrabDrops = Value
    end,
})

LeftGroupBox:AddToggle("Zone d'achat automatique", {
    Text = "Zone d'achat automatique",
    Default = false,
    Callback = function(Value)
        getgenv().AutoBuyZone = Value
    end,
})

LeftGroupBox:AddToggle("AutoAr", {
    Text = "Renaissance automatique de l'Ascension",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAr = Value
    end,
})

LeftGroupBox:AddToggle("AttaqueNPC", {
    Text = "Caractère non-joueur d'attaque",
    Default = false,
    Callback = function(Value)
        getgenv().AttackNPC = Value
    end,
})

LeftGroupBox:AddToggle("AutoAttackNearest", {
    Text = "Attaquer automatiquement l'ennemi le plus proche",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAttackNearest = Value
    end,
})

Module.availableNPCs = Module.scanAllNPCs()
table.insert(Module.availableNPCs, 1, "None")

Module.NPCDropdown = RightGroupBox:AddDropdown("NPCSelector", {
    Values = Module.availableNPCs,
    Default = 1,
    Multi = false,
    Text = "Sélectionner le caractère non-joueur",
    Callback = function(Value)
        Module.selectedNPC = Value
    end,
})

RightGroupBox:AddButton({
    Text = "Entrer dans la bataille du raid",
    Func = function()
        Module.joinRaid()
    end,
})

RightGroupBox:AddToggle("AutoJoinRaid", {
    Text = "Entrer automatiquement dans la bataille du raid",
    Default = false,
    Callback = function(Value)
        getgenv().AutoJoinRaid = Value
    end,
})

PetGroupBox:AddToggle("AutoEquipBest", {
    Text = "Équiper automatiquement les animaux les plus forts",
    Default = false,
    Callback = function(Value)
        getgenv().AutoEquipBest = Value
    end,
})

PetGroupBox:AddSlider("EquipIntervalle", {
    Text = "Matériel Temps d'attente (secondes)",
    Default = 15,
    Min = 5,
    Max = 600,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        getgenv().EquipInterval = Value
    end,
})

PetGroupBox:AddButton({
    Text = "Équiper les animaux les plus forts (Manuel)",
    Func = function()
        Module.equipBestPets()
    end,
})

PetGroupBox:AddToggle("AutoRollEggs", {
    Text = "Accumulation automatique des oeufs",
    Default = false,
    Callback = function(Value)
        getgenv().AutoRollEggs = Value
    end,
})

PetGroupBox:AddDropdown("EggMap", {
    Values = {"Lost Temple", "Desert", "Snow", "Volcano"},
    Default = 1,
    Multi = false,
    Text = "Sélectionnez la carte pour l'éclosion d'oeufs",
    Callback = function(Value)
        getgenv().SelectedEggMap = Value
    end,
})

ShadowGroupBox:AddLabel("Shadow Essence Generator")
ShadowGroupBox:AddInput("ShadowAmount", {
    Default = "9e15",
    Numeric = false,
    Finished = false,
    Text = "Montant",
    Placeholder = "Enter amount (example: 9e15)",
})

ShadowGroupBox:AddButton({
    Text = "Générer l'essence de l'ombre infini",
    Func = function()
        local amount = Options.ShadowAmount.Value
        if amount == "" then return end
        Module.giveShadowEssence(amount)
    end,
})

MiscGroupBox:AddToggle("Anti-AFK", {
    Text = "Anti-Away-From-Keyboard",
    Default = false,
    Callback = function(Value)
        getgenv().AntiAFK = Value
    end,
})

local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Menu")

MenuGroup:AddToggle("KeybindMenuOuvrir", {
    Default = Library.KeybindFrame.Visible,
    Text = "Afficher le menu Raccourcis clavier",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})

MenuGroup:AddToggle("AfficherCurseurCustom", {
    Text = "Curseur de souris personnalisé",
    Default = true,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})

MenuGroup:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = "Right",
    Text = "Position de notification",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})

MenuGroup:AddDivider()
MenuGroup:AddLabel("Menu Hotkey"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Touche de menu" })

MenuGroup:AddButton("Close Script", function()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("AutoFarm")
SaveManager:SetFolder("AutoFarm/game")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])

Module.startLoops()