-- English (Original)
-- Script ID: 3f9e0a014ae6e3741f5362633f69c8b0
-- Created: 2026-05-13T19:04:17.716Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo         = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library      = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager  = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Module       = loadstring(game:HttpGet("https://raw.githubusercontent.com/checkurasshole/Script/main/Traitorvssherifftraitormodule"))()

local Options = Library.Options
local Toggles = Library.Toggles

Module.Initialize()

local Window = Library:CreateWindow({
    Title            = "combowick",
    Footer           = "knife.gg",
    ShowCustomCursor = false,
    NotifySide       = "Right",
    Size             = UDim2.new(0, 400, 0, 500),
})

local Tabs = {
    Main            = Window:AddTab("Main ",     "zap"),
    ["UI Settings"] = Window:AddTab("Settings ", "settings"),
}

local CombatGroup = Tabs.Main:AddLeftGroupbox("Combat ")

CombatGroup:AddToggle("AutoSlashToggle", {
    Text     = "Automatic knife cutting",
    Default  = false,
    Callback = function(v)
        if v then Module.StartAutoSlash() else Module.StopAutoSlash() end
    end,
})

CombatGroup:AddToggle("AutoThrowToggle", {
    Text     = "automatic knife throwing",
    Default  = false,
    Callback = function(v)
        if v then Module.StartAutoThrow() else Module.StopAutoThrow() end
    end,
})

CombatGroup:AddSlider("ThrowDelay", {
    Text     = "Throw Delay (seconds) ",
    Default  = 2.1, Min=1, Max=10, Rounding=1,
    Callback = function(v) Module.State.ThrowDelay = v end,
})

CombatGroup:AddDivider()

CombatGroup:AddButton({
    Text = "Teleport and Kill Nearest Enemy",
    Func = function() task.spawn(function() Module.TeleportAndKill(Library) end) end,
})

local GunGroup = Tabs.Main:AddLeftGroupbox("Gun ")

GunGroup:AddToggle("AimbotToggle", {
    Text     = "Aimbot (When Visible)",
    Default  = false,
    Callback = function(v)
        Module.State.AimbotEnabled = v
        if v then Module.StartAimbot() else Module.StopAimbot() end
    end,
})

local TargetGroup = Tabs.Main:AddRightGroupbox("Targeting ")

TargetGroup:AddToggle("TeamCheckToggle", {
    Text     = "Team Check (Ignore Teammates)",
    Default  = false,
    Callback = function(v)
        Module.State.TargetTeamCheck = v
        Library:Notify({
            Title       = "Team Check",
            Description = v and "Enabled — teammates will be ignored" or "Disabled — all players are targets",
            Time        = 2,
        })
    end,
})

local VisualsGroup = Tabs.Main:AddRightGroupbox("Visuals ")

VisualsGroup:AddToggle("ESPToggle", {
    Text     = "Player ESP",
    Default  = false,
    Callback = function(v)
        Module.State.ESPEnabled = v
        if v then Module.StartESP() else Module.StopESP() end
    end,
})

VisualsGroup:AddLabel("Enemy Highlight Color"):AddColorPicker("ESPFillColorPicker", {
    Default  = Color3.fromRGB(255, 50, 50),
    Title    = "Enemy Highlight Color",
    Callback = function(v) Module.State.ESPFillColor = v end,
})

VisualsGroup:AddLabel("Outline Color"):AddColorPicker("ESPOutlineColorPicker", {
    Default  = Color3.fromRGB(255, 255, 255),
    Title    = "Outline Color",
    Callback = function(v) Module.State.ESPOutlineColor = v end,
})

local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Menu", "wrench")

MenuGroup:AddToggle("KeybindMenuOpen", {
    Default  = Library.KeybindFrame.Visible,
    Text     = "Show Keybind Menu",
    Callback = function(v)
        Library.KeybindFrame.Visible = v
    end,
})

MenuGroup:AddToggle("ShowCustomCursorToggle", {
    Text     = "Custom Cursor",
    Default  = false,
    Callback = function(v)
        Library.ShowCustomCursor = v
    end,
})

MenuGroup:AddDropdown("NotificationSide", {
    Values   = { "Left", "Right" },
    Default  = "Right",
    Text     = "Notification Position",
    Callback = function(v)
        Library:SetNotifySide(v)
    end,
})

MenuGroup:AddDropdown("DPIDropdown", {
    Values   = { "50%", "75%", "100%", "125%", "150%", "175%", "200%" },
    Default  = "100%",
    Text     = "UI Scale",
    Callback = function(v)
        v = v:gsub("%%", "")
        Library:SetDPIScale(tonumber(v))
    end,
})

MenuGroup:AddSlider("UICornerSlider", {
    Text     = "Corner Roundness",
    Default  = Library.CornerRadius,
    Min      = 0,
    Max      = 20,
    Rounding = 0,
    Callback = function(v)
        Window:SetCornerRadius(v)
    end,
})

MenuGroup:AddDivider()
MenuGroup:AddLabel("Menu Keybind")
    :AddKeyPicker("MenuKeybind", { Default="RightShift", NoUI=true, Text="Menu Keybind" })

MenuGroup:AddButton("Unload Script", function()
    Module.Cleanup()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("combowick")
SaveManager:SetFolder("combowick/knifegg")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])
SaveManager:LoadAutoloadConfig()

Library:Notify({ Title="combowick", Description="Loaded successfully", Time=3 })