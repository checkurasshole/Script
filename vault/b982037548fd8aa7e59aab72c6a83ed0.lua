-- Chinese (Simplified) Translation
-- Script ID: b982037548fd8aa7e59aab72c6a83ed0
-- Created: 2026-01-13T17:33:45.135Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local AutoRebirth = false
local AutoClaimRewards = false
local AutoBuyEgg = false
local SelectedEgg = "BasicEgg"
local WinAmount = 100

local EggNames = {
    ["Basic Egg"] = "BasicEgg",
    ["Fairy Egg"] = "FairyEgg",
    ["Dragon Egg"] = "DragonEgg",
    ["Computer Egg (15M)"] = "ComputerEgg",
    ["Galaxy Egg (80M)"] = "GalaxyEgg",
    ["Evil Egg (250M)"] = "EvilEgg",
    ["Pirate Egg (3B)"] = "PirateEgg",
    ["Jungle Egg (20B)"] = "JungleEgg",
    ["Underworld Egg (60B)"] = "UnderworldEgg"
}

local Window = Library:CreateWindow({
    Title = "combo_wick",
    Footer = "所有功能",
    ShowCustomCursor = false,
    Size = UDim2.new(0, 400, 0, 450),
})

local MainTab = Window:AddTab("主页", "home")

local WinsGroup = MainTab:AddLeftGroupbox("奖励")
local EggGroup = MainTab:AddRightGroupbox("购买")
local SettingsGroup = MainTab:AddLeftGroupbox("选项")

WinsGroup:AddSlider("WinAmount", {Text = "总金额", Default = 100, Min = 1, Max = 1000, Rounding = 0, Callback = function(v) WinAmount = v end})

WinsGroup:AddButton({Text = "无限奖励", Func = function()
    task.spawn(function()
        local Remote = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("ProgressionControllerEvents"):WaitForChild("RewardWins")
        local args = {}
        for i = 1, WinAmount do
            task.spawn(function()
                pcall(function()
                    Remote:FireServer(unpack(args))
                end)
            end)
        end

    end)
end})

WinsGroup:AddDivider()

WinsGroup:AddToggle("AutoRebirth", {Text = "自动重生", Default = false, Callback = function(v)
    AutoRebirth = v
    if AutoRebirth then
        task.spawn(function()
            while AutoRebirth do
                pcall(function()
                    local args = {}
                    ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("RebirthEvents"):WaitForChild("PurchaseRebirth"):InvokeServer(unpack(args))
                end)
                task.wait(1)
            end
        end)
    end
end})

WinsGroup:AddButton({Text = "重生一次", Func = function()
    pcall(function()
        local args = {}
        ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("RebirthEvents"):WaitForChild("PurchaseRebirth"):InvokeServer(unpack(args))

    end)
end})

WinsGroup:AddDivider()

WinsGroup:AddToggle("AutoClaim", {Text = "自动领取奖励", Default = false, Callback = function(v)
    AutoClaimRewards = v
    if AutoClaimRewards then
        task.spawn(function()
            while AutoClaimRewards do
                for i = 1, 12 do
                    if not AutoClaimRewards then break end
                    pcall(function()
                        local args = {[1] = i}
                        ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("FreeGiftEvents"):WaitForChild("ClaimReward"):InvokeServer(unpack(args))
                    end)
                    task.wait(0.1)
                end
                task.wait(60)
            end
        end)
    end
end})

EggGroup:AddDropdown("EggSelect", {
    Text = "选择蛋",
    Default = 1,
    Values = {
        "Basic Egg",
        "Fairy Egg",
        "Dragon Egg",
        "Computer Egg (15M)",
        "Galaxy Egg (80M)",
        "Evil Egg (250M)",
        "Pirate Egg (3B)",
        "Jungle Egg (20B)",
        "Underworld Egg (60B)"
    },
    Callback = function(v)
        SelectedEgg = EggNames[v]
    end
})

EggGroup:AddToggle("AutoBuyEgg", {Text = "自动购买", Default = false, Callback = function(v)
    AutoBuyEgg = v
    if AutoBuyEgg then
        task.spawn(function()
            while AutoBuyEgg do
                pcall(function()
                    local args = {
                        [1] = SelectedEgg,
                        [2] = false,
                        [3] = false,
                        [4] = {}
                    }
                    ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("PetShopHandlerEvents"):WaitForChild("AttemptPurchase"):InvokeServer(unpack(args))
                end)
                task.wait(0.5)
            end
        end)
    end
end})

EggGroup:AddButton({Text = "购买一次", Func = function()
    pcall(function()
        local args = {
            [1] = SelectedEgg,
            [2] = false,
            [3] = false,
            [4] = {}
        }
        ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("PetShopHandlerEvents"):WaitForChild("AttemptPurchase"):InvokeServer(unpack(args))

    end)
end})

SettingsGroup:AddButton({Text = "卸载脚本", Func = function()
    AutoRebirth = false
    AutoClaimRewards = false
    AutoBuyEgg = false
    Library:Unload()
end})