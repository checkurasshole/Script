-- Protected Script (Russian)
-- Script ID: 7547b1feb2f9bb585b52b972b04593ea
-- Created: 2025-10-27T13:59:28.730Z
-- Source: External API
-- Auto-generated by Lua Script Vault

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()


local moduleLink = "https://raw.githubusercontent.com/checkurasshole/PET/refs/heads/main/AnimeRise"
local Module = loadstring(game:HttpGet(moduleLink))()

local Options = Library.Options
local Toggles = Library.Toggles

local Window = Library:CreateWindow({
    Title = "COMBO_WICK",
    Footer = "Version: 1.0",
    NotifySide = "Right",
    ShowCustomCursor = true,
    Size = UDim2.new(0, 460, 0, 480),
})

local Tabs = {
    Main = Window:AddTab("Главное", "user"),
    Pets = Window:AddTab("Животные", "package"),
    Misc = Window:AddTab("Разное", "sparkles"),
    ["UI Settings"] = Window:AddTab("Параметры интерфейса", "settings"),
}

local LeftGroupBox = Tabs.Main:AddLeftGroupbox("Автоматическое земледелие")
local RightGroupBox = Tabs.Main:AddRightGroupbox("Выбор НПК")
local MiscGroupBox = Tabs.Misc:AddLeftGroupbox("Разное")
local PetGroupBox = Tabs.Pets:AddLeftGroupbox("Peet Management")
local ShadowGroupBox = Tabs.Pets:AddRightGroupbox("Тень")

LeftGroupBox:AddToggle("АвтоRank", {
    Text = "Автоматическое ранжирование",
    Default = false,
    Callback = function(Value)
        getgenv().AutoRank = Value
    end,
})

LeftGroupBox:AddToggle("Автозащита", {
    Text = "Автоматическое нападение",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAttack = Value
    end,
})

LeftGroupBox:AddToggle("Хватка", {
    Text = "Собирайте сброшенные вещи",
    Default = false,
    Callback = function(Value)
        getgenv().GrabDrops = Value
    end,
})

LeftGroupBox:AddToggle("Автобус", {
    Text = "Автоматическая закупочная зона",
    Default = false,
    Callback = function(Value)
        getgenv().AutoBuyZone = Value
    end,
})

LeftGroupBox:AddToggle("Автоаэр", {
    Text = "Автоматическое возрождение Вознесения",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAr = Value
    end,
})

LeftGroupBox:AddToggle("Нападающий НПК", {
    Text = "Неиспытательный символ",
    Default = false,
    Callback = function(Value)
        getgenv().AttackNPC = Value
    end,
})

LeftGroupBox:AddToggle("AutoAttackNear", {
    Text = "Автоматически атаковать ближайший враг",
    Default = false,
    Callback = function(Value)
        getgenv().AutoAttackNearest = Value
    end,
})

Module.availableNPCs = Module.scanAllNPCs()
table.insert(Module.availableNPCs, 1, "None")

Module.NPCDropdown = RightGroupBox:AddDropdown("NPCSelector", {
    Values = Module.availableNPCs,
    Default = 1,
    Multi = false,
    Text = "Выбрать символ неиспытателя",
    Callback = function(Value)
        Module.selectedNPC = Value
    end,
})

RightGroupBox:AddButton({
    Text = "Введите рэйд Битва",
    Func = function()
        Module.joinRaid()
    end,
})

RightGroupBox:AddToggle("Автозапуск", {
    Text = "Автоматически введите рэйд Битва",
    Default = false,
    Callback = function(Value)
        getgenv().AutoJoinRaid = Value
    end,
})

PetGroupBox:AddToggle("АвтоэквипBest", {
    Text = "Автоматически наиболее крепкие питомцы",
    Default = false,
    Callback = function(Value)
        getgenv().AutoEquipBest = Value
    end,
})

PetGroupBox:AddSlider("Автомобиль &quot; EquipInterval &quot;", {
    Text = "Время ожидания оборудования (секунды)",
    Default = 15,
    Min = 5,
    Max = 600,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        getgenv().EquipInterval = Value
    end,
})

PetGroupBox:AddButton({
    Text = "Наиболее крепкие питомцы (руководство)",
    Func = function()
        Module.equipBestPets()
    end,
})

PetGroupBox:AddToggle("АвтоRollEggs", {
    Text = "Автоматически яйцеклетки Hatch",
    Default = false,
    Callback = function(Value)
        getgenv().AutoRollEggs = Value
    end,
})

PetGroupBox:AddDropdown("EggMap", {
    Values = {"Lost Temple", "Desert", "Snow", "Volcano"},
    Default = 1,
    Multi = false,
    Text = "Выбрать карту для яиц Hatching",
    Callback = function(Value)
        getgenv().SelectedEggMap = Value
    end,
})

ShadowGroupBox:AddLabel("Shadow Essence Generator")
ShadowGroupBox:AddInput("ShadowAmount", {
    Default = "9e15",
    Numeric = false,
    Finished = false,
    Text = "Сумма",
    Placeholder = "Enter amount (example: 9e15)",
})

ShadowGroupBox:AddButton({
    Text = "Создать Бесконечную тень",
    Func = function()
        local amount = Options.ShadowAmount.Value
        if amount == "" then return end
        Module.giveShadowEssence(amount)
    end,
})

MiscGroupBox:AddToggle("АнтиАФК", {
    Text = "Противоположная клавиатура",
    Default = false,
    Callback = function(Value)
        getgenv().AntiAFK = Value
    end,
})

local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Меню")

MenuGroup:AddToggle("Keybind Menu Open", {
    Default = Library.KeybindFrame.Visible,
    Text = "Показать меню Коротких вырезок клавиатуры",
    Callback = function(value)
        Library.KeybindFrame.Visible = value
    end,
})

MenuGroup:AddToggle("Показать CustomCoursor", {
    Text = "Обычный куратор мыши",
    Default = true,
    Callback = function(Value)
        Library.ShowCustomCursor = Value
    end,
})

MenuGroup:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" },
    Default = "Right",
    Text = "Позиция уведомления",
    Callback = function(Value)
        Library:SetNotifySide(Value)
    end,
})

MenuGroup:AddDivider()
MenuGroup:AddLabel("Menu Hotkey"):AddKeyPicker("MenuKeybind", { Default = "RightShift", NoUI = true, Text = "Настройка хот-дога@ info: whatsthis" })

MenuGroup:AddButton("Close Script", function()
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })
ThemeManager:SetFolder("AutoFarm")
SaveManager:SetFolder("AutoFarm/game")
SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])

Module.startLoops()